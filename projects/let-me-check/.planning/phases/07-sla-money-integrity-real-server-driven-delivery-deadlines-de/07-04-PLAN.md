---
phase: 07-sla-money-integrity-real-server-driven-delivery-deadlines-de
plan: 04
type: execute
wave: 4
depends_on: [01, 02, 03]
files_modified:
  - lmc-app/app/lib/database.types.ts
  - supabase/functions/sla-sweeper/index.ts
autonomous: false
requirements: [D-01, D-02, D-03, D-04, D-06]
user_setup:
  - service: supabase
    why: "Enable pg_cron + pg_net extensions and schedule the SLA sweeps (management API / dashboard)"
    dashboard_config:
      - task: "Enable pg_cron and pg_net extensions; schedule expire_stale_dispatching + expire_stale_filming every minute"
        location: "Supabase Dashboard -> Database -> Extensions / Cron (or via the orchestrator management API)"
  - service: stripe
    why: "Live Stripe test-mode account already configured (Phase 4); payouts/transfers/balance reuse existing secrets"
    dashboard_config:
      - task: "No new config — STRIPE secrets already set in Edge env from Phase 4"
        location: "Supabase Edge Function secrets"
must_haves:
  truths:
    - "Migrations 0015 AND 0016 are pushed live (0016 unconditional); the pgTAP suites pass against the live schema"
    - "pg_cron + pg_net are enabled and cron.job has rows for lmc-expire-dispatching AND lmc-sla-sweeper (NOT a separate lmc-expire-filming — the sweeper Edge fn owns the filming sweep + hold release)"
    - "trouble-report, scout-earnings, stripe-connect-payout are deployed with verify_jwt=true; webhooks redeployed (if touched) keep --no-verify-jwt"
    - "database.types.ts is regenerated so deadline_at/accepted_at are typed; tsc clean"
    - "On device: the filming countdown reads the real deadline and survives reopen; Trouble-Here releases the hold + pays the Scout; earnings + withdraw show/move real money"
  artifacts:
    - path: "supabase/functions/sla-sweeper/index.ts"
      provides: "Edge fn that cancels uncaptured PIs for SLA-missed checks (the hold-release the SQL sweeper cannot do)"
      contains: "paymentIntents.cancel"
  key_links:
    - from: "pg_cron job lmc-sla-sweeper"
      to: "expire_stale_filming() + authorized-hold PI cancel"
      via: "single Edge-fn path scheduled every minute (no redundant SQL lmc-expire-filming)"
      pattern: "cron.schedule"
---

<objective>
[BLOCKING] Live deploy + cron enable wave. Mirrors the Phase 5/6 deploy plan exactly: the orchestrator pushes the migration, enables pg_cron, schedules the sweeps, deploys the Edge Functions, regenerates types, and verifies cron rows. This wave is `autonomous: false` because it ends with the on-device human-verify gate for the real countdown + Trouble-Here + earnings.

A critical detail this wave closes: `expire_stale_filming()` (Plan 01) drives `no_scout` in SQL, but an AUTHORIZED (uncaptured) PaymentIntent is NOT released by `transition_check` or `stripe-refund` (stripe-refund only refunds captured charges — confirmed in source, Pitfall 4). So a tiny `sla-sweeper` Edge fn performs the actual `paymentIntents.cancel()` for SLA-missed checks, scheduled alongside the SQL sweep via pg_cron + pg_net.

Purpose: make the whole SLA + money chain live and prove it end-to-end.
Output: live schema + cron + functions + regenerated types + device sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-sla-money-integrity-real-server-driven-delivery-deadlines-de/07-RESEARCH.md
@.planning/STATE.md

@supabase/migrations/0015_sla_deadline.sql
@supabase/functions/trouble-report/index.ts
@supabase/functions/stripe-refund/index.ts

<interfaces>
Supabase project ref: cawqasszfbzvbtunamda (from MEMORY).
LESSON (STATE.md / Phase 6): ANY webhook redeploy MUST keep `--no-verify-jwt` (mux-webhook, stripe-webhook). User/service functions keep verify_jwt=true. The three Plan-02 functions are user-facing → verify_jwt=true.
pg_cron guard already in 0015 (safe). This wave EXPLICITLY enables the extension so the guard's `cron.schedule` actually runs.
sla-sweeper should be service-to-service (called by pg_cron via pg_net with the service-role bearer) → verify_jwt can be false OR it checks the service-role key; follow the existing service-invoke pattern (mux-webhook style) and deploy with --no-verify-jwt since pg_net calls it with the service role key in the Authorization header.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: sla-sweeper Edge fn — release uncaptured holds for SLA-missed checks</name>
  <files>supabase/functions/sla-sweeper/index.ts</files>
  <read_first>supabase/functions/trouble-report/index.ts (PI cancel pattern from Plan 02), supabase/functions/stripe-refund/index.ts (why uncaptured PIs can't be refunded), 07-RESEARCH "Refund + Hold-Release Wiring"</read_first>
  <action>
Implement a small service-role Edge fn that:
(1) runs expire_stale_filming() via svc.rpc (idempotent — only acts on past-deadline rows). This Edge fn is the SINGLE caller of expire_stale_filming (BLOCKER 3 — the redundant SQL `lmc-expire-filming` cron is intentionally NOT scheduled in Task 2; scheduling both would double-run the sweep. expire_stale_dispatching keeps its own SQL cron because its hold-release goes through the existing no_scout path, not this sweeper).
(2) Hold-release query — NO time window (BLOCKER 3): select ALL checks WHERE checks.status='no_scout' AND the joined payments.status='authorized'. For each, `stripe.paymentIntents.cancel(pi)` then svc update payments.status='canceled', then log_event 'payment.hold_released' ({ check_id, reason: 'sla' }). The `payments.status='authorized'` filter + the per-row guard IS the idempotency: once cancelled, payments.status becomes 'canceled' and the row never re-matches, so re-running every minute is safe and any orphaned authorized hold (from a missed earlier run) is eventually retried. Do NOT bound the query by a recent-time window — that would orphan holds whose check expired before a sweeper run.
Guard each cancel: only when payment.status === 'authorized' (double-refund / premature-capture safety).
Deploy note: invoked by pg_cron+pg_net with the service-role key; deploy with --no-verify-jwt (reachable only with the service-role bearer; only acts on authorized holds). Add import.meta.main Deno.serve wrapper.
Keep it tiny and idempotent; safe to run every minute.
  </action>
  <acceptance_criteria>
    sla-sweeper exists; queries ALL no_scout checks with authorized payments (NO recent-time window); only cancels PIs for authorized payments; logs hold_released; is the single caller of expire_stale_filming. grep paymentIntents.cancel + 'authorized' guard present.
  </acceptance_criteria>
  <verify>
    <automated>test -f supabase/functions/sla-sweeper/index.ts && grep -q "paymentIntents.cancel" supabase/functions/sla-sweeper/index.ts && grep -q "authorized" supabase/functions/sla-sweeper/index.ts</automated>
  </verify>
  <done>The hold-release gap is closed: SLA-missed authorized holds are explicitly cancelled, idempotently, on the minute sweep.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] live deploy — push 0015 + 0016, enable pg_cron+pg_net, schedule sweeps, deploy functions, regen types, verify</name>
  <files>lmc-app/app/lib/database.types.ts</files>
  <read_first>supabase/migrations/0015_sla_deadline.sql, supabase/migrations/0016_scout_earnings.sql, STATE.md (--no-verify-jwt lesson)</read_first>
  <action>
Orchestrator runs (exact sequence):
1. `supabase db push` — applies 0015_sla_deadline.sql AND 0016_scout_earnings.sql (both hard deliverables; 0016 is unconditional, BLOCKER 2).
2. Run the two pgTAP suites live: `supabase/tests/0015_sla_deadline.test.sql` + `supabase/tests/0015_expire_stale_filming.test.sql` (via the project's pgTAP runner / psql) — both must pass GREEN.
3. Enable extensions via management API / SQL: `CREATE EXTENSION IF NOT EXISTS pg_cron;` and `CREATE EXTENSION IF NOT EXISTS pg_net;`
4. Schedule the sweeps (idempotent — unschedule-then-schedule, guarded):
```sql
-- dispatch sweep stays a pure SQL cron (its no_scout path already releases the hold elsewhere):
select cron.unschedule('lmc-expire-dispatching') where exists (select 1 from cron.job where jobname='lmc-expire-dispatching');
select cron.schedule('lmc-expire-dispatching','* * * * *','select public.expire_stale_dispatching()');
-- BLOCKER 3: do NOT schedule a separate SQL `lmc-expire-filming` cron — the sla-sweeper Edge fn
-- below is the SINGLE caller of expire_stale_filming (it must follow the sweep with PI cancel,
-- which SQL cannot do). Scheduling both would double-run the filming sweep. If a stale
-- `lmc-expire-filming` job exists from a prior attempt, unschedule it:
select cron.unschedule('lmc-expire-filming') where exists (select 1 from cron.job where jobname='lmc-expire-filming');
-- sla-sweeper: runs expire_stale_filming THEN releases authorized holds, via pg_net (service role key):
select cron.schedule('lmc-sla-sweeper','* * * * *', $$ select net.http_post(
  url:='https://cawqasszfbzvbtunamda.supabase.co/functions/v1/sla-sweeper',
  headers:='{"Authorization":"Bearer <SERVICE_ROLE_KEY>","Content-Type":"application/json"}'::jsonb,
  body:='{}'::jsonb) $$);
-- job_run_details hygiene (Pitfall 6):
select cron.schedule('lmc-cron-cleanup','17 3 * * *','delete from cron.job_run_details where end_time < now() - interval ''7 days''');
```
5. Deploy Edge Functions:
   - `supabase functions deploy trouble-report` (verify_jwt=true)
   - `supabase functions deploy scout-earnings` (verify_jwt=true)
   - `supabase functions deploy stripe-connect-payout` (verify_jwt=true)
   - `supabase functions deploy sla-sweeper --no-verify-jwt` (service-to-service)
   - If any webhook was touched this phase, redeploy it with `--no-verify-jwt` (LESSON). They were NOT touched here, so leave mux-webhook/stripe-webhook as-is.
6. Regenerate types: `supabase gen types typescript ... > lmc-app/app/lib/database.types.ts` so deadline_at/accepted_at are typed; remove any as-any casts added in Plan 03 if now unnecessary.
7. Verify: `select jobname, schedule, active from cron.job` → confirm rows for lmc-expire-dispatching AND lmc-sla-sweeper (and NO lmc-expire-filming — the sweeper owns the filming sweep). Confirm the three user functions return 401 to a forged/anon request (verify_jwt working).
8. `cd lmc-app && npx tsc --noEmit` clean after type regen.
  </action>
  <acceptance_criteria>
    0015 + 0016 live (unconditional); the pgTAP suites (0015 deadline + filming + edges, 0016 earnings) GREEN live; cron.job has lmc-expire-dispatching + lmc-sla-sweeper (NOT lmc-expire-filming); 4 functions deployed (3 verify_jwt=true, sla-sweeper --no-verify-jwt); types regenerated; tsc clean.
  </acceptance_criteria>
  <verify>
    <automated>cd lmc-app && npx tsc --noEmit && grep -q "deadline_at" app/lib/database.types.ts</automated>
  </verify>
  <done>The full SLA + money chain is live: deadlines set, sweeps scheduled, holds released, functions deployed, types current.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [DEVICE] on-device SLA + money walk-through</name>
  <files>(none — device verification only)</files>
  <what-built>Server-driven deadlines + Trouble-Here refund + real Scout earnings/withdraw, live behind pg_cron.</what-built>
  <action>Human-verify checkpoint. The orchestrator pauses execution and presents the how-to-verify steps to Troy on a real EAS dev build. No code is written in this task. Also confirm the three flagged SLA defaults at this review: clock-start at Scout-accept, auto-refund on a missed deadline, and the $3.00 Scout no-fault amount.</action>
  <how-to-verify>
On an EAS dev build, two sessions (Seeker + Scout), real GPS:
1. Seeker requests a check + pays the hold. Scout accepts. Open filming.tsx → the countdown shows the REAL remaining time (priority ~7 min / standard ~10 min). Background the app, reopen → the countdown RESUMES from server time, it does NOT reset to a fresh 7/10 min.
2. Tap a Trouble-Here reason → confirm it shows REPORTED only after a brief network call (not instantly). Check: Seeker's hold is released (they are NOT charged), the job is freed, and the Scout receives the flat no-fault pay (verify in Stripe test dashboard a Transfer to the Scout account + a canceled PaymentIntent).
3. Let a different accepted check sit past its deadline without delivering → within ~1 minute the check flips to no_scout and the Seeker's hold is released (cron + sla-sweeper).
4. Open the Scout earnings screen → real totals, weekly bars, and payout history (no 220 / fake bars). Tap withdraw → a real payout initiates (verify in Stripe test dashboard); instant payout amount reflects the after-fee net.
Reply "approved" when all four pass, or describe what failed.
  </how-to-verify>
  <verify>Human confirms all four device behaviours pass; Troy confirms the three SLA defaults.</verify>
  <done>Troy replies "approved" after the four device checks pass and the three SLA defaults are confirmed.</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| pg_cron/pg_net → sla-sweeper | service-role key in Authorization header; only reachable path |
| anon/forged → user Edge fns | rejected by verify_jwt=true |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-16 | Tampering (double-refund) | sla-sweeper cancelling an already-captured/canceled PI | mitigate | only cancels when payment.status === 'authorized'; idempotent re-runs |
| T-07-17 | Spoofing | anon calling user money functions | mitigate | trouble-report/scout-earnings/stripe-connect-payout deployed verify_jwt=true; verified to 401 forged requests |
| T-07-18 | DoS | sweeper firing during an active Mux upload | accept | sweep only targets assigned/filming past deadline_at (7-10 min); Mux upload+processing < 2 min and webhook advances status before the window; deadline_at window far exceeds upload time |
| T-07-19 | Repudiation | cron job runs untracked | mitigate | cron.job_run_details retained 7 days (cleanup job); every hold release logged payment.hold_released |
</threat_model>

<verification>
- Live pgTAP GREEN, cron.job rows present, functions 401 forged requests, tsc clean.
- Device walk-through (Task 3) signed off by Troy.
</verification>

<success_criteria>
SLA + money integrity is LIVE and proven on device: real resumable deadlines, real auto-expiry + hold release, real Trouble-Here refund + Scout no-fault pay, real earnings + withdraw. All three SLA defaults (clock-start at accept, auto-refund on miss, $3 no-fault) confirmed by Troy at this review.
</success_criteria>

<output>
After completion, create `.planning/phases/07-sla-money-integrity-real-server-driven-delivery-deadlines-de/07-04-SUMMARY.md`
</output>
