---
phase: 07-sla-money-integrity-real-server-driven-delivery-deadlines-de
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - lmc-app/app/lib/payments.ts
  - lmc-app/app/(scout)/filming.tsx
  - lmc-app/app/(seeker)/waiting.tsx
  - lmc-app/app/(scout)/earnings.tsx
  - lmc-app/app/(scout)/withdraw.tsx
autonomous: true
requirements: [D-01, D-04, D-06]
must_haves:
  truths:
    - "The filming countdown is seeded from check.deadline_at, so it shows the real remaining time and survives an app reopen (no longer a fresh 420/600 constant)"
    - "Tapping a Trouble-Here reason calls reportTrouble() and only shows REPORTED after the server confirms (no false 'SEEKER REFUNDED' before the call)"
    - "The Scout earnings screen shows real DB totals, weekly bars, available balance, and payout history from scout-earnings (no BAR_DATA/PAYOUTS/220 constants)"
    - "Withdraw calls stripe-connect-payout with the real available balance and speed (no setTimeout fake)"
  artifacts:
    - path: "lmc-app/app/lib/payments.ts"
      provides: "reportTrouble(), getScoutEarnings(), requestPayout() typed client helpers"
      exports: ["reportTrouble", "getScoutEarnings", "requestPayout"]
    - path: "lmc-app/app/(scout)/filming.tsx"
      provides: "deadline_at-seeded countdown + wired Trouble-Here"
      contains: "deadline_at"
    - path: "lmc-app/app/(scout)/earnings.tsx"
      provides: "real earnings render from getScoutEarnings"
      contains: "getScoutEarnings"
  key_links:
    - from: "filming.tsx countdown"
      to: "check.deadline_at"
      via: "secondsLeft seeded from (deadline_at - now)/1000 after getCheck resolves"
      pattern: "deadline_at"
    - from: "filming.tsx Trouble-Here"
      to: "reportTrouble() -> trouble-report Edge fn"
      via: "invokeEdgeFunction, awaited before UI confirm"
      pattern: "reportTrouble"
    - from: "withdraw.tsx"
      to: "requestPayout() -> stripe-connect-payout Edge fn"
      via: "real payout call replacing setTimeout"
      pattern: "requestPayout"
---

<objective>
Wire the client to the real server. Four cosmetic fakes become real: (1) the filming countdown reads the server `deadline_at` so it shows true remaining time and survives app reopen (D-01); (2) Trouble-Here actually calls the `trouble-report` Edge fn and only shows the refunded/no-fault-paid state after the server confirms (D-04 — today it lies, showing "SEEKER REFUNDED" with zero backend call); (3) the earnings screen renders real DB totals + Stripe balance + payout history (D-06 — today BAR_DATA/PAYOUTS/`220` are hardcoded); (4) withdraw fires a real payout (D-06 — today it's a `setTimeout`).

Depends on Plan 01 (deadline_at column) and Plan 02 (the three Edge Functions exist as typed contracts). Interface-first: add the typed client helpers in payments.ts first, then wire screens against them. No file overlap with Plans 01/02.

Purpose: the user-facing surface tells the truth about time and money.
Output: payments.ts helpers + 4 wired screens, `tsc --noEmit` clean. Device-verified in Plan 04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-sla-money-integrity-real-server-driven-delivery-deadlines-de/07-CONTEXT.md
@.planning/phases/07-sla-money-integrity-real-server-driven-delivery-deadlines-de/07-RESEARCH.md
@.planning/SCOUT-AUDIT.md

@lmc-app/app/lib/payments.ts
@lmc-app/app/(scout)/filming.tsx
@lmc-app/app/(scout)/earnings.tsx
@lmc-app/app/(scout)/withdraw.tsx
@lmc-app/app/(seeker)/waiting.tsx

<interfaces>
<!-- Existing client shapes. Use them; do not re-explore. -->

payments.ts (existing): `async function invokeEdgeFunction(functionName, payload)` — plain fetch + 30s AbortController (Hermes-safe). Existing exports follow `export async function requestRefund(checkId, reasonCode, note)` etc. Add new helpers in the SAME style.

filming.tsx (existing, the sites to change):
- line 21: `import { markFilming, getCheck } from '../lib/checks';`
- line 63-64: `const isPriority = tier === 'priority'; const totalSeconds = isPriority ? 420 : 600;`
- line 97: `getCheck(checkId).then(...)` already runs on mount — the resolved check row has `deadline_at` after Plan 01.
- line 129: `const [secondsLeft, setSecondsLeft] = useState(totalSeconds);`
- line 133: `const [troubleReason, setTroubleReason] = useState<string | null>(null);`
- line 616: `onPress={() => { setTroubleReason(r); ... }}` — currently sets local state only and renders "REPORTED · SEEKER REFUNDED" (line 579) with NO API call. This is the lie to fix.

earnings.tsx (existing): line 12 `import { useScoutEarnings }`; line 14 `const BAR_DATA = [...]`; line 34 `const PAYOUTS = [...]`; line 45 `const monthTotal = 220 + earnings.earningsToday`; line 216/63 available balance = `earnings.earningsToday`; line 227 withdraw → `Alert.alert` placeholder.

withdraw.tsx (existing): line 5 `const AVAILABLE = 137.0;`; line 22 `setTimeout(...)` fake processing; routes back to /(scout)/earnings.

Edge fn contracts (from Plan 02) to call:
- trouble-report → { status: 'reported' }, body { checkId, reason }
- scout-earnings → { weeklyByDay, allTimeCents, availableCents, instantNetCents, payoutSpeed, payouts }, body {} (scout = caller)
- stripe-connect-payout → { status: 'initiated', payoutId }, body { amountCents, speed? }

New Arch stays OFF (Stripe RN constraint, prior decision) — no New-Arch-only APIs.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: payments.ts client helpers (interface-first) + filming.tsx deadline countdown + wired Trouble-Here</name>
  <files>lmc-app/app/lib/payments.ts, lmc-app/app/(scout)/filming.tsx</files>
  <read_first>lmc-app/app/lib/payments.ts (invokeEdgeFunction + existing export style), lmc-app/app/(scout)/filming.tsx (lines 60-135 + 570-625), 07-RESEARCH.md code example 2</read_first>
  <action>
payments.ts — add three typed exports mirroring requestRefund's style:
```ts
export type TroubleReason = string; // matches filming.tsx TROUBLE_REASONS
export async function reportTrouble(checkId: string, reason: TroubleReason): Promise<{ status: 'reported' }> {
  const data = await invokeEdgeFunction('trouble-report', { checkId, reason }) as Record<string, unknown>;
  if (data.status !== 'reported') throw new Error('reportTrouble: unexpected response');
  return { status: 'reported' };
}
export interface ScoutEarnings { weeklyByDay: {day:string;cents:number}[]; allTimeCents:number; availableCents:number; instantNetCents:number; payoutSpeed:'instant'|'standard'; payouts:{id:string;amountCents:number;status:string;arrivalDate:string;method:string}[]; }
export async function getScoutEarnings(): Promise<ScoutEarnings> { return await invokeEdgeFunction('scout-earnings', {}) as ScoutEarnings; }
export async function requestPayout(amountCents: number, speed?: 'instant'|'standard'): Promise<{ status:'initiated'; payoutId:string }> {
  return await invokeEdgeFunction('stripe-connect-payout', { amountCents, speed }) as { status:'initiated'; payoutId:string };
}
```
filming.tsx — two changes:
1. Deadline-seeded countdown (D-01): in the existing getCheck(checkId).then(...) on mount, read `c.deadline_at`; compute `const initialSecs = c?.deadline_at ? Math.max(0, Math.round((new Date(c.deadline_at).getTime() - Date.now())/1000)) : totalSeconds; setSecondsLeft(initialSecs);`. Keep `totalSeconds` as the fallback only. The setInterval at line 158 already decrements — leave it. Re-seed on AppState foreground if filming.tsx subscribes (optional; at minimum seed on mount).
2. Wire Trouble-Here (D-04): change the reason onPress (line 616) to call the server BEFORE confirming. Add a `troubleBusy` state. On tap: setTroubleBusy(true); try `await reportTrouble(checkId, r)`; on success setTroubleReason(r) (now truthful — server cancelled + refunded + paid no-fault) and after a short delay route the Scout off the job (e.g. router.replace('/(scout)/dashboard')); on error show an Alert and DO NOT show the REPORTED state. Update the success copy at line 579-581 to reflect real behaviour ("REPORTED · SEEKER REFUNDED · You're covered") only after the await resolves. Respect the no-em-dash copy rule (use commas / clean grammar).
Keep filming.tsx < 500 lines (it is 675 — do NOT grow it materially; these are small edits, extract a helper only if needed to stay maintainable, but do not refactor broadly).
  </action>
  <acceptance_criteria>
    payments.ts exports reportTrouble/getScoutEarnings/requestPayout. filming.tsx seeds secondsLeft from deadline_at and awaits reportTrouble before showing REPORTED. tsc clean.
  </acceptance_criteria>
  <verify>
    <automated>cd lmc-app && npx tsc --noEmit && grep -q "reportTrouble" app/lib/payments.ts && grep -q "deadline_at" "app/(scout)/filming.tsx" && grep -q "reportTrouble" "app/(scout)/filming.tsx"</automated>
  </verify>
  <done>Filming countdown reads the real deadline; Trouble-Here makes a real server call and only confirms on success. Helpers typed and exported.</done>
</task>

<task type="auto">
  <name>Task 2: earnings.tsx + withdraw.tsx real data + waiting.tsx deadline-aware copy</name>
  <files>lmc-app/app/(scout)/earnings.tsx, lmc-app/app/(scout)/withdraw.tsx, lmc-app/app/(seeker)/waiting.tsx</files>
  <read_first>lmc-app/app/(scout)/earnings.tsx (BAR_DATA/PAYOUTS/monthTotal/withdraw sites), lmc-app/app/(scout)/withdraw.tsx (AVAILABLE + setTimeout), lmc-app/app/(seeker)/waiting.tsx (status-driven steps), SCOUT-AUDIT.md earnings/withdraw sections</read_first>
  <action>
earnings.tsx (D-06): on mount, `const [data, setData] = useState<ScoutEarnings|null>(null)`; useEffect → `getScoutEarnings().then(setData)` with a loading + error state. Replace:
- `monthTotal` → `(data?.allTimeCents ?? 0)/100`.
- BAR_DATA → `data?.weeklyByDay` (map cents→bar height; keep the existing bar component, feed real values; handle empty week).
- PAYOUTS → `data?.payouts` (map id/amountCents/status/arrivalDate; keep the existing row component).
- AVAILABLE balance → `(data?.availableCents ?? 0)/100`.
- The WITHDRAW button → router.push to /(scout)/withdraw passing the real available balance + payoutSpeed as params (remove the Alert.alert placeholder).
withdraw.tsx (D-06): replace `const AVAILABLE = 137.0` with the available balance from route params (fallback 0). Replace the QUICK_AMOUNTS "All ($137)" with the dynamic available. Replace the `setTimeout` fake (line 22) with: `await requestPayout(Math.round(numAmount*100), speed)` (speed from params); show success only after it resolves; show an Alert on error. Keep the bank-destination display as-is for now (cosmetic; real bank detail is a later phase) but do not claim a fake processing success.
waiting.tsx (D-01 minor): the Seeker waiting screen is status-driven (correct). Do NOT add a countdown to the Seeker (07-RESEARCH Open Q1 / Pitfall 1 — the Seeker sees status, not the internal accept-start deadline). Only adjust copy if it currently implies a fixed timer; otherwise leave waiting.tsx behaviour unchanged. If no copy change is needed, this file may end up untouched — that is acceptable (it is listed for the deadline-awareness review, not a forced edit).
tsc clean throughout.
  </action>
  <acceptance_criteria>
    earnings.tsx renders from getScoutEarnings (no 220/BAR_DATA/PAYOUTS constants driving displayed numbers); withdraw.tsx calls requestPayout (no setTimeout fake success); waiting.tsx shows no fixed Seeker countdown. tsc clean.
  </acceptance_criteria>
  <verify>
    <automated>cd lmc-app && npx tsc --noEmit && grep -q "getScoutEarnings" "app/(scout)/earnings.tsx" && grep -q "requestPayout" "app/(scout)/withdraw.tsx" && ! grep -q "const AVAILABLE = 137" "app/(scout)/withdraw.tsx"</automated>
  </verify>
  <done>Earnings + withdraw screens show and move real money; the Seeker waiting screen stays honestly status-driven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client UI → Edge fn | client passes checkId/amount; server re-derives identity + money from bearer + DB |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-12 | Spoofing/Repudiation | client showing 'refunded' before server confirms | mitigate | filming.tsx awaits reportTrouble() and only renders REPORTED on resolved success; error path shows Alert, no false claim |
| T-07-13 | Tampering | client sending an inflated withdraw amount | mitigate | amount is a request; stripe-connect-payout server bounds it to instant net / available (Plan 02 T-07-09) — client cannot overdraw |
| T-07-14 | Information Disclosure (IDOR) | earnings/withdraw for another scout | mitigate | helpers send no scoutId; server uses callerId only (Plan 02 T-07-10) |
| T-07-15 | Tampering | client faking a later deadline to keep filming | accept | deadline_at is display-only on the client; enforcement is server-side via expire_stale_filming (Plan 01) — a client-faked clock has no server effect |
</threat_model>

<verification>
- `cd lmc-app && npx tsc --noEmit` clean.
- grep acceptance on all five files passes.
- Device behaviour (real countdown from deadline, real Trouble-Here refund, real earnings/withdraw) is the human-verify gate in Plan 04.
</verification>

<success_criteria>
Client reads the real deadline, fires real Trouble-Here/earnings/withdraw calls, and never shows fake money/time states. tsc clean.
</success_criteria>

<output>
After completion, create `.planning/phases/07-sla-money-integrity-real-server-driven-delivery-deadlines-de/07-03-SUMMARY.md`
</output>
