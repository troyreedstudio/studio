---
phase: 10-push-notifications
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - supabase/functions/send-push/index.ts
  - supabase/functions/send-push/index.test.ts
autonomous: true
requirements: [PUSH-04, PUSH-05, PUSH-06, PUSH-07, PUSH-08, PUSH-09]
user_setup: []

must_haves:
  truths:
    - "send-push resolves the seeker for a video-ready event and posts to the Expo Push API"
    - "send-push resolves in-range ONLINE scouts for a job-nearby event (reuses the geofence)"
    - "send-push skips a recipient whose notification_prefs disables the matching category"
    - "send-push degrades gracefully (pushes all) when notification_prefs is null"
    - "send-push deletes a stale token when Expo returns DeviceNotRegistered"
    - "send-push never throws — all paths return 200"
  artifacts:
    - path: "supabase/functions/send-push/index.ts"
      provides: "handleSendPush() + import.meta.main entrypoint"
      exports: ["handleSendPush"]
    - path: "supabase/functions/send-push/index.test.ts"
      provides: "Deno tests for PUSH-04..PUSH-09 with mocked svc + fetch"
      contains: "handleSendPush"
  key_links:
    - from: "supabase/functions/send-push/index.ts"
      to: "https://exp.host/--/api/v2/push/send"
      via: "fetch POST of batched messages"
      pattern: "exp.host/--/api/v2/push/send"
    - from: "send-push (job-nearby)"
      to: "in-range online scouts"
      via: "scout_locations is_online + ST_DWithin against the check coord"
      pattern: "is_online|ST_DWithin|scouts_in_range"
---

<objective>
Build the server-owned `send-push` Edge Function: it receives `{ checkId, event }`, resolves the correct recipients (seeker for video-ready, in-range online scouts for job-nearby), respects each recipient's notification_prefs, batches their tokens to the Expo Push API, cleans up DeviceNotRegistered tokens, and never throws.

Purpose: This is the single send path both the dispatching trigger (Plan 01) and the mux-webhook delivery hook (Plan 03) call fire-and-forget. It is the IDOR boundary — recipients are derived server-side from checkId, never from client input.
Output: send-push/index.ts + its green Deno test suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-push-notifications-expo-push-device-tokens-table-notify-scou/10-RESEARCH.md

# The fire-and-forget verdict-engine pattern to mirror (never throws, import.meta.main guard)
@supabase/functions/fraud-eval/index.ts
@supabase/functions/fraud-eval/index.test.ts
# Service-role client factory
@supabase/functions/_shared/supabase.ts
# The geofence query the job-nearby audience reverses
@supabase/migrations/20260621000002_dispatch_rpc_accept.sql
@supabase/migrations/0012_dispatch_verification_spine.sql

<interfaces>
<!-- Existing contracts send-push consumes. Use directly. -->

serviceClient(): SupabaseClient  // from _shared/supabase.ts — bypasses RLS, service role

checks: id uuid, seeker_id uuid, scout_id uuid, status check_status, coord geography(point,4326)
scout_locations: scout_id uuid, coord geography(point,4326), is_online boolean
profiles: id uuid, notification_prefs jsonb  // Phase 9 column (migration 0017). MAY be null/absent if Phase 9 not merged — degrade to push-all (D-04)
device_push_tokens (Plan 01): user_id uuid, token text, platform text

log_event RPC (positional): svc.rpc('log_event', { p_event_type, p_subject_type, p_subject_id, p_context })

Expo Push API: POST https://exp.host/--/api/v2/push/send
  body: array of up to 100 { to: ExponentPushToken, title, body, data? }
  response: { data: ticket[] } where ticket.status==='error' && ticket.details?.error==='DeviceNotRegistered' means delete that token
  No access token needed for basic sends.

notification_prefs key map: video-ready -> 'delivered'; job-nearby -> 'job-nearby'. Default ON when key/column null.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the Deno test suite (RED) pinning the send-push contracts</name>
  <files>supabase/functions/send-push/index.test.ts</files>
  <read_first>supabase/functions/fraud-eval/index.test.ts (mockSvc + assert pattern, std@0.224.0 imports)</read_first>
  <action>
Write `supabase/functions/send-push/index.test.ts` importing `handleSendPush` from `./index.ts` (RED — index.ts created in Task 2). Mirror fraud-eval's mock-svc style (a chainable `from()` builder recording calls, and a `rpc()` recorder). Also stub global `fetch` per-test to capture the Expo Push request and return a controllable ticket response. Pin:
- PUSH-04: event='video-ready' resolves checks.seeker_id, reads that user's device_push_tokens, and POSTs to `https://exp.host/--/api/v2/push/send` with the seeker's token(s). Assert the fetch URL and that `messages[].to` are the seeker's tokens.
- PUSH-05: event='job-nearby' resolves the in-range ONLINE scout set (mock the scouts-in-range query / RPC to return two scout_ids), looks up their tokens, and POSTs them. Assert the scout tokens are the recipients (NOT the seeker).
- PUSH-06: a recipient whose `notification_prefs.delivered === false` (video-ready) is SKIPPED — no message built for that user.
- PUSH-07: `notification_prefs` null -> recipient IS included (push-all degrade, D-04).
- PUSH-08: an Expo ticket with `status:'error', details.error:'DeviceNotRegistered'` triggers a delete on device_push_tokens for that token (assert the delete/eq call recorded).
- PUSH-09: when the mock svc THROWS on a query, handleSendPush still resolves (returns, no throw) — wrap the call in a try and assert no exception propagated.
Use `deno test --allow-env`. Keep under 500 lines.
  </action>
  <acceptance_criteria>
    - File exists; `grep -q "handleSendPush" supabase/functions/send-push/index.test.ts`
    - `grep -q "exp.host/--/api/v2/push/send" supabase/functions/send-push/index.test.ts`
    - `grep -q "DeviceNotRegistered" supabase/functions/send-push/index.test.ts`
  </acceptance_criteria>
  <verify>
    <automated>grep -q "handleSendPush" supabase/functions/send-push/index.test.ts && grep -q "exp.host/--/api/v2/push/send" supabase/functions/send-push/index.test.ts && grep -q "DeviceNotRegistered" supabase/functions/send-push/index.test.ts && echo TEST-SCAFFOLD-OK</automated>
  </verify>
  <done>Deno test file pins PUSH-04..PUSH-09. RED until Task 2 implements index.ts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement send-push Edge Function (recipient resolution + Expo batch + cleanup, never throws)</name>
  <files>supabase/functions/send-push/index.ts</files>
  <behavior>
    - handleSendPush(checkId, event, svc): for 'video-ready' -> [seeker]; for 'job-nearby' -> in-range online scouts; filter by notification_prefs; batch tokens (<=100) to Expo; delete DeviceNotRegistered tokens; log event; never throw
    - The entrypoint validates checkId as a UUID and event as the 'video-ready'|'job-nearby' enum before doing work; returns 200 even on internal failure
  </behavior>
  <read_first>supabase/functions/fraud-eval/index.ts (never-throws structure, log_event usage, import.meta.main guard, `as any` Svc type)</read_first>
  <action>
Create `supabase/functions/send-push/index.ts`:

1. `type Svc = any;` and an exported `handleSendPush(checkId: string, event: 'video-ready'|'job-nearby', svc: Svc): Promise<void>`. Wrap the WHOLE body in try/catch that swallows and logs — NEVER throws (mirror fraud-eval).

2. Resolve recipients (server-side, IDOR-safe — recipients come from checkId, never client input):
   - video-ready: `select seeker_id from checks where id = checkId` -> recipient = [seeker_id].
   - job-nearby: get the in-range ONLINE scouts for this check. Read the check's coord (`select coord, ... from checks where id=checkId`). Reuse the geofence by calling a scouts-in-range query: select scout_id from scout_locations where is_online = true AND ST_DWithin(coord, <check coord>, dispatch_radius_m). Implement this as a small inline query using the service client. IMPORTANT: `list_open_checks_for_scout` is scout-centric (wrong direction). Instead query scout_locations directly. If a server-side helper RPC `scouts_in_range_of_check(checkId)` does not exist, query `scout_locations` filtered on `is_online=true` and ST_DWithin against the check's coord and market_config.dispatch_radius_m via a `.rpc()` you add OR an inline PostgREST filter. Prefer adding a tiny SECURITY DEFINER RPC IF NEEDED — but if the inline service-role select with a raw filter is simpler, use it. Document the chosen approach in a header comment. (Audience MUST be online scouts only — Pitfall 4: the token table never dictates audience.)

3. For each recipient: read `profiles.notification_prefs` (jsonb). Map event->key (video-ready->'delivered', job-nearby->'job-nearby'). Skip the recipient ONLY if `prefs[key] === false`. Null prefs/column or missing key => include (D-04 push-all default).

4. Look up `device_push_tokens` (token, platform) for each included recipient. Build Expo messages `{ to: token, title, body, data: { checkId, event } }`. Copy (use the word "video", never "clip"):
   - job-nearby: title "New check nearby", body "A new check just dropped near you — tap to claim."
   - video-ready: title "Your check is ready", body "Your video is ready to watch."

5. Chunk messages into batches of <=100 and POST each to `https://exp.host/--/api/v2/push/send` (Content-Type + Accept application/json). Add a local `chunk<T>(arr,size)` helper (no external dep).

6. Parse the ticket array; for any ticket with `status==='error'` and `details?.error==='DeviceNotRegistered'`, delete that index's token from device_push_tokens (`.from('device_push_tokens').delete().eq('token', token)`).

7. Log `push.sent` (with counts) / `push.skipped_prefs` via `svc.rpc('log_event', {...})`. Never let logging throw.

8. `import.meta.main` entrypoint: parse `{ checkId, event }` from req.json(); validate checkId is a UUID (regex) and event is one of the two literals — return 400 on bad input; otherwise call handleSendPush with `serviceClient()` and ALWAYS return `Response('ok',{status:200})` (push never signals failure upstream — fire-and-forget). Keep the file under 500 lines (split into a handler helper file if it exceeds; expected ~200).
  </action>
  <acceptance_criteria>
    - `deno test --allow-env supabase/functions/send-push/index.test.ts` passes (GREEN)
    - `grep -q "exp.host/--/api/v2/push/send" supabase/functions/send-push/index.ts`
    - `grep -qE "is_online|ST_DWithin|scouts_in_range" supabase/functions/send-push/index.ts` (online-scout audience)
    - `grep -q "DeviceNotRegistered" supabase/functions/send-push/index.ts`
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/troyreed/studio/projects/let-me-check && deno test --allow-env supabase/functions/send-push/index.test.ts && grep -q "exp.host/--/api/v2/push/send" supabase/functions/send-push/index.ts && grep -qE "is_online|ST_DWithin|scouts_in_range" supabase/functions/send-push/index.ts</automated>
  </verify>
  <done>send-push resolves correct recipients, respects prefs, batches to Expo, cleans DeviceNotRegistered, never throws; Deno suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| trigger/webhook -> send-push | Service-role invoke carrying checkId only |
| send-push -> Expo Push API | Outbound HTTPS with recipient tokens |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-05 | Tampering | wrong-recipient / IDOR (pushing user A's content to user B) | mitigate | Recipients derived server-side from checkId (seeker_id from checks; scout set from scout_locations) — never from caller input. job-nearby audience is online-scouts-only via is_online + ST_DWithin (PUSH-05) |
| T-10-06 | Information Disclosure | leaking a token belonging to a non-recipient | mitigate | Token lookup keyed strictly to the resolved recipient user_ids; service-role read scoped per recipient |
| T-10-07 | Denial of Service | stale DeviceNotRegistered tokens accumulating, slowing sends | mitigate | Inline cleanup deletes DeviceNotRegistered tokens on each send (PUSH-08) |
| T-10-08 | Denial of Service | send-push throwing and blocking the calling transition/webhook | mitigate | Whole body try/catch; entrypoint always returns 200 (PUSH-09) |
| T-10-09 | Spoofing | forged event causing an unintended push | mitigate | event validated against the 'video-ready'|'job-nearby' enum; checkId validated as UUID before any query |
</threat_model>

<verification>
- `cd /Users/troyreed/studio/projects/let-me-check && deno test --allow-env supabase/functions/send-push/index.test.ts` — PUSH-04..PUSH-09 green
- greps confirm Expo endpoint, online-scout audience, DeviceNotRegistered cleanup present
</verification>

<success_criteria>
- send-push sends to the right recipients (seeker for video-ready, in-range online scouts for job-nearby)
- Respects notification_prefs; degrades to push-all on null prefs
- Cleans DeviceNotRegistered tokens; never throws; entrypoint always 200
</success_criteria>

<output>
After completion, create `.planning/phases/10-push-notifications-expo-push-device-tokens-table-notify-scou/10-02-SUMMARY.md`. Note the deploy flag for Wave 4: `send-push` deploys with `--no-verify-jwt` (called server-to-server by the trigger + mux-webhook, no end-user JWT).
</output>
