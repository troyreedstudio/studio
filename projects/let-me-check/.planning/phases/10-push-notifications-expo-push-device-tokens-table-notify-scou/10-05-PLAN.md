---
phase: 10-push-notifications
plan: 05
type: execute
wave: 4
depends_on: [10-01, 10-02, 10-03, 10-04]
files_modified:
  - lmc-app/app/lib/database.types.ts
autonomous: false
requirements: [PUSH-01, PUSH-02, PUSH-03, PUSH-04, PUSH-05, PUSH-06, PUSH-07, PUSH-08, PUSH-09, PUSH-10, PUSH-11, PUSH-12, PUSH-13]
user_setup:
  - service: apple-apns
    why: "Live iOS push delivery on a physical device needs the APNs key in EAS credentials"
    dashboard_config:
      - task: "On the next `eas build -p ios`, answer YES to the APNs key prompt — EAS generates + registers the .p8. This is NOT required to deploy the server pipeline; it gates only on-device delivery."
        location: "EAS CLI build prompt"

must_haves:
  truths:
    - "Migration 0018 is live on the database (device_push_tokens + dispatching trigger exist)"
    - "The trigger's service-role auth header is resolvable (GUCs set, or the Dashboard webhook fallback is in place)"
    - "send-push is deployed with --no-verify-jwt"
    - "mux-webhook is redeployed and STILL has --no-verify-jwt"
    - "Live pgTAP + Deno + vitest suites are green; tsc is clean; database.types.ts includes device_push_tokens"
  artifacts:
    - path: "lmc-app/app/lib/database.types.ts"
      provides: "regenerated types including device_push_tokens"
      contains: "device_push_tokens"
  key_links:
    - from: "checks status->dispatching"
      to: "send-push job-nearby"
      via: "live pg trigger -> deployed send-push"
      pattern: "checks_push_on_dispatching"
---

<objective>
Ship Phase 10: push the migration, set the trigger's auth GUCs (or wire the Dashboard webhook fallback), deploy send-push, redeploy mux-webhook (keeping --no-verify-jwt), regenerate types, and confirm the full suite + trigger are live. This is the BLOCKING deploy plan — the orchestrator runs the privileged commands.

Purpose: Code from Waves 1-3 is inert until the migration is live, the function is deployed, and the trigger can authenticate. This plan also flags the one human EAS step (APNs key) for on-device delivery.
Output: live trigger + deployed send-push + redeployed mux-webhook + regenerated types + green suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-push-notifications-expo-push-device-tokens-table-notify-scou/10-RESEARCH.md
@.planning/phases/10-push-notifications-expo-push-device-tokens-table-notify-scou/10-01-PLAN.md
@.planning/phases/10-push-notifications-expo-push-device-tokens-table-notify-scou/10-02-PLAN.md

<interfaces>
<!-- Standing deploy lessons baked into this codebase. -->
- mux-webhook MUST deploy with --no-verify-jwt (Mux 401'd when it was dropped — standing lesson)
- send-push deploys with --no-verify-jwt (called server-to-server by the trigger + mux-webhook; no end-user JWT)
- Types regen after a migration; device_push_tokens uses `as any` casts until this runs
- Project ref: cawqasszfbzvbtunamda
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Push migration 0018 + set the trigger auth GUCs (orchestrator-run)</name>
  <files>supabase/migrations/0018_device_push_tokens.sql</files>
  <what-built>Migration 0018 (device_push_tokens + dispatching trigger) and the GUCs the trigger needs for its service-role auth header.</what-built>
  <action>
The orchestrator runs (from repo root):
1. `supabase db push` — applies migration 0018.
2. Set the trigger auth GUCs (Open-Q2 resolution). Either:
   (a) PREFERRED — set database settings so the trigger's net.http_post header resolves:
       `ALTER DATABASE postgres SET app.settings.supabase_url = 'https://cawqasszfbzvbtunamda.supabase.co';`
       `ALTER DATABASE postgres SET app.settings.service_role_key = '<SERVICE_ROLE_KEY>';`
       (Run via the Supabase SQL editor / a privileged psql session. The service-role key comes from the Supabase dashboard — NEVER commit it. After ALTER DATABASE, reconnect so the GUC takes effect.)
   OR
   (b) FALLBACK — create the trigger via Supabase Dashboard -> Database Webhooks pointing at /functions/v1/send-push for `checks` UPDATE; the Dashboard injects the service-role auth header automatically. If you choose (b), the SQL trigger's null-GUC guard makes it a safe no-op (so both can't double-fire), or drop the SQL trigger.
3. Verify the trigger + table exist live:
   `cd lmc-app && npm run test:db` (runs supabase/tests/0018_push_tokens.test.sql — PUSH-01/02/03/10 go GREEN).
  </action>
  <how-to-verify>
After `supabase db push` + GUCs (or Dashboard webhook), run `cd lmc-app && npm run test:db` — PUSH-01/02/03/10 (table, RLS, idempotent upsert, dispatching trigger) go GREEN against the live DB.
  </how-to-verify>
  <verify>
    <automated>cd lmc-app && npm run test:db</automated>
  </verify>
  <done>Migration 0018 applied live; device_push_tokens + checks_push_on_dispatching trigger exist; GUCs set or Dashboard webhook wired; live pgTAP green.</done>
  <resume-signal>Type "pushed" once `npm run test:db` is green (0018 tests pass live) and the GUCs are set (or the Dashboard webhook is wired).</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Deploy send-push (--no-verify-jwt) + redeploy mux-webhook (keep --no-verify-jwt)</name>
  <files>supabase/functions/send-push/index.ts, supabase/functions/mux-webhook/index.ts</files>
  <what-built>The send-push Edge Function and the updated mux-webhook (step 8c).</what-built>
  <action>
The orchestrator runs (from repo root):
1. `supabase functions deploy send-push --no-verify-jwt` — send-push is called server-to-server (trigger + mux-webhook), no end-user JWT.
2. `supabase functions deploy mux-webhook --no-verify-jwt` — CRITICAL: KEEP --no-verify-jwt. Dropping it previously 401'd Mux (standing lesson). Redeploy is required so step 8c (the video-ready push) goes live.
3. Confirm both deploys succeeded (CLI reports the function URLs / no error).
  </action>
  <how-to-verify>
Both `supabase functions deploy` commands exit 0 and the CLI reports the deployed function URLs. mux-webhook was deployed WITH --no-verify-jwt (Mux integration unbroken).
  </how-to-verify>
  <verify>
    <automated>echo "Deploy is human-run; confirmed by clean CLI exit and Task 3 live suite"</automated>
  </verify>
  <done>send-push deployed --no-verify-jwt; mux-webhook redeployed KEEPING --no-verify-jwt; both deploys clean.</done>
  <resume-signal>Type "deployed" once both functions deploy cleanly with --no-verify-jwt.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Regenerate database.types.ts + run the full suite + verify the trigger in pg</name>
  <files>lmc-app/app/lib/database.types.ts</files>
  <read_first>lmc-app/app/lib/database.types.ts (current shape — confirm regen replaces, not appends)</read_first>
  <action>
After Tasks 1-2 (migration live, functions deployed):
1. Regenerate types so device_push_tokens is typed (drops the `as any` debt):
   `supabase gen types typescript --project-id cawqasszfbzvbtunamda > lmc-app/app/lib/database.types.ts`
   (or the project's established gen-types command if it differs — match how prior phases regenerated). Confirm `grep -q device_push_tokens lmc-app/app/lib/database.types.ts`.
2. Run the full verification suite:
   - `cd lmc-app && npm run test:db` (pgTAP live — PUSH-01/02/03/10)
   - `cd /Users/troyreed/studio/projects/let-me-check && deno test --allow-env supabase/functions/send-push supabase/functions/mux-webhook` (PUSH-04..PUSH-09, PUSH-11)
   - `cd lmc-app && npx vitest run app/lib/push.test.ts` (PUSH-12/PUSH-13)
   - `cd lmc-app && npm run typecheck` (tsc clean — verifies regenerated types compile with push.ts/auth.ts)
3. Verify the trigger is registered live: the pgTAP run in step 2 already asserts has_trigger('public','checks','checks_push_on_dispatching') — that passing is sufficient proof the trigger is live.
  </action>
  <acceptance_criteria>
    - `grep -q "device_push_tokens" lmc-app/app/lib/database.types.ts`
    - `cd lmc-app && npm run test:db` green
    - `deno test --allow-env supabase/functions/send-push supabase/functions/mux-webhook` green
    - `cd lmc-app && npx vitest run app/lib/push.test.ts` green
    - `cd lmc-app && npm run typecheck` clean
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/troyreed/studio/projects/let-me-check && grep -q "device_push_tokens" lmc-app/app/lib/database.types.ts && deno test --allow-env supabase/functions/send-push supabase/functions/mux-webhook && cd lmc-app && npm run test:db && npx vitest run app/lib/push.test.ts && npm run typecheck</automated>
  </verify>
  <done>Types regenerated with device_push_tokens; full suite green (PUSH-01..PUSH-13); tsc clean; the dispatching trigger is confirmed live.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deploy surface | Privileged migration push + function deploy + GUC/secret handling |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-16 | Information Disclosure | service-role key committed via the GUC step | mitigate | The key is set with ALTER DATABASE in a privileged session and read from the dashboard — never written to a migration, .env, or commit |
| T-10-17 | Spoofing | mux-webhook losing --no-verify-jwt causing Mux 401 (delivery breaks) | mitigate | Task 2 explicitly redeploys mux-webhook KEEPING --no-verify-jwt (standing lesson called out in the resume-signal) |
| T-10-18 | Elevation of Privilege | send-push deployed with verify_jwt=true blocking the trigger's service-role call | mitigate | send-push deploys with --no-verify-jwt (server-to-server caller) |
</threat_model>

<verification>
- Live pgTAP: `cd lmc-app && npm run test:db` (PUSH-01/02/03/10)
- Live Deno: `deno test --allow-env supabase/functions/send-push supabase/functions/mux-webhook` (PUSH-04..PUSH-09, PUSH-11)
- vitest: `cd lmc-app && npx vitest run app/lib/push.test.ts` (PUSH-12/PUSH-13)
- tsc: `cd lmc-app && npm run typecheck`
- device_push_tokens present in regenerated database.types.ts
</verification>

<success_criteria>
- Migration 0018 live; trigger auth resolvable (GUC or Dashboard webhook)
- send-push deployed --no-verify-jwt; mux-webhook redeployed KEEPING --no-verify-jwt
- Types regenerated; full suite green; tsc clean
- APNs EAS-key step flagged for on-device delivery (does not block this deploy)
</success_criteria>

<output>
After completion, create `.planning/phases/10-push-notifications-expo-push-device-tokens-table-notify-scou/10-05-SUMMARY.md`. Record: the GUC-vs-Dashboard-webhook choice made, that mux-webhook kept --no-verify-jwt, and the outstanding human EAS APNs-key step for live on-device push.
</output>
