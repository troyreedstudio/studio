---
phase: 10-push-notifications
plan: 04
type: execute
wave: 3
depends_on: [10-02]
files_modified:
  - supabase/functions/mux-webhook/index.ts
autonomous: true
requirements: [PUSH-11]
user_setup: []

must_haves:
  truths:
    - "When a check reaches 'delivered', mux-webhook fires send-push {event:'video-ready'} to the seeker"
    - "A send-push failure NEVER blocks or undoes the delivered transition (fire-and-forget)"
    - "The existing mux-webhook tests still pass after the addition"
  artifacts:
    - path: "supabase/functions/mux-webhook/index.ts"
      provides: "step 8c — fire-and-forget seeker video-ready push after delivered"
      contains: "send-push"
  key_links:
    - from: "supabase/functions/mux-webhook/index.ts (after delivered)"
      to: "send-push (video-ready)"
      via: "deps.svc.functions.invoke fire-and-forget"
      pattern: "invoke\\('send-push'"
---

<objective>
Add the seeker delivery push: one fire-and-forget `send-push` invoke in mux-webhook on the `delivered` path, mirroring the existing fraud-eval / signage-check advisory steps.

Purpose: `delivered` has exactly ONE source (mux-webhook), so the inline pattern is safe and consistent (D-03, Research Pattern 4). This is the only file touched in its wave so the change can't collide with anything.
Output: mux-webhook step 8c.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-push-notifications-expo-push-device-tokens-table-notify-scou/10-RESEARCH.md

# The exact file + the fraud-eval/signage fire-and-forget pattern to mirror (steps 8b/9)
@supabase/functions/mux-webhook/index.ts

<interfaces>
<!-- The advisory pattern to copy. delivered transition is step 7; capture is step 8;
     fraud-eval is step 8b; signage is step 9. Insert the push as step 8c, AFTER the
     delivered transition (step 7) and AFTER capture (step 8), alongside the other
     advisory fire-and-forget invokes. -->

deps.svc.functions.invoke('send-push', { body: { checkId, event: 'video-ready' } })

Existing fraud-eval line (the pattern to mirror exactly):
  try { await deps.svc.functions.invoke('fraud-eval', { body: { checkId } }); } catch (_e) { /* advisory only — D-04 */ }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add step 8c — fire-and-forget video-ready push after delivered</name>
  <files>supabase/functions/mux-webhook/index.ts</files>
  <read_first>supabase/functions/mux-webhook/index.ts (steps 7, 8, 8b, 9 — lines ~176-206)</read_first>
  <action>
In `handleMuxWebhook`, AFTER the `delivered` transition (step 7) and the stripe-capture invoke (step 8) — alongside the fraud-eval (8b) and signage (9) advisory invokes — add a new advisory step "8c" that fires the seeker video-ready push. Mirror the fraud-eval line exactly (single-line try/catch that swallows ALL errors):
```ts
// 8c. Seeker delivery push (Phase 10, D-03/PUSH). Fire-and-forget AFTER delivered.
//     send-push resolves the seeker server-side from checkId (IDOR-safe) and respects
//     notification_prefs. A push failure NEVER undoes a completed delivery (mirrors 8b).
try { await deps.svc.functions.invoke('send-push', { body: { checkId, event: 'video-ready' } }); } catch (_e) { /* advisory only — D-03, push never blocks delivery */ }
```
Place it after the stripe-capture block (step 8) and ideally adjacent to the fraud-eval/signage advisory steps. Do NOT change the delivered transition, the GPS gate, the blur gate, or any other logic. Do NOT touch the dispatching push (that is the Plan-01 trigger, not this file). The word "video" (not "clip") is used in send-push copy, not here.
  </action>
  <acceptance_criteria>
    - `grep -q "invoke('send-push'" supabase/functions/mux-webhook/index.ts`
    - `grep -q "video-ready" supabase/functions/mux-webhook/index.ts`
    - The send-push invoke is wrapped in try/catch (advisory) — `grep -A1 "send-push" supabase/functions/mux-webhook/index.ts | grep -q "catch"` or on the same line
    - Existing mux-webhook tests still pass
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/troyreed/studio/projects/let-me-check && grep -q "invoke('send-push'" supabase/functions/mux-webhook/index.ts && grep -q "video-ready" supabase/functions/mux-webhook/index.ts && deno test --allow-env supabase/functions/mux-webhook/index.test.ts</automated>
  </verify>
  <done>mux-webhook fires the seeker video-ready push fire-and-forget on delivered; existing mux-webhook tests green (PUSH-11).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mux-webhook (service role) -> send-push | Service-role invoke carrying checkId only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-14 | Denial of Service | send-push throwing and undoing/blocking the delivered transition | mitigate | Single-line try/catch swallows ALL errors; runs AFTER the delivered transition + capture — the seeker already has the video before the push is attempted |
| T-10-15 | Information Disclosure | pushing the delivery to the wrong user | mitigate | send-push derives the seeker from checkId server-side (Plan 02 T-10-05); mux-webhook passes only checkId + the event literal |
</threat_model>

<verification>
- `cd /Users/troyreed/studio/projects/let-me-check && deno test --allow-env supabase/functions/mux-webhook/index.test.ts` — existing suite green (PUSH-11)
- greps confirm the advisory send-push invoke on the delivered path
</verification>

<success_criteria>
- mux-webhook fires send-push {video-ready} fire-and-forget after delivered
- No change to delivery, GPS, blur, or capture logic; existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/10-push-notifications-expo-push-device-tokens-table-notify-scou/10-04-SUMMARY.md`. Reminder for Wave 4: mux-webhook MUST be redeployed KEEPING `--no-verify-jwt` (standing lesson — dropping it 401'd Mux).
</output>
