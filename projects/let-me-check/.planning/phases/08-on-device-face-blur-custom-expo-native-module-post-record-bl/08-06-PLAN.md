---
phase: 08-on-device-face-blur-custom-expo-native-module-post-record-bl
plan: 06
type: execute
wave: 6
depends_on: ["08-05"]
files_modified:
  - lmc-app/app/(scout)/_filming-blur-overlay.tsx
  - lmc-app/babel.config.js
  - lmc-app/app.config.js
  - lmc-app/package.json
  - lmc-app/app/lib/blur-config.ts
autonomous: false
requirements: [BLUR-NATIVE-01, BLUR-03, BLUR-04, BLUR-05]
must_haves:
  truths:
    - "End-to-end: a Scout films a real clip with a face, and the DELIVERED clip the Seeker watches has the face blurred (Troy visual confirm)"
    - "No crash filming/securing/submitting repeatedly (native stability)"
    - "The dead worklets-core live-blur scaffold is removed so it can't reintroduce build-linking fragility"
    - "On the happy path the raw never leaves the device; the privacy promise holds end-to-end"
  artifacts:
    - path: "lmc-app/app/lib/blur-config.ts"
      provides: "BLUR_POST_RECORD_ENABLED flag, ready to flip true per-market after this confirm"
      contains: "BLUR_POST_RECORD_ENABLED"
  key_links:
    - from: "Scout films a face"
      to: "Seeker watches a blurred delivered clip"
      via: "full record→blur→upload→Mux→delivery chain"
      pattern: "delivery"
---

<objective>
STEP 6 of the 6-step plan (D-05): the FULL end-to-end proof on a real device, plus removal of the abandoned live-blur scaffold. A Scout films a clip with a face; the Seeker opens the delivered clip; Troy confirms the face is blurred. This is the phase's real acceptance gate.

This plan also DELETES the dormant worklets-core live-blur scaffold (06-RESEARCH-BLUR-V2 Pitfall 2 / "remove the live-blur scaffold once #1 is proven") — leaving it installed keeps build-linking fragility (undefined RNWorklet symbol, Hermes CMake) even unused. With the post-record module proven (Plans 01-05), the live path is dead weight and a stability risk.

Purpose: Prove the whole privacy promise works from camera to Seeker delivery, confirm native stability under repeated use, and remove the crash-class scaffold so the codebase is clean and the build can't regress to the worklets-core failure.
Output: An end-to-end-confirmed blur (Troy approves the delivered clip), the worklets-core/face-detector(v1)/Skia live-blur scaffold removed (package + babel plugin + overlay + config-plugin notes), and BLUR_POST_RECORD_ENABLED ready to flip true (per-market, alongside the dormant server net staying as the last-resort fallback).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-on-device-face-blur-custom-expo-native-module-post-record-bl/08-CONTEXT.md
@.planning/phases/08-on-device-face-blur-custom-expo-native-module-post-record-bl/08-VALIDATION.md
@.planning/phases/06-privacy-anti-fraud-hardening-on-device-face-plate-blur-befor/06-RESEARCH-BLUR-V2.md
@lmc-app/babel.config.js
@lmc-app/app.config.js
@lmc-app/app/lib/blur-config.ts

<interfaces>
<!-- The scaffold to REMOVE (06-RESEARCH-BLUR-V2 "remove the live-blur scaffold" + Pitfall 2). -->
Dead live-blur path (abandoned, D-02):
- app/(scout)/_filming-blur-overlay.tsx (Skia Canvas + face-detector frame processor — the crashing overlay)
- babel.config.js plugin ['react-native-worklets-core/plugin'] (only needed for the worklet overlay)
- package.json: react-native-worklets-core, react-native-vision-camera-face-detector(v1.10.2), @shopify/react-native-skia (added Phase 6 06-05 for the live path)
- app.config.js Phase-6 Category-B comment block describing the three live-path packages
- blur-config.ts BLUR_NATIVE_ENABLED (the live-viewfinder flag — distinct from BLUR_POST_RECORD_ENABLED)
- filming.tsx already references BLUR_NATIVE_ENABLED + <BlurViewfinder> conditionally (the false branch is the only live one)

KEEP (the dormant server net — last-resort fallback, D-04): supabase/functions/face-blur-check + the mux-webhook blur gate + market_config.blur_enabled. Do NOT delete these.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: [DEVICE BUILD] Step-6 gate — film a face, delivered clip is blurred (Troy confirm)</name>
  <action>Orchestrator gate: run the device build below and walk the verification steps. This is a verification checkpoint AFTER the autonomous tasks above completed their work; the build command and checks are in <how-to-verify>. On any failure, STOP and diagnose at this layer before the next wave.</action>
  <what-built>The complete post-record blur path (Plans 01-05) running end-to-end behind BLUR_POST_RECORD_ENABLED, flipped ON for this acceptance test. This is the real proof the whole phase exists for.</what-built>
  <how-to-verify>
    Orchestrator runs the device build with BLUR_POST_RECORD_ENABLED ON (and, if the server gate is involved, market_config.blur_enabled handled so it doesn't double-hold a correctly-blurred clip):

    1. `cd lmc-app && npx expo run:ios --configuration Release --device <udid>` — "Build Succeeded".
    2. Two sessions (or one device, both roles): a Scout accepts a real check and films a ~15s clip with Troy's face clearly in frame; sees "Securing your clip…" then upload; submit succeeds.
    3. The Seeker opens the DELIVERED clip on the delivery screen → Troy watches it → the FACE IS BLURRED across the whole delivered clip. THIS is the acceptance criterion.
    4. Stability: repeat accept→film→submit 3-5 times (with and without faces) → NO crash, no SIGSEGV, securing step stays a few seconds each time.
    5. Privacy: confirm (via logs/behaviour) the raw unblurred file was NOT uploaded on the happy path and was deleted after.

    PASS = build succeeds + Troy confirms the DELIVERED clip's face is blurred + no crash over repeats + raw never left on the happy path. FAIL → diagnose which layer (re-run the matching earlier step's gate). Do NOT mark the phase complete until Troy confirms the delivered clip is blurred.
  </how-to-verify>
  <resume-signal>Troy: type "approved" when the delivered clip you watch has the face clearly blurred and filming/submit is stable over several runs; or describe what's wrong.</resume-signal>
  <threat_model>
    - This is the end-to-end privacy proof — Troy's eyes on the DELIVERED clip (not just the local file). It catches anything that survived the isolated steps (e.g. Mux re-encode, delivery playback).
    - Native stability under repeated real use is confirmed here (the worklets-core path failed exactly under repeated mounts).
  </threat_model>
</task>

<task type="auto">
  <name>Task 2: remove the dead worklets-core live-blur scaffold</name>
  <read_first>
    - 06-RESEARCH-BLUR-V2.md "Also: remove the live-blur scaffold once #1 is proven" + Pitfall 2 (leaving worklets-core installed keeps build-linking fragility).
    - 08-CONTEXT D-02 (live path abandoned, do not revisit) + deferred list.
    - babel.config.js, app.config.js Phase-6 Category-B block, blur-config.ts (BLUR_NATIVE_ENABLED), filming.tsx BLUR_NATIVE_ENABLED branch + _filming-blur-overlay.tsx import.
    - lmc-app/CLAUDE.md (files <500 lines); do NOT touch the server-side face-blur-check / mux-webhook gate (kept as the dormant net).
  </read_first>
  <files>lmc-app/app/(scout)/_filming-blur-overlay.tsx, lmc-app/babel.config.js, lmc-app/app.config.js, lmc-app/package.json, lmc-app/app/lib/blur-config.ts</files>
  <action>
    ONLY after Task 1 passes (the post-record path is proven), remove the abandoned live-blur scaffold:
    - Delete `app/(scout)/_filming-blur-overlay.tsx`.
    - Remove its import + the BLUR_NATIVE_ENABLED conditional <BlurViewfinder> branch from filming.tsx (filming.tsx is in this plan's read_first; edit it minimally to drop the dead branch — the camera + onRecordingFinished stay exactly as-is). NOTE: filming.tsx is listed in Plan 05 files_modified; this plan runs in a LATER wave so there is no parallel conflict — sequential edit is safe.
    - Remove `['react-native-worklets-core/plugin']` from babel.config.js (only needed for the worklet overlay). If that leaves babel.config.js with no plugins, keep the file with an empty plugins array + a comment.
    - Uninstall the three live-path packages from package.json: react-native-worklets-core, react-native-vision-camera-face-detector, @shopify/react-native-skia (none are used by the post-record module, which uses first-party iOS frameworks). Run the package manager remove so the lockfile updates.
    - Update the app.config.js Phase-6 Category-B comment block to reflect that the live path is removed and the post-record lmc-blur module is the mechanism (don't leave stale instructions referencing the deleted packages).
    - In blur-config.ts, remove or clearly retire BLUR_NATIVE_ENABLED (the live-viewfinder flag) and keep BLUR_POST_RECORD_ENABLED as the single live flag, with the per-market enable checklist comment.

    DO NOT delete: supabase/functions/face-blur-check, the mux-webhook blur gate, or market_config.blur_enabled — these stay as the dormant last-resort server net (D-04).
  </action>
  <verify>
    <automated>cd lmc-app && npx tsc --noEmit</automated>
  </verify>
  <done>_filming-blur-overlay.tsx deleted; filming.tsx live-blur branch removed (camera/recording intact); worklets-core babel plugin removed; the three live-path packages uninstalled (lockfile updated); app.config.js comment updated; BLUR_NATIVE_ENABLED retired, BLUR_POST_RECORD_ENABLED kept; server-side net untouched; tsc clean.</done>
  <threat_model>
    - Build fragility: removing worklets-core eliminates the undefined-RNWorklet/Hermes-CMake risk class (Pitfall 2) — a stability gain, but it changes native deps, so it MUST be re-verified by a build (Task 3).
    - Accidental removal of the safety net: explicitly KEEP face-blur-check + the server gate (D-04) — only the live-viewfinder scaffold goes.
  </threat_model>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [DEVICE BUILD] post-cleanup build still compiles, boots, and blurs</name>
  <action>Orchestrator gate: run the device build below and walk the verification steps. This is a verification checkpoint AFTER the autonomous tasks above completed their work; the build command and checks are in <how-to-verify>. On any failure, STOP and diagnose at this layer before the next wave.</action>
  <what-built>The codebase with the dead worklets-core live-blur scaffold removed. This confirms removing those native deps didn't break the build and the proven post-record blur still works.</what-built>
  <how-to-verify>
    Orchestrator runs the device build after the cleanup:

    1. `cd lmc-app && npx expo prebuild --clean -p ios` then `npx expo run:ios --configuration Release --device <udid>` — "Build Succeeded" with worklets-core/face-detector/Skia gone (lmc-blur still links).
    2. App boots; Scout filming opens (no more BlurViewfinder branch); no crash.
    3. Re-run the end-to-end smoke (flag ON): film a face → delivered clip is still blurred → confirm the cleanup didn't regress the proven path.

    PASS = clean prebuild + Release build succeeds without the removed packages + app boots + blur still works end-to-end. FAIL → the removal broke linking/babel; restore the minimum needed and re-verify (do NOT re-add the live overlay).
  </how-to-verify>
  <resume-signal>Type "approved" when the post-cleanup build compiles, boots, and the delivered clip is still blurred; or paste the build error.</resume-signal>
  <threat_model>
    - This guards the dependency-removal change behind a build — removing native packages can break linking, so it gets its own gate (consistent with the per-step discipline).
  </threat_model>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Scout camera → Seeker delivered clip | The full chain; the delivered clip the Seeker watches must have faces blurred (the end-to-end privacy promise). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-18 | Information disclosure (delivered clip unblurred) | full record→deliver chain | mitigate | Troy visually confirms the DELIVERED clip is blurred (Task 1); dormant server net still backs failures (kept). |
| T-08-19 | DoS (native instability under repeated use) | repeated film/submit | mitigate | Repeat 3-5 cycles at the Step-6 gate; the post-record module has no worklet bridge (the repeated-mount crash class is gone). |
| T-08-20 | Build fragility (removing native deps breaks build) | worklets-core/Skia removal | mitigate | Cleanup gated behind a fresh prebuild + Release build (Task 3); server net explicitly kept. |
| T-08-21 | Information disclosure (accidental net removal) | face-blur-check deletion | mitigate | Explicitly KEEP the server-side detect-and-hold + mux-webhook gate (D-04); only the live-viewfinder scaffold is removed. |
</threat_model>

<verification>
- tsc clean.
- [DEVICE, orchestrator + Troy] Step-6 gate: Scout films a face → Seeker's DELIVERED clip is blurred (Troy confirm); stable over 3-5 repeats; raw never left on the happy path.
- [DEVICE, orchestrator] post-cleanup gate: clean prebuild + Release build succeeds without worklets-core/face-detector/Skia; app boots; blur still works end-to-end; server-side net intact.
</verification>

<success_criteria>
End-to-end: a Scout films a face and the Seeker watches a delivered clip with the face blurred (Troy-confirmed), stable over repeats, raw never leaving the device on the happy path. The dead worklets-core live-blur scaffold is removed and the post-cleanup build still compiles, boots, and blurs. BLUR_POST_RECORD_ENABLED is ready to flip true per-market with the server net kept as the last-resort fallback. Phase complete only on Troy's confirm.
</success_criteria>

<output>
After completion, create `.planning/phases/08-on-device-face-blur-custom-expo-native-module-post-record-bl/08-06-SUMMARY.md`
</output>
