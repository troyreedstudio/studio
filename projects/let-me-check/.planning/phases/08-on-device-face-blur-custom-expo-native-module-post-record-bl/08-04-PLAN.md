---
phase: 08-on-device-face-blur-custom-expo-native-module-post-record-bl
plan: 04
type: execute
wave: 4
depends_on: ["08-03"]
files_modified:
  - lmc-app/modules/lmc-blur/ios/LmcVideoExport.swift
  - lmc-app/modules/lmc-blur/ios/LmcFaceBlur.swift
  - lmc-app/modules/lmc-blur/ios/LmcBlurModule.swift
autonomous: false
requirements: [BLUR-NATIVE-01, BLUR-03]
must_haves:
  truths:
    - "A REAL 15s 1080p recorded clip (multiple faces, moving) is blurred end-to-end and the output plays correctly with faces obscured across the whole clip"
    - "Blur processing time on a real device is acceptable (~ a few seconds, target ≤ ~10s on iOS for a 15s clip)"
    - "Memory stays bounded processing a 15s 1080p clip (no OOM crash)"
  artifacts:
    - path: "lmc-app/modules/lmc-blur/ios/LmcVideoExport.swift"
      provides: "Full-clip blur export tuned for 1080p/15s performance + quality"
      contains: "AVAssetExportSession"
  key_links:
    - from: "lmc-app/modules/lmc-blur/ios/LmcBlurModule.swift"
      to: "device clock"
      via: "blurFaces measures + logs processing duration"
      pattern: "duration|elapsed|CFAbsoluteTime"
---

<objective>
STEP 4 of the 6-step plan (D-05): prove the blur works on a REAL recorded VIDEO FILE end-to-end — a full 15s 1080p clip with movement and possibly multiple faces — AND that the processing time + memory are acceptable on a real device. Plan 03 proved the math on a clip; this proves it at production scale and within the UX/perf budget.

Purpose: Find perf/memory problems (slow blur, OOM on a long 1080p clip, dropped detections on fast movement) BEFORE wiring into the upload flow — so Plan 05 wires a path that's already known to be fast and stable. This is where the 06-RESEARCH-BLUR-V2 latency assumption (A1: ~3-10s) is verified on device.
Output: A perf-tuned blurFaces that processes a real 15s 1080p clip in an acceptable time, logs its duration, keeps memory bounded, and blurs faces across the WHOLE clip (temporal coverage, not just one frame). Tunables adjusted as needed (sample rate, pixelate vs gaussian, export preset). Still NOT wired into the flow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-on-device-face-blur-custom-expo-native-module-post-record-bl/08-CONTEXT.md
@.planning/phases/08-on-device-face-blur-custom-expo-native-module-post-record-bl/08-VALIDATION.md
@.planning/phases/06-privacy-anti-fraud-hardening-on-device-face-plate-blur-befor/06-RESEARCH-BLUR-V2.md

<interfaces>
<!-- Contract unchanged. This plan tunes performance, not the surface. -->
BlurResult = { outputPath: string; facesBlurred: number; status: 'blurred'|'no_faces'|'failed' }

Perf levers (06-RESEARCH-BLUR-V2 Part 3 + Assumptions A1):
- Detect at ≤15fps + interpolate/track between samples (don't re-detect every frame — Pitfall 3).
- Metal-backed CIContext; consider MPSImageGaussianBlur if CIGaussianBlur is too slow.
- CIPixellate is cheaper than CIGaussianBlur — viable if gaussian is over budget.
- Export preset matching capture quality (keep 1080p, D-05 "keep 1080p/quality"); video track only (VID-02).
- A1 target: ~3-10s for a 15s clip on a modern iPhone. If much slower, lower sample rate / switch to pixelate / use MPS.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: temporal coverage + perf instrumentation</name>
  <read_first>
    - 06-RESEARCH-BLUR-V2.md Pitfall 3 (≤15fps sampling + track/interpolate) and Assumptions A1 (latency target, mitigations).
    - Step 4 of the incremental table: "wire-up not needed yet — measure time on a real mid-tier device; 15s clip blurred; ≤~10s iOS."
  </read_first>
  <files>lmc-app/modules/lmc-blur/ios/LmcVideoExport.swift, lmc-app/modules/lmc-blur/ios/LmcBlurModule.swift</files>
  <action>
    Ensure TEMPORAL coverage: faces detected at sampled times must be interpolated/held across the in-between frames so a moving face stays blurred continuously (not just on sampled frames). Implement a simple hold/interpolate: for each frame, use the nearest sampled detection within a small window (and optionally expand the rect a little extra to absorb motion between samples). This guarantees no un-blurred frame slips through as a face moves.

    Add perf instrumentation in `LmcBlurModule.swift`: measure wall-clock duration of the full blurFaces call (CFAbsoluteTimeGetCurrent before/after) and log it (e.g. `[LMC-BLUR] blurred N faces in X.Xs for a Ys clip`). This number is read at the Step-4 device check to confirm the budget.

    Keep export preset at capture quality (1080p, D-05). Files <500 lines.
  </action>
  <verify>
    <automated>cd lmc-app && npx tsc --noEmit</automated>
  </verify>
  <done>Detections are interpolated/held across frames (continuous blur on movement); blurFaces logs its processing duration; export stays 1080p video-only; tsc clean.</done>
  <threat_model>
    - Temporal leak: a face un-blurred on un-sampled frames is a privacy hole — interpolation + rect padding mitigate; Troy confirms "blurred across the whole clip" at the gate.
    - Processing-time/UX: instrumented so the budget is measured, not assumed.
  </threat_model>
</task>

<task type="auto">
  <name>Task 2: memory + perf tuning to budget</name>
  <read_first>
    - 06-RESEARCH-BLUR-V2.md Assumptions A1 mitigations (MPS / pixelate / lower sample rate); Part 3 "use Metal-backed CIContext".
    - 08-CONTEXT D-05 discretion: re-encode settings (keep 1080p quality), processing-progress UX.
  </read_first>
  <files>lmc-app/modules/lmc-blur/ios/LmcFaceBlur.swift, lmc-app/modules/lmc-blur/ios/LmcVideoExport.swift</files>
  <action>
    Process frames in a streaming fashion (the applyingCIFiltersWithHandler pipeline already does this — verify no accumulation of frames/CIImages in memory across the whole clip; release per-frame intermediates). Reuse a single Metal-backed CIContext (don't allocate per frame). If the Step-4 device check shows the duration is over budget (>~10-12s for a 15s clip), apply ONE mitigation in priority order and re-measure: (a) lower detection sample rate, (b) switch blur mode to CIPixellate, (c) use MPSImageGaussianBlur. Document which mitigation (if any) was applied and the resulting time/quality tradeoff.

    Do NOT drop below 1080p output (D-05 keep quality) unless absolutely necessary, and if so, flag it for Troy rather than doing it silently.
  </action>
  <verify>
    <automated>cd lmc-app && npx tsc --noEmit</automated>
  </verify>
  <done>Single reused CIContext; per-frame intermediates released (bounded memory); blur within the time budget on device (mitigation applied + documented only if needed); 1080p preserved; tsc clean.</done>
  <threat_model>
    - Memory on 15s 1080p: streaming + per-frame release + single CIContext prevent OOM; verified by no-crash on a full 15s clip at the gate.
    - Quality: keep 1080p (D-05); any forced downscale is surfaced to Troy, not silent.
  </threat_model>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: [DEVICE BUILD] Step-4 gate — full 15s 1080p clip blurred, fast, no OOM</name>
  <action>Orchestrator gate: run the device build below and walk the verification steps. This is a verification checkpoint AFTER the autonomous tasks above completed their work; the build command and checks are in <how-to-verify>. On any failure, STOP and diagnose at this layer before the next wave.</action>
  <what-built>blurFaces tuned to process a real 15s 1080p clip end-to-end with continuous face coverage, measured timing, and bounded memory. Still NOT wired into the upload flow.</what-built>
  <how-to-verify>
    Orchestrator runs the device build:

    1. `cd lmc-app && npx expo run:ios --configuration Release --device <udid>` — "Build Succeeded".
    2. Film a FULL ~15s 1080p clip with a moving face (Troy walking/turning), ideally a second face in frame → trigger blurFaces.
    3. Output plays start-to-finish with EVERY face blurred across the WHOLE clip (no un-blurred frames as the face moves), status 'blurred'.
    4. Read the `[LMC-BLUR] ... in X.Xs` log — X should be roughly a few seconds (target ≤ ~10s for a 15s clip). Note the actual number.
    5. No memory crash; the app stays responsive. Run it 2-3 times back to back to check stability/leaks.

    PASS = build succeeds + whole-clip blur is continuous + time is acceptable + no OOM/crash over repeats. FAIL on time → apply a perf mitigation (Task 2) and re-measure. FAIL on coverage → fix interpolation (Task 1). FAIL on memory → fix streaming/release. STOP and fix at this layer before Plan 05 (wiring).
  </how-to-verify>
  <resume-signal>Troy/orchestrator: type "approved" with the measured time when a full 15s clip is fully blurred in acceptable time with no crash; or report the failing dimension (time / coverage / memory).</resume-signal>
  <threat_model>
    - This is the perf/scale proof at production size — a slow or crashy blur would otherwise only surface AFTER wiring (Plan 05), polluting the flow check. Isolate it here.
  </threat_model>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| full recorded clip → blurred output at scale | Production-size 15s 1080p clip processed entirely on-device before any delivery candidacy. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-11 | Information disclosure (temporal leak on movement) | sampled detection interpolation | mitigate | Hold/interpolate detections across frames + extra motion padding; Troy confirms continuous coverage on a moving-face clip. |
| T-08-12 | DoS (OOM on 15s 1080p) | per-frame memory | mitigate | Stream frames, release intermediates, single reused Metal CIContext; verified by no-crash over repeated runs. |
| T-08-13 | DoS / poor UX (blur too slow) | blur+export duration | mitigate | Instrumented timing; mitigations (lower sample rate → pixelate → MPS) applied to hit the budget; measured at the gate. |
</threat_model>

<verification>
- tsc clean.
- [DEVICE, orchestrator + Troy] Step-4 gate: Release build succeeds; full 15s 1080p moving-face clip fully+continuously blurred; processing time acceptable (measured); no OOM over repeats; still not wired into upload.
</verification>

<success_criteria>
A real 15s 1080p clip is blurred end-to-end with continuous coverage, in an acceptable time, with bounded memory and no crashes — proven at production scale before wiring. If time/coverage/memory fail, STOP and tune at this layer.
</success_criteria>

<output>
After completion, create `.planning/phases/08-on-device-face-blur-custom-expo-native-module-post-record-bl/08-04-SUMMARY.md`
</output>
