---
phase: 08-on-device-face-blur-custom-expo-native-module-post-record-bl
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - lmc-app/modules/lmc-blur/ios/LmcBlurModule.swift
  - lmc-app/modules/lmc-blur/ios/LmcVideoExport.swift
  - lmc-app/modules/lmc-blur/ios/LmcFaceDetect.swift
  - lmc-app/modules/lmc-blur/src/LmcBlur.types.ts
autonomous: false
requirements: [BLUR-NATIVE-01]
must_haves:
  truths:
    - "blurFaces re-encodes a recorded video file through AVFoundation and returns a NEW playable file (no blur yet)"
    - "blurFaces runs Vision face detection and reports a plausible face count (1 in a selfie, 0 on a wall shot) — still no blur applied"
    - "The app still boots and the call still resolves under a Release device build"
  artifacts:
    - path: "lmc-app/modules/lmc-blur/ios/LmcVideoExport.swift"
      provides: "AVAsset → AVAssetExportSession passthrough re-encode (no blur), returns new temp path"
      contains: "AVAssetExportSession"
    - path: "lmc-app/modules/lmc-blur/ios/LmcFaceDetect.swift"
      provides: "Vision VNDetectFaceRectanglesRequest face detection over sampled frames"
      contains: "VNDetectFaceRectanglesRequest"
  key_links:
    - from: "lmc-app/modules/lmc-blur/ios/LmcBlurModule.swift"
      to: "LmcVideoExport + LmcFaceDetect"
      via: "blurFaces orchestrates export + detect, returns facesBlurred count"
      pattern: "facesBlurred"
---

<objective>
STEPS 2+3 of the 6-step plan (D-05): prove the native FRAMEWORKS link and run — first that AVFoundation can re-encode the file (Step 1.5 in 06-RESEARCH-BLUR-V2 table: copy through export with NO blur), then that Vision can DETECT faces and report a count (Step 2: detect, log count, still don't blur). No blurring happens yet. Splitting export-link from detection from blur-compositing means a failure points at exactly one framework.

These two are combined because each is a small, additive change to the same Swift module and both are non-visual checks (the call returns a count / a playable file) — but they are verified as TWO distinct device checks in one build.

Purpose: Isolate "do the Apple frameworks link and run on device" (export) from "does detection find faces" from "does compositing look right" (next plan). If export fails it's a codec/export config issue; if detection fails it's a model/threshold issue — neither is confused with blur.
Output: blurFaces now opens the asset, re-encodes it via AVAssetExportSession (video track only — audio is stripped, VID-02), runs Vision face detection on sampled frames, and returns `{ outputPath: <new file>, facesBlurred: <detected count>, status: 'no_faces' }`. Still returns 'no_faces' because no pixels are blurred yet (Pitfall 5 — never claim 'blurred' until pixels actually change).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-on-device-face-blur-custom-expo-native-module-post-record-bl/08-CONTEXT.md
@.planning/phases/08-on-device-face-blur-custom-expo-native-module-post-record-bl/08-VALIDATION.md
@.planning/phases/06-privacy-anti-fraud-hardening-on-device-face-plate-blur-befor/06-RESEARCH-BLUR-V2.md

<interfaces>
<!-- Contract locked in Plan 01. This plan FILLS IN export + detection but does NOT change the shape. -->
BlurResult = { outputPath: string; facesBlurred: number; status: 'blurred'|'no_faces'|'failed' }

iOS pipeline (06-RESEARCH-BLUR-V2 Part 3, native section):
- AVURLAsset from inputPath
- Detection: VNDetectFaceRectanglesRequest (sample at ≤15fps, not every frame — Pitfall 3)
- Export: AVAssetExportSession, video track ONLY (no audio — VID-02 mic already disabled), Metal-backed CIContext available for the blur step (next plan)
- Vision/CoreImage/AVFoundation are first-party — NO extra pods (08-CONTEXT canonical_refs note).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AVFoundation re-encode passthrough (export links, no blur)</name>
  <read_first>
    - 06-RESEARCH-BLUR-V2.md Part 3 iOS native steps 1-4 (AVURLAsset → AVMutableVideoComposition/applyingCIFiltersWithHandler → AVAssetExportSession → temp path).
    - Pitfall 4: clips are video-only — do NOT re-add audio during re-encode; pass the video track only.
    - Step 1 of the 06-RESEARCH-BLUR-V2 incremental table: "copies it through AVAssetExportSession with NO blur, returns the new path."
  </read_first>
  <files>lmc-app/modules/lmc-blur/ios/LmcVideoExport.swift, lmc-app/modules/lmc-blur/ios/LmcBlurModule.swift</files>
  <action>
    Create `ios/LmcVideoExport.swift`: a helper that takes an input file path, builds an `AVURLAsset`, sets up an `AVMutableVideoComposition(asset:applyingCIFiltersWithHandler:)` whose handler currently just passes the source frame straight through (`request.finish(with: request.sourceImage, context: ...)` — NO filter applied yet), and exports via `AVAssetExportSession` (preset matching capture quality, video track only, no audio per VID-02) to a NEW temp file path under the app's tmp dir. Use a Metal-backed `CIContext` (reused for the blur step next plan). Return the new path.

    Wire blurFaces in `LmcBlurModule.swift` to call this export and resolve `{ outputPath: <new path>, facesBlurred: 0, status: 'no_faces' }`. If export fails, resolve `{ outputPath: inputPath, facesBlurred: 0, status: 'failed' }` (NEVER throw raw; 'failed' routes to the fallback in Step 5). Keep each Swift file <500 lines.

    Do NOT apply any blur. Do NOT touch app-flow files. The applyingCIFiltersWithHandler scaffold is built now so the blur step (Plan 03) only swaps the passthrough for a real CIFilter composite — isolating the export plumbing from the blur math.
  </action>
  <verify>
    <automated>cd lmc-app && npx tsc --noEmit</automated>
  </verify>
  <done>LmcVideoExport.swift exports a video-only re-encode to a new temp path via AVAssetExportSession; blurFaces returns the new path with status 'no_faces' on success / 'failed' on export error; tsc clean; no app-flow files touched.</done>
  <threat_model>
    - File-path/size: output is a new tmp file; size should be comparable to input. A 0-byte or missing output → return 'failed' (caught by the Step-2 device check).
    - Audio leak (VID-02): export video track only — assert no audio track in output during the device check.
    - Native crash: AVAssetExportSession is first-party + async; no worklet bridge. A crash here = export config error, isolated from CV.
  </threat_model>
</task>

<task type="auto">
  <name>Task 2: Vision face detection (count only, still no blur)</name>
  <read_first>
    - 06-RESEARCH-BLUR-V2.md Part 3: VNDetectFaceRectanglesRequest; sample at ≤15fps and track/interpolate for speed (Pitfall 3); cache detections.
    - Step 2 of the incremental table: "Add Vision detection; log facesBlurred count but still don't blur. Count plausible (1 in a selfie, 0 in a wall shot)."
    - Discretion: detection confidence threshold (08-CONTEXT D-05 Claude discretion).
  </read_first>
  <files>lmc-app/modules/lmc-blur/ios/LmcFaceDetect.swift, lmc-app/modules/lmc-blur/ios/LmcBlurModule.swift, lmc-app/modules/lmc-blur/src/LmcBlur.types.ts</files>
  <action>
    Create `ios/LmcFaceDetect.swift`: run `VNDetectFaceRectanglesRequest` over the asset's frames SAMPLED at ≤15fps (not every frame — Pitfall 3), collecting per-time face bounding boxes (normalized rects + the times they apply to) so Plan 03 can composite blur exactly where faces are. Expose: a function returning the sampled detections AND a distinct count of frames-with-≥1-face (or a max-faces count) for the `facesBlurred` telemetry field. Apply a sensible confidence threshold (Claude discretion; default ~0.3 and document it).

    In `LmcBlurModule.swift`, call detection inside blurFaces and set `facesBlurred` to the detected count. STILL return `status: 'no_faces'` if you want the truthful "no pixels blurred yet" — OR add a transient internal-only status for this step. Recommended: keep the public status 'no_faces' (no pixels changed) but LOG the real detected count to the device console so the Step-2 check can read "1 face detected" without lying about blur having happened (Pitfall 5).

    Add to `LmcBlur.types.ts` (if useful) an internal detection shape, but do NOT change the public BlurResult contract. No blur applied. No app-flow files touched.
  </action>
  <verify>
    <automated>cd lmc-app && npx tsc --noEmit</automated>
  </verify>
  <done>LmcFaceDetect.swift runs VNDetectFaceRectanglesRequest on sampled frames and returns detections + count; blurFaces logs/returns the count; no blur applied; public contract unchanged; tsc clean.</done>
  <threat_model>
    - Detection accuracy: a wall shot must report ~0 faces, a selfie ~1 — verified on device (Step-2 check). Over-detection wastes blur; under-detection is the privacy risk handled in Plan 03's compositing + Plan 05's fallback.
    - Processing-time: ≤15fps sampling (not per-frame) keeps a 15s 1080p clip cheap — the perf budget proper is measured in Plan 04.
    - Memory on 15s 1080p: detection reads frames sequentially (sampled), not all at once — avoids loading the whole clip into memory.
  </threat_model>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [DEVICE BUILD] Step-2 gate — export re-encodes + detection counts faces</name>
  <action>Orchestrator gate: run the device build below and walk the verification steps. This is a verification checkpoint AFTER the autonomous tasks above completed their work; the build command and checks are in <how-to-verify>. On any failure, STOP and diagnose at this layer before the next wave.</action>
  <what-built>blurFaces now re-encodes the recorded file via AVFoundation (video-only) and runs Vision face detection, reporting a face count. NO blur is applied and nothing is wired into the upload flow yet.</what-built>
  <how-to-verify>
    Orchestrator runs the device build:

    1. `cd lmc-app && npx expo run:ios --configuration Release --device <udid>` — MUST "Build Succeeded" (Vision + AVFoundation now linked).
    2. App boots, filming opens — no crash.
    3. Export check: trigger blurFaces on a real recorded clip (temporary dev hook, removed after) and confirm it returns a NEW outputPath that PLAYS, with NO audio track (VID-02), status 'no_faces'.
    4. Detection check: film a SELFIE clip → device log reports ~1 face. Film a WALL/empty shot → device log reports ~0 faces. The count must be plausible, not always-0 or always-huge.

    PASS = build succeeds + app boots + a playable re-encoded (audio-free) file comes back + face count is plausible (1 selfie / 0 wall). FAIL on export → codec/export-config issue (isolated). FAIL on detection → model/threshold issue (isolated). Either way STOP and fix at THIS layer before Plan 03 (blur).
  </how-to-verify>
  <resume-signal>Type "approved" when the re-encode plays (audio-free) and face counts are plausible; or paste the failure (export vs detection) to diagnose.</resume-signal>
  <threat_model>
    - This gate separates "frameworks link + export works" from "detection works" so a Plan-03 blur failure later can't be confused with an export or detection failure.
    - Unblurred-leak: still 'no_faces' / no pixels changed; nothing delivered (upload untouched).
  </threat_model>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| recorded file → native processing | A locally recorded clip path is read, decoded, detected-over, and re-encoded to a new tmp file. All on-device. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | Information disclosure (audio leak) | AVAssetExportSession re-encode | mitigate | Export video track ONLY (VID-02); device check asserts no audio track in output. |
| T-08-05 | Tampering / wrong-result (false detection) | VNDetectFaceRectanglesRequest threshold | mitigate | Confidence threshold tuned + verified on device (selfie→1, wall→0); under-detection backstopped by Plan-05 fallback. |
| T-08-06 | DoS (memory on 15s 1080p) | frame sampling | mitigate | Sample ≤15fps sequentially, never load the whole clip into RAM (Pitfall 3); perf budget measured in Plan 04. |
| T-08-07 | DoS (export failure → bad file) | export error path | mitigate | On export failure return status 'failed' (not a broken file as 'blurred'); routes to fallback in Plan 05. |
</threat_model>

<verification>
- tsc clean.
- [DEVICE, orchestrator] Step-2 gate: Release build succeeds; re-encoded file plays and has NO audio; face count plausible (selfie ~1, wall ~0); still no blur, upload untouched.
</verification>

<success_criteria>
AVFoundation export and Vision detection both link and run on device: blurFaces returns a playable audio-free re-encode and a plausible face count, with no blur applied and no app-flow changes. Failures isolate to export vs detection. If either fails, STOP at this layer.
</success_criteria>

<output>
After completion, create `.planning/phases/08-on-device-face-blur-custom-expo-native-module-post-record-bl/08-02-SUMMARY.md`
</output>
