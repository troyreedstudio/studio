---
phase: 08-on-device-face-blur-custom-expo-native-module-post-record-bl
plan: 03
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - lmc-app/modules/lmc-blur/ios/LmcBlurModule.swift
  - lmc-app/modules/lmc-blur/ios/LmcFaceBlur.swift
  - lmc-app/modules/lmc-blur/ios/LmcVideoExport.swift
  - lmc-app/app/lib/blur-config.ts
autonomous: false
requirements: [BLUR-NATIVE-01, BLUR-03]
must_haves:
  truths:
    - "blurFaces blurs the detected face regions in the OUTPUT video file and returns status 'blurred' with the count"
    - "When faces are found, the delivered file's faces are visibly obscured on playback (Troy confirms)"
    - "When NO faces are found, the original is returned untouched with status 'no_faces' (still safe to deliver)"
  artifacts:
    - path: "lmc-app/modules/lmc-blur/ios/LmcFaceBlur.swift"
      provides: "Core Image blur (CIGaussianBlur/CIPixellate) masked to face rects, composited per frame"
      contains: "CIGaussianBlur"
    - path: "lmc-app/app/lib/blur-config.ts"
      provides: "Blur tunables (radius, mode) for the post-record module"
      contains: "BLUR"
  key_links:
    - from: "lmc-app/modules/lmc-blur/ios/LmcVideoExport.swift"
      to: "LmcFaceBlur"
      via: "applyingCIFiltersWithHandler calls the face-blur compositor per frame"
      pattern: "CIFilter|applyingCIFilters"
---

<objective>
STEP 3 of the 6-step plan (D-05): apply the actual BLUR. Swap the passthrough handler built in Plan 02 for a real Core Image composite that blurs ONLY the detected face rects, frame by frame, during export. This is the first step with a VISUAL outcome — Troy confirms faces are blurred in the saved clip.

Purpose: Prove the masking/compositing math is correct, in isolation from the upload wiring (that's Plan 05). A failure here is a compositing/mask issue only — not export, not detection, not flow.
Output: blurFaces returns a re-encoded file whose detected faces are blurred (CIGaussianBlur default; CIPixellate as a cheaper alternative — Claude discretion D-05), with status 'blurred' + count. status 'no_faces' returns the original untouched (safe). status 'failed' on any error (never the raw file as 'blurred' — Pitfall 5). Still NOT wired into the upload flow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-on-device-face-blur-custom-expo-native-module-post-record-bl/08-CONTEXT.md
@.planning/phases/08-on-device-face-blur-custom-expo-native-module-post-record-bl/08-VALIDATION.md
@.planning/phases/06-privacy-anti-fraud-hardening-on-device-face-plate-blur-befor/06-RESEARCH-BLUR-V2.md
@lmc-app/app/lib/blur-config.ts

<interfaces>
<!-- Contract locked in Plan 01; this plan starts RETURNING status 'blurred'. -->
BlurResult = { outputPath: string; facesBlurred: number; status: 'blurred'|'no_faces'|'failed' }

Compositing (06-RESEARCH-BLUR-V2 Part 3 iOS step 2):
- In applyingCIFiltersWithHandler, per frame: build a blurred copy (CIGaussianBlur, or CIPixellate),
  composite the blurred pixels ONLY inside each detected face rect (expanded ~20%) over the original via a mask.
- Tunables (Claude discretion): radius (~18-25px equiv at 1080p), mode (gaussian default, pixelate cheaper), face-rect padding.
- blur-config.ts already holds BLUR_PIXEL_RADIUS=18 from Phase 6 — reuse/extend it for the post-record module (the BLUR_NATIVE_ENABLED viewfinder flag there is the OLD live path; keep it false, add post-record tunables).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Core Image face-blur compositor</name>
  <read_first>
    - 06-RESEARCH-BLUR-V2.md Part 3 iOS native step 2 (CIGaussianBlur masked to expanded face rects); Don't-Hand-Roll table (use CIGaussianBlur/CIPixellate, not a custom Metal shader).
    - Pitfall 5: 'failed' and 'no_faces' must stay distinct from 'blurred'; never return the raw file labelled 'blurred'.
    - blur-config.ts current contents (BLUR_PIXEL_RADIUS, BLUR_NATIVE_ENABLED — the live-path flag, keep false).
  </read_first>
  <files>lmc-app/modules/lmc-blur/ios/LmcFaceBlur.swift, lmc-app/modules/lmc-blur/ios/LmcVideoExport.swift</files>
  <action>
    Create `ios/LmcFaceBlur.swift`: given a source `CIImage` for a frame + the face rects active at that frame's time (from LmcFaceDetect's cached detections), produce a composited `CIImage` where each face rect (expanded ~20% padding) is replaced by a blurred version of the underlying pixels. Implementation: apply `CIGaussianBlur` (radius from config) to the whole frame, then mask-composite the blurred output only inside the union of face rects over the sharp original (e.g. a rect mask via CISourceOverCompositing / blendWithMask). Support a `mode` of 'gaussian' (default) or 'pixelate' (CIPixellate — cheaper, used by the Plan-05 fallback). Use the Metal-backed CIContext from Plan 02.

    Modify `LmcVideoExport.swift`: replace the passthrough in `applyingCIFiltersWithHandler` with a call to LmcFaceBlur, passing the face rects for `request.compositionTime`. If there are no faces at a given frame, pass the source image through unchanged.

    Keep each Swift file <500 lines (split a helper if needed). Map the detection->time lookup efficiently (don't re-detect per frame — reuse Plan 02's sampled cache).
  </action>
  <verify>
    <automated>cd lmc-app && npx tsc --noEmit</automated>
  </verify>
  <done>LmcFaceBlur.swift blurs detected face rects (gaussian default, pixelate option) and is wired into the export handler; export still produces a playable file; tsc clean.</done>
  <threat_model>
    - Mask correctness: blur must cover the FULL face (20% padding) — too-tight a rect leaks the face edge. Verified visually by Troy in Task 3.
    - Unblurred-leak: if compositing throws, return 'failed' (Plan-05 fallback), never the sharp file as 'blurred'.
    - Native crash: Core Image filters are first-party + run on the export thread, no worklet bridge.
  </threat_model>
</task>

<task type="auto">
  <name>Task 2: status semantics + blur tunables</name>
  <read_first>
    - 06-RESEARCH-BLUR-V2.md Part 3 status semantics: 'blurred' (faces blurred), 'no_faces' (none found, original returned, safe), 'failed' (fallback, never deliver as blurred).
    - 08-CONTEXT D-05 Claude discretion: blur strength/style, detection threshold, re-encode quality (keep 1080p), processing UX.
  </read_first>
  <files>lmc-app/modules/lmc-blur/ios/LmcBlurModule.swift, lmc-app/app/lib/blur-config.ts</files>
  <action>
    In `LmcBlurModule.swift` finalize blurFaces status logic:
    - detections found AND blur+export succeeded → `{ outputPath: <new file>, facesBlurred: count, status: 'blurred' }`, and delete nothing here (raw deletion happens in Plan 05 after a successful upload).
    - zero detections → skip the blur composite, return the ORIGINAL path untouched, `status: 'no_faces'` (re-encoding a face-free clip is wasted time; original is safe to deliver).
    - any error (export/detect/blur) → `{ outputPath: inputPath, facesBlurred: 0, status: 'failed' }` (Plan 05 turns 'failed' into retry→pixelate→server-fallback; NEVER uploads inputPath thinking it's blurred).

    Extend `blur-config.ts` with POST-RECORD tunables consumed by blur-native.ts/the module: a default radius and mode for the post-record path (reuse BLUR_PIXEL_RADIUS=18 or add BLUR_POST_RECORD_RADIUS + BLUR_POST_RECORD_MODE). Leave BLUR_NATIVE_ENABLED (the OLD live-viewfinder flag) as-is and FALSE — that path is abandoned (08-CONTEXT D-02); add a comment that the post-record module is the live mechanism now and is gated by a NEW flag added in Plan 05.
  </action>
  <verify>
    <automated>cd lmc-app && npx tsc --noEmit</automated>
  </verify>
  <done>blurFaces returns 'blurred'/'no_faces'/'failed' correctly; no_faces returns the untouched original; failed returns input with status 'failed' (never mislabelled); blur-config exposes post-record tunables; tsc clean.</done>
  <threat_model>
    - Privacy-by-default: 'failed' never masquerades as 'blurred' — the single most important invariant (Pitfall 5 / D-04). Enforced here and consumed by Plan 05.
    - File-path: 'no_faces' returns the original path (caller uploads it as-is, which is correct — no faces to hide).
  </threat_model>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [DEVICE BUILD] Step-3 gate — faces are visibly blurred in the saved clip</name>
  <action>Orchestrator gate: run the device build below and walk the verification steps. This is a verification checkpoint AFTER the autonomous tasks above completed their work; the build command and checks are in <how-to-verify>. On any failure, STOP and diagnose at this layer before the next wave.</action>
  <what-built>blurFaces now actually blurs detected faces in the re-encoded output file. Still NOT wired into the upload flow — this proves the blur/compositing math is correct in isolation.</what-built>
  <how-to-verify>
    Orchestrator runs the device build:

    1. `cd lmc-app && npx expo run:ios --configuration Release --device <udid>` — "Build Succeeded".
    2. App boots, filming opens — no crash.
    3. Troy films HIMSELF (face clearly in frame) → trigger blurFaces (temporary dev hook) → SAVE/open the output file → Troy visually confirms his FACE IS BLURRED across the clip (not just one frame; the whole face, with margin). status 'blurred', count ≥1.
    4. Film a WALL/no-face shot → output returns untouched, status 'no_faces', clip looks normal.
    5. (Optional) toggle mode 'pixelate' to sanity-check the cheaper path also obscures the face.

    PASS = build succeeds + Troy confirms his face is blurred in the saved clip + no-face clip is untouched. FAIL = masking/compositing issue (isolated — not export, not detection). STOP and fix the blur math before Plan 04 (video-file perf) / Plan 05 (wiring).
  </how-to-verify>
  <resume-signal>Troy: type "approved" when your face is clearly blurred in the saved clip and a no-face clip is untouched; or describe what's wrong (face not fully covered, wrong region, etc.).</resume-signal>
  <threat_model>
    - This is the first VISUAL privacy proof — Troy's eyes are the gate. An incomplete blur (face edge showing) is caught here, before any real upload exists.
  </threat_model>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| detected face rects → blurred output pixels | Face regions are obscured before the file is ever a delivery candidate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-08 | Information disclosure (face edge leak) | mask/padding around face rect | mitigate | Expand each rect ~20%; Troy visually confirms full-face coverage on device (Step-3 gate). |
| T-08-09 | Information disclosure (raw labelled blurred) | status finalization | mitigate | 'failed'/'no_faces' kept distinct from 'blurred' (Pitfall 5); never return the sharp file as 'blurred'. |
| T-08-10 | DoS (compositing crash) | Core Image per-frame filter | mitigate | First-party CIFilter on the export thread (no worklet bridge); on throw return 'failed' → fallback (Plan 05). |
</threat_model>

<verification>
- tsc clean.
- [DEVICE, orchestrator + Troy] Step-3 gate: Release build succeeds; Troy confirms his face is blurred in the saved clip; no-face clip untouched ('no_faces'); not yet wired into upload.
</verification>

<success_criteria>
blurFaces blurs detected faces in the output file (Troy-confirmed), returns 'blurred' with count, returns the untouched original on 'no_faces', and 'failed' on error (never the raw file mislabelled). Compositing is proven in isolation. If the blur is wrong, STOP at this layer.
</success_criteria>

<output>
After completion, create `.planning/phases/08-on-device-face-blur-custom-expo-native-module-post-record-bl/08-03-SUMMARY.md`
</output>
