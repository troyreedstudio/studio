---
phase: 01-foundation-auth-persistence-event-log
plan: 03
type: execute
wave: 3
depends_on: ["02"]
files_modified:
  - lmc-app/app/state/intended-role.ts
  - lmc-app/app/state/saved.ts
  - lmc-app/app/state/recents.ts
  - lmc-app/app/state/payment-method.ts
  - lmc-app/app/state/recurring.ts
  - lmc-app/app/state/scout-earnings.ts
  - lmc-app/app/onboarding/role.tsx
  - lmc-app/app/onboarding/quick-finish.tsx
  - lmc-app/app/legal/[doc].tsx
  - lmc-app/app/(seeker)/profile.tsx
  - lmc-app/app/(scout)/profile.tsx
  - lmc-app/app/(seeker)/payment-methods.tsx
  - lmc-app/.env.example
  - lmc-app/eas.json
  - lmc-app/app/state/saved.test.ts
  - lmc-app/app/state/consent.test.ts
autonomous: false
requirements: [AUTH-03, AUTH-04, DATA-01, DATA-03, SAFE-02]
user_setup:
  - service: eas
    why: "Supabase URL + anon key must be registered as EAS env vars or they resolve null in TestFlight (the Mapbox blank-map bug repeats)"
    env_vars:
      - name: EXPO_PUBLIC_SUPABASE_URL
        source: "Set via `eas env:create` / EAS Dashboard -> Project -> Environment Variables (same value as local .env)"
      - name: EXPO_PUBLIC_SUPABASE_ANON_KEY
        source: "Set via `eas env:create` / EAS Dashboard -> Environment Variables"
    dashboard_config:
      - task: "Register EXPO_PUBLIC_SUPABASE_URL and EXPO_PUBLIC_SUPABASE_ANON_KEY for the build profile (not just local .env)"
        location: "EAS Dashboard -> Environment Variables, or `eas env:create`"
must_haves:
  truths:
    - "Saved places, recents, payment method, role, and recurring checks persist in Supabase and survive an app restart"
    - "The 6 rewired stores keep their existing export surface so the ~12 importing screens need no signature changes"
    - "18+/Terms/Privacy/AUP acceptance is recorded to the consents table + event_log at onboarding (SAFE-02)"
    - "A user can switch Seeker↔Scout (current_role) and sign out from the profile screens"
    - "Supabase URL + anon key are registered as EAS env vars (not just local .env)"
    - "state/location.ts is unchanged (stays client-side per the locked decision)"
  artifacts:
    - path: "lmc-app/app/state/saved.ts"
      provides: "Saved-places store backed by lib/api (same exports)"
      contains: "lib/api"
    - path: "lmc-app/app/legal/[doc].tsx"
      provides: "Consent acceptance wired to recordConsent (SAFE-02)"
      contains: "recordConsent"
    - path: "lmc-app/.env.example"
      provides: "Documented client env vars (no secrets committed)"
      contains: "EXPO_PUBLIC_SUPABASE_URL"
  key_links:
    - from: "lmc-app/app/state/saved.ts"
      to: "lib/api.ts"
      via: "store mutators call api wrappers"
      pattern: "from '.*lib/api'"
    - from: "lmc-app/app/onboarding/quick-finish.tsx"
      to: "consents + event_log"
      via: "recordConsent at onboarding"
      pattern: "recordConsent"
    - from: "lmc-app/app/(seeker)/profile.tsx"
      to: "lib/auth.ts"
      via: "switch role + sign out"
      pattern: "switchRole|signOut|setCurrentRole"
---

<objective>
Make persistence real behind the existing screens. Rewire the 6 in-memory stores (NOT location.ts) so their mutators read/write Supabase via `lib/api.ts` while keeping each store's export surface byte-compatible — so the ~12 importing screens barely change. Wire 18+/Terms/Privacy/AUP consent acceptance to the `consents` table + event log at onboarding (SAFE-02). Wire Seeker↔Scout switch + sign-out into the profile screens. Register the Supabase env vars in EAS so the TestFlight build can reach the backend. Finish with an on-device checkpoint that proves the real-auth + restart-persistence success criteria.

Purpose: This converts "schema + client layer exist" into "the app actually persists and gates on real identity." It closes DATA-01/03, SAFE-02, and the user-facing half of AUTH-03/04.
Output: Persistent stores, recorded consent, working role-switch + sign-out, EAS env config, and a device-verified phase gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundation-auth-persistence-event-log/01-RESEARCH.md
@lmc-app/app/state/saved.ts
@lmc-app/app/state/recents.ts
@lmc-app/app/state/payment-method.ts
@lmc-app/app/state/recurring.ts
@lmc-app/app/state/intended-role.ts
@lmc-app/app/state/scout-earnings.ts

<interfaces>
<!-- The export surface that MUST be preserved per store (grep'd from the existing files). -->
<!-- Plan 02's lib/api.ts already exposes the matching async wrappers. -->

saved.ts:        getSaved, isSaved, savePlace, removeSaved, toggleSaved, useSavedPlaces  | type SavedPlace
recents.ts:      addRecent, getRecents, useRecents, relativeTime                          | type RecentCheck
payment-method.ts: getCard, saveCard, clearCard, usePaymentMethod                        | type SavedCard, CardBrand
recurring.ts:    getRecurring, addRecurring, toggleRecurring, removeRecurring, useRecurring | type RecurringCheck, RecurringFreq
intended-role.ts: getIntendedRole, setIntendedRole, useIntendedRole                       | type IntendedRole
scout-earnings.ts: getScoutEarnings, addClipEarning, resetScoutEarnings, useScoutEarnings (REMOVE the 127.0/12 fake seed)

From lib/api.ts (Plan 02): getSavedPlaces/addSavedPlace/removeSavedPlace, getRecents/addRecent,
  getPaymentMethod/savePaymentMethod/clearPaymentMethod, getRecurring/addRecurring/toggleRecurring/removeRecurring,
  getProfile/setCurrentRole, recordConsent, logEvent
From lib/auth.ts (Plan 02): signOut, switchRole

Screens importing these stores (preserve their imports): (scout)/dashboard, (scout)/earnings, (scout)/submitted,
  (seeker)/confirmed, (seeker)/home, (seeker)/payment, (seeker)/recurring-setup, (seeker)/recurring, (seeker)/saved,
  onboarding/quick-finish, onboarding/role, seeker/rules
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewire the 6 stores to Supabase behind their existing export surface (DATA-01/03, AUTH-03)</name>
  <read_first>
    - lmc-app/app/state/saved.ts, recents.ts, payment-method.ts, recurring.ts, intended-role.ts, scout-earnings.ts (preserve every exported name + type)
    - lmc-app/app/lib/api.ts (the wrappers to delegate to — written in Plan 02)
    - lmc-app/app/state/location.ts (DO NOT TOUCH — confirm it stays client-side)
  </read_first>
  <files>lmc-app/app/state/saved.ts, lmc-app/app/state/recents.ts, lmc-app/app/state/payment-method.ts, lmc-app/app/state/recurring.ts, lmc-app/app/state/intended-role.ts, lmc-app/app/state/scout-earnings.ts, lmc-app/app/state/saved.test.ts</files>
  <behavior>
    - savePlace(...) persists to Supabase (saved_places) and a subsequent getSaved()/round-trip returns it (tested against local supabase).
    - The store still exports getSaved, isSaved, savePlace, removeSaved, toggleSaved, useSavedPlaces with the same signatures.
    - scout-earnings no longer seeds 127.0 / 12 — it starts empty/zero and reflects real data (placeholder until Phase 4 aggregates).
    - intended-role reads/writes profiles.current_role (+ is_seeker/is_scout) via api, replacing the module global.
  </behavior>
  <action>
    Rewire each store so its mutators call `lib/api.ts` and its `use*` hook subscribes to a Supabase-backed cache, while keeping the exact export names + types listed in <interfaces>. Pattern per store: keep the pub/sub `_listeners`/`notify()`/`useX()` shape as a local cache, but have mutators `await` the api wrapper then refresh the cache; hydrate the cache on first hook mount from `api.getX()`.

    - `state/saved.ts`: `savePlace/removeSaved/toggleSaved` → `api.addSavedPlace/removeSavedPlace`; hydrate `_saved` from `api.getSavedPlaces()`. Map `SavedPlace.id` ↔ `saved_places.place_key`, `coord [lon,lat]` ↔ geography.
    - `state/recents.ts`: `addRecent` → `api.addRecent`; hydrate from `api.getRecents()`. Keep `relativeTime` pure (unchanged).
    - `state/payment-method.ts`: `saveCard/clearCard` → `api.savePaymentMethod/clearPaymentMethod`; hydrate from `api.getPaymentMethod()`. Keep brand+last4 only (NO Stripe).
    - `state/recurring.ts`: mutators → matching `api.*Recurring`; hydrate from `api.getRecurring()`.
    - `state/intended-role.ts`: `setIntendedRole` → write `profiles.is_seeker/is_scout/current_role` via api (`both` sets both flags, current_role default 'seeker'); `getIntendedRole` derives from the loaded profile (AUTH-03).
    - `state/scout-earnings.ts`: remove the hard-coded `127.0`/`12` seed; default to 0/0; keep the export surface (real aggregate is Phase 4).
    - DO NOT modify `state/location.ts`.
    Run `npx tsc --noEmit` to confirm no screen broke (signatures preserved).
  </action>
  <verify>
    <automated>cd lmc-app && for f in saved recents payment-method recurring intended-role; do grep -q "lib/api" app/state/$f.ts || { echo "MISSING api wiring in $f"; exit 1; }; done && ! grep -q "127.0" app/state/scout-earnings.ts && grep -q "export function getSaved" app/state/saved.ts && grep -q "export function useSavedPlaces" app/state/saved.ts && git diff --quiet app/state/location.ts && npx tsc --noEmit && echo STORES_OK</automated>
  </verify>
  <acceptance_criteria>
    - Each of saved/recents/payment-method/recurring/intended-role `.ts` imports from `lib/api`
    - `scout-earnings.ts` no longer contains `127.0`
    - `saved.ts` still exports `getSaved` and `useSavedPlaces` (surface preserved)
    - `git diff --quiet app/state/location.ts` (location.ts untouched)
    - `npx tsc --noEmit` exits 0 (no screen signature broke)
  </acceptance_criteria>
  <done>All 6 stores persist via Supabase with unchanged export surfaces; location.ts untouched; fake earnings seed removed.</done>
</task>

<task type="auto">
  <name>Task 2: Record consent at onboarding (SAFE-02) + wire role-switch & sign-out into profile screens (AUTH-03/04)</name>
  <read_first>
    - lmc-app/app/legal/[doc].tsx (the terms/privacy/aup/code viewer — hook "accept" to recordConsent)
    - lmc-app/app/onboarding/quick-finish.tsx, lmc-app/app/onboarding/role.tsx (onboarding acceptance points)
    - lmc-app/app/(seeker)/profile.tsx, lmc-app/app/(scout)/profile.tsx (role switch + sign out)
    - lmc-app/app/(seeker)/payment-methods.tsx (read/write payment_methods placeholder)
    - .planning/phases/01-foundation-auth-persistence-event-log/01-RESEARCH.md (SAFE-02 section; Code Examples: recordConsent, role switch, sign-out)
  </read_first>
  <files>lmc-app/app/onboarding/quick-finish.tsx, lmc-app/app/onboarding/role.tsx, lmc-app/app/legal/[doc].tsx, lmc-app/app/(seeker)/profile.tsx, lmc-app/app/(scout)/profile.tsx, lmc-app/app/(seeker)/payment-methods.tsx, lmc-app/app/state/consent.test.ts</files>
  <action>
    1. SAFE-02 — record consent:
       - In `onboarding/quick-finish.tsx` (the final onboarding step where the user accepts terms/18+), call `api.recordConsent('age_18plus', <docVersion>)`, `recordConsent('terms', v)`, `recordConsent('privacy', v)`, `recordConsent('aup', v)` on completion. Use a single `DOC_VERSION` constant (e.g. '2026-06-20') and pass the user's market/country as `jurisdiction` when available.
       - In `legal/[doc].tsx`, when the user taps Accept on a doc, call `recordConsent(<doc>, DOC_VERSION, jurisdiction)`. Each call also emits `consent.accepted` to event_log (handled inside api.recordConsent).
    2. AUTH-03/04 — profile screens:
       - `(seeker)/profile.tsx` and `(scout)/profile.tsx`: wire the "Switch to Scout/Seeker" control to `auth.switchRole('scout'|'seeker')` (delegates to `api.setCurrentRole`, which logs `auth.role_switched`) and route to the other hub; wire "Sign out" to `auth.signOut()` then route to `/index`/`/welcome`.
    3. `(seeker)/payment-methods.tsx`: read the saved card via the rewired `payment-method` store / `api.getPaymentMethod()` and persist new cards via `api.savePaymentMethod(brand, last4)` (placeholder, brand+last4 only — NO Stripe).
    Keep existing layouts/copy; only swap handlers and data sources.
  </action>
  <verify>
    <automated>cd lmc-app && grep -q "recordConsent" app/onboarding/quick-finish.tsx && grep -q "recordConsent" app/legal/\[doc\].tsx && grep -Eq "age_18plus" app/onboarding/quick-finish.tsx && grep -Eq "switchRole|setCurrentRole" app/\(seeker\)/profile.tsx && grep -q "signOut" app/\(seeker\)/profile.tsx && grep -Eq "switchRole|setCurrentRole" app/\(scout\)/profile.tsx && npx tsc --noEmit && echo CONSENT_PROFILE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `onboarding/quick-finish.tsx` calls `recordConsent` including `age_18plus`
    - `legal/[doc].tsx` calls `recordConsent`
    - `(seeker)/profile.tsx` calls a role-switch (`switchRole`/`setCurrentRole`) AND `signOut`
    - `(scout)/profile.tsx` calls a role-switch
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Consent is recorded + logged at onboarding; role switch and sign-out work from both profile screens; payment-method placeholder persists.</done>
</task>

<task type="auto">
  <name>Task 3: EAS env config + .env.example (avoid the TestFlight null-env bug)</name>
  <read_first>
    - lmc-app/eas.json (build profiles — add env or document `eas env` usage)
    - .planning/phases/01-foundation-auth-persistence-event-log/01-RESEARCH.md (Pitfall 2: EAS env vars not set → null in TestFlight)
    - CLAUDE.md (never commit .env or secrets)
  </read_first>
  <files>lmc-app/.env.example, lmc-app/eas.json</files>
  <action>
    1. Create `lmc-app/.env.example` documenting the client env vars (NO real values):
       `EXPO_PUBLIC_SUPABASE_URL=` and `EXPO_PUBLIC_SUPABASE_ANON_KEY=` and `EXPO_PUBLIC_MAPBOX_TOKEN=` (existing). Add a comment: anon key is RLS-protected and safe to ship; NEVER add the service-role key here.
    2. Ensure `.env` is gitignored (verify `.gitignore` includes `.env`; add if missing — do NOT commit the real `.env`).
    3. In `lmc-app/eas.json`, for the build profiles (development + production), reference the two `EXPO_PUBLIC_SUPABASE_*` env vars (or add a clear comment block instructing the operator to run `eas env:create` for each, since EAS-managed env vars are the source of truth at build time). The acceptance is that the requirement is documented + the profile is wired, so the Mapbox-class null-env bug cannot recur silently.
    4. This task does NOT put real secret values anywhere in git — Troy registers the actual values in the EAS dashboard (see user_setup).
  </action>
  <verify>
    <automated>cd lmc-app && grep -q "EXPO_PUBLIC_SUPABASE_URL" .env.example && grep -q "EXPO_PUBLIC_SUPABASE_ANON_KEY" .env.example && grep -q "^\.env$\|/\.env$\|\.env" ../.gitignore .gitignore 2>/dev/null && grep -Eqi "supabase|EXPO_PUBLIC_SUPABASE|env" eas.json && echo ENV_OK</automated>
  </verify>
  <acceptance_criteria>
    - `lmc-app/.env.example` contains `EXPO_PUBLIC_SUPABASE_URL` and `EXPO_PUBLIC_SUPABASE_ANON_KEY` (no real values)
    - `.env` is gitignored (no real .env committed)
    - `eas.json` references the Supabase env vars or contains an explicit operator instruction block for `eas env:create`
  </acceptance_criteria>
  <done>Client env vars documented; EAS build profile accounts for the Supabase vars so TestFlight builds reach the backend.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: On-device verification — real auth + persistence survives restart</name>
  <what-built>
    Real Supabase auth behind the entry screens, 6 persistent stores, recorded consent, role-switch + sign-out, and EAS env config. Several success criteria (real OAuth, true app-restart persistence) can only be proven on a real device via an EAS dev build — not by tsc/Vitest.
  </what-built>
  <how-to-verify>
    On an EAS dev build on a real iPhone (Supabase + anon key set in EAS env):
    1. Sign in with Apple, then with Google, then with phone + OTP (real SMS). Each lands you in your hub.
    2. Save a place, set a payment-method card, create a recurring check. Force-quit the app, relaunch — you are still signed in and your saved place / card / recurring check are all still there (persistence survived restart, AUTH-02 + DATA-01/03).
    3. From the profile screen, switch Seeker↔Scout (lands in the other hub), then sign out (returns to the entry flow). Re-open — you are signed out.
    4. Complete onboarding once on a fresh account and confirm (in the Supabase dashboard) that `consents` has age_18plus/terms/privacy/aup rows and `event_log` has matching `consent.accepted` + `auth.*` events (SAFE-02 + DATA-04 wired end-to-end).
  </how-to-verify>
  <files>lmc-app/app (EAS dev build on device — no files written in this task)</files>
  <action>
    Trigger an EAS dev build with the Supabase env vars set in EAS (`eas build --profile development`), install it on a real iPhone, and walk the four verification steps below. This task writes no code — it is the on-device acceptance gate that the automated suite cannot cover (real OAuth, real SMS, true force-quit persistence). If any step fails, capture the failure and route back to the relevant task before resuming.
  </action>
  <verify>
    <automated>cd lmc-app && npm run test && npx tsc --noEmit && echo SUITE_GREEN</automated>
  </verify>
  <acceptance_criteria>
    - `cd lmc-app && npm run test` exits 0 and `npx tsc --noEmit` exits 0
    - Manual: all three sign-in methods work on device; session + saved data survive a force-quit/relaunch; role switch + sign-out work; consents + event_log rows visible in Supabase
  </acceptance_criteria>
  <done>All three sign-in methods work on device; session + saved place/card/recurring survive force-quit; role switch + sign-out work; consents + event_log rows confirmed in Supabase.</done>
  <resume-signal>Type "verified" if all four checks pass on device, or describe what failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| onboarding form → consents/event_log | Underage/AUP-bypass must be recorded server-side, not trusted client-side |
| build pipeline → runtime env | Missing EAS env = app silently can't reach backend (and could fall back to insecure defaults) |
| client store mutator → Supabase | Writes still cross RLS (set up in Plan 01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-12 | Repudiation/Compliance | consent acceptance | mitigate | `recordConsent` writes a versioned `consents` row + an immutable `consent.accepted` event with doc_version + timestamp; verified by `consent.test.ts` and the on-device dashboard check |
| T-01-13 | Information Disclosure | secrets in repo (.env) | mitigate | `.env` gitignored; `.env.example` has placeholders only; only anon key ships; service-role key never client-side. Verified by acceptance grep |
| T-01-14 | DoS/Integrity | EAS env null in TestFlight (Mapbox-class bug) | mitigate | Supabase URL/anon key registered as EAS env vars; eas.json wired; documented in user_setup so the build cannot silently ship without backend connectivity |
| T-01-15 | Tampering | cross-user write via rewired stores | mitigate | All store mutators go through `lib/api` → Supabase under the owner-only RLS from Plan 01 (a store cannot write another user's row) |
</threat_model>

<verification>
- `cd lmc-app && npm run test` exits 0; `npx tsc --noEmit` exits 0.
- 5 of 6 stores import `lib/api`; `scout-earnings.ts` no longer seeds 127.0; `location.ts` unchanged.
- `recordConsent` wired in onboarding + legal viewer; profile screens call role-switch + sign-out.
- `.env.example` documents Supabase vars; `.env` gitignored; `eas.json` accounts for them.
- On-device: real auth + restart-persistence + role switch + consent rows confirmed.
</verification>

<success_criteria>
- Saved places, recents, payment method, role, recurring checks persist in Supabase and survive restart (DATA-01/03).
- Stores keep their export surface — importing screens unchanged.
- 18+/Terms/Privacy/AUP recorded to consents + event_log at onboarding (SAFE-02).
- Seeker↔Scout switch + sign-out work from profile screens (AUTH-03/04).
- EAS env vars registered (no TestFlight null-env regression).
- location.ts remains client-side (locked decision honored).
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-auth-persistence-event-log/01-03-SUMMARY.md`.
</output>
