---
phase: 01-foundation-auth-persistence-event-log
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/config.toml
  - supabase/migrations/0001_event_log.sql
  - supabase/migrations/0002_profiles_roles_consents.sql
  - supabase/migrations/0003_markets_venues.sql
  - supabase/migrations/0004_core_entities.sql
  - supabase/migrations/0005_rls_policies.sql
  - supabase/migrations/0006_check_state_machine.sql
  - supabase/seed.sql
  - supabase/tests/event_log_immutable.test.sql
  - supabase/tests/rls_isolation.test.sql
  - supabase/tests/check_status_server_only.test.sql
  - lmc-app/vitest.config.ts
  - lmc-app/package.json
  - lmc-app/test/setup.ts
autonomous: false
requirements: [DATA-01, DATA-02, DATA-03, DATA-04, SAFE-02]
user_setup:
  - service: supabase
    why: "Postgres + Auth + RLS backend — the entire phase needs a live project to push migrations to"
    env_vars:
      - name: SUPABASE_ACCESS_TOKEN
        source: "Supabase Dashboard -> Account -> Access Tokens (for non-interactive `supabase db push`)"
      - name: EXPO_PUBLIC_SUPABASE_URL
        source: "Supabase Dashboard -> Project Settings -> API -> Project URL"
      - name: EXPO_PUBLIC_SUPABASE_ANON_KEY
        source: "Supabase Dashboard -> Project Settings -> API -> Project API keys -> anon public"
    dashboard_config:
      - task: "Create one Supabase project (dev/staging) and copy the project ref"
        location: "Supabase Dashboard -> New Project"

must_haves:
  truths:
    - "The event_log table exists and physically rejects UPDATE and DELETE (append-only)"
    - "Every table has Row-Level Security enabled (no table ships with RLS off)"
    - "A client/anon role cannot write checks.status or checks.scout_id"
    - "A user cannot read another user's rows (saved_places, recents, profiles, etc.)"
    - "18+/consent/AUP acceptance has a durable, versioned home (consents table)"
    - "Markets carry country + currency + locale as data (no hard-coded USD/US-only)"
    - "Running the test suite (Vitest + Supabase-local SQL tests) exits 0"
  artifacts:
    - path: "supabase/migrations/0001_event_log.sql"
      provides: "Immutable append-only event log, created before any entity table"
      contains: "create trigger event_log_no_update"
    - path: "supabase/migrations/0002_profiles_roles_consents.sql"
      provides: "profiles (dual-role + current_role) + consents (SAFE-02)"
      contains: "create table public.consents"
    - path: "supabase/migrations/0003_markets_venues.sql"
      provides: "Market-aware catalog (country, currency, locale)"
      contains: "currency"
    - path: "supabase/migrations/0004_core_entities.sql"
      provides: "checks + check_status enum, saved_places, recents, recurring_checks, payment_methods, ratings"
      contains: "create type check_status as enum"
    - path: "supabase/migrations/0005_rls_policies.sql"
      provides: "RLS enabled + owner policies on every table"
      contains: "enable row level security"
    - path: "supabase/migrations/0006_check_state_machine.sql"
      provides: "SECURITY DEFINER transition fn — the only writer of checks.status"
      contains: "security definer"
    - path: "lmc-app/vitest.config.ts"
      provides: "Vitest test runner (Wave 0 harness)"
      contains: "defineConfig"
  key_links:
    - from: "supabase/migrations/0001_event_log.sql"
      to: "event_log_immutable()"
      via: "BEFORE UPDATE/DELETE trigger"
      pattern: "before update on (public\\.)?event_log"
    - from: "supabase/migrations/0006_check_state_machine.sql"
      to: "event_log"
      via: "transition fn inserts check.status_changed"
      pattern: "insert into (public\\.)?event_log"
    - from: "supabase/tests/check_status_server_only.test.sql"
      to: "checks.status"
      via: "negative RLS test asserting client write fails"
      pattern: "status"
---

<objective>
Stand up the entire Supabase backend for LMC: the immutable event log FIRST (migration 0001, per the CTO mandate), then the dual-role profile + consent schema, the market-aware catalog, the core entities with the checks state machine, and RLS on every table. Push the schema to a live Supabase project and prove the security-critical invariants with SQL tests. Also stand up the Wave-0 test harness (Vitest + Supabase-local) so every later plan and phase adds tests cheaply.

Purpose: This is the source-of-truth layer. The client (Plan 02/03) is a thin reader over it. If the event log isn't immutable, RLS isn't on every table, or the client can write checks.status, the whole phase's trust promise is hollow.
Output: A version-controlled `supabase/` directory, a pushed live schema, passing SQL/RLS tests, and a working Vitest harness.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-auth-persistence-event-log/01-RESEARCH.md
@.planning/phases/01-foundation-auth-persistence-event-log/01-VALIDATION.md
@lmc-app/app/data/markets.ts

<interfaces>
<!-- Existing store types the schema must be able to back. Source: lmc-app/app/state/*.ts. -->
<!-- Column names below MUST match these shapes so Plan 03 can rewire stores with no screen churn. -->

From state/saved.ts — SavedPlace:
  id: string; name: string; address?: string; category?: string;
  coord: [number, number] (Mapbox [lon, lat]); marketId: string; savedAt: string (ISO)

From state/recents.ts — RecentCheck:
  name: string; city: string; ts: number (epoch ms)

From state/payment-method.ts — SavedCard:
  brand: 'Visa'|'Mastercard'|'Amex'|'ApplePay'; last4: string; savedAt: string (ISO)

From state/recurring.ts — RecurringCheck:
  id: string; venueName: string; address?: string;
  freq: 'daily'|'weekly'|'monthly'; time: string ("08:00"); marketId: string;
  coord: [number, number]; createdAt: string; active: boolean

From state/intended-role.ts — IntendedRole: 'seeker'|'scout'|'both'|null
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Stand up the Wave-0 test harness (Vitest + Supabase local) and project scaffolding</name>
  <read_first>
    - lmc-app/package.json (current scripts: start/ios/android/web only — no test/lint/typecheck)
    - .planning/phases/01-foundation-auth-persistence-event-log/01-VALIDATION.md (Test Infrastructure + Wave 0 Requirements)
    - .planning/phases/01-foundation-auth-persistence-event-log/01-RESEARCH.md (Migrations Workflow section)
  </read_first>
  <files>lmc-app/vitest.config.ts, lmc-app/test/setup.ts, lmc-app/package.json, supabase/config.toml</files>
  <action>
    Initialize the backend + test harness. Do NOT write business SQL yet (later tasks do).

    1. From `projects/let-me-check/`, run `supabase init` to create `supabase/` + `supabase/config.toml`. If the Supabase CLI is not installed, install it (`brew install supabase/tap/supabase` on macOS) and re-run. Confirm `supabase/config.toml` exists.
    2. In `lmc-app/`, install dev deps for the runner:
       `npm install -D vitest @vitest/coverage-v8 @supabase/supabase-js`
       (supabase-js is also a runtime dep added in Plan 02; installing now is fine.)
    3. Create `lmc-app/vitest.config.ts` exporting `defineConfig({ test: { environment: 'node', setupFiles: ['./test/setup.ts'], include: ['**/*.test.ts'], exclude: ['**/node_modules/**', '**/ios/**'] } })`.
    4. Create `lmc-app/test/setup.ts` reading `EXPO_PUBLIC_SUPABASE_URL` and `EXPO_PUBLIC_SUPABASE_ANON_KEY` from env, exporting a helper `makeClient(url, key)` used by integration tests (no top-level side effects).
    5. Add scripts to `lmc-app/package.json`:
       `"test": "vitest run"`, `"test:watch": "vitest"`, `"typecheck": "tsc --noEmit"`, and `"test:db": "supabase test db"`.
    6. Create directory `supabase/tests/` (test files are written in Task 5).
    Do NOT modify any existing screen file in this task.
  </action>
  <verify>
    <automated>cd lmc-app && npx vitest run --reporter=basic; test -f ../supabase/config.toml && grep -q '"test"' package.json && grep -q 'defineConfig' vitest.config.ts && echo HARNESS_OK</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/config.toml` exists (file-read)
    - `lmc-app/vitest.config.ts` contains `defineConfig`
    - `lmc-app/test/setup.ts` contains `makeClient`
    - `lmc-app/package.json` scripts block contains `"test"` and `"test:db"` and `"typecheck"`
    - `cd lmc-app && npx vitest run` exits 0 (no tests yet is acceptable — runner works)
  </acceptance_criteria>
  <done>Vitest runs, supabase/ scaffolded, npm scripts present. No screen files touched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Event log FIRST (migration 0001) — append-only + immutability trigger</name>
  <read_first>
    - .planning/phases/01-foundation-auth-persistence-event-log/01-RESEARCH.md ("Event Log Design (DATA-04)" section — has the exact SQL)
    - supabase/config.toml (confirm supabase initialized)
  </read_first>
  <files>supabase/migrations/0001_event_log.sql</files>
  <behavior>
    - INSERT into event_log succeeds for an authenticated actor.
    - UPDATE on any event_log row raises an exception ('event_log is append-only').
    - DELETE on any event_log row raises an exception.
    - event_log is created BEFORE any entity table (it is migration 0001).
  </behavior>
  <action>
    Create `supabase/migrations/0001_event_log.sql` as the FIRST migration (CTO mandate: event log before entity schemas). Enable required extensions then the table:

    - `create extension if not exists postgis;` (geo column; venue geofence columns land later but the type is needed now)
    - Table `public.event_log` columns exactly:
      `id bigint generated always as identity primary key`,
      `created_at timestamptz not null default now()`,
      `actor_id uuid references auth.users(id)`,
      `event_type text not null`,
      `subject_type text`,
      `subject_id uuid`,
      `geo geography(point, 4326)`,
      `context jsonb not null default '{}'::jsonb`.
    - Indexes: btree on `(created_at)`, on `(event_type, created_at)`, on `(subject_type, subject_id)`, and a GIN index on `(context)`.
    - Immutability function `public.event_log_immutable()` (language plpgsql) that does `raise exception 'event_log is append-only: % not allowed', tg_op;`.
    - Trigger `event_log_no_update before update on public.event_log for each row execute function public.event_log_immutable();`
    - Trigger `event_log_no_delete before delete on public.event_log for each row execute function public.event_log_immutable();`
    - A helper `public.log_event(p_event_type text, p_subject_type text, p_subject_id uuid, p_context jsonb)` SECURITY DEFINER function that inserts a row with `actor_id = auth.uid()`. This is the single canonical event writer used by later transition functions and the client wrapper.
    Do NOT add RLS here (RLS is migration 0005) — but DO add a comment noting event_log RLS is insert-only + read-restricted, set in 0005.
  </action>
  <verify>
    <automated>cd supabase && grep -q "before update on public.event_log" migrations/0001_event_log.sql && grep -q "before delete on public.event_log" migrations/0001_event_log.sql && grep -q "generated always as identity" migrations/0001_event_log.sql && grep -q "geography(point, 4326)" migrations/0001_event_log.sql && grep -q "create or replace function public.log_event" migrations/0001_event_log.sql && echo EVENTLOG_OK</automated>
  </verify>
  <acceptance_criteria>
    - File is named `0001_event_log.sql` (lowest-numbered migration — sequenced FIRST)
    - Contains `before update on public.event_log` AND `before delete on public.event_log`
    - Contains `raise exception` inside `event_log_immutable`
    - Contains `geography(point, 4326)` and `context jsonb`
    - Contains `create or replace function public.log_event` with `security definer`
  </acceptance_criteria>
  <done>0001 creates an append-only, trigger-protected event_log + log_event helper, before any entity table.</done>
</task>

<task type="auto">
  <name>Task 2: Profiles + dual-role + consents (SAFE-02) — migration 0002</name>
  <read_first>
    - .planning/phases/01-foundation-auth-persistence-event-log/01-RESEARCH.md ("SAFE-02" section + Runtime State Inventory row for intended-role)
    - lmc-app/app/state/intended-role.ts (role values to preserve: seeker/scout/both)
  </read_first>
  <files>supabase/migrations/0002_profiles_roles_consents.sql</files>
  <action>
    Create `supabase/migrations/0002_profiles_roles_consents.sql`.

    - Table `public.profiles`:
      `id uuid primary key references auth.users(id) on delete cascade`,
      `display_name text`,
      `phone text`,
      `is_seeker boolean not null default false`,
      `is_scout boolean not null default false`,
      `current_role text not null default 'seeker' check (current_role in ('seeker','scout'))`,
      `created_at timestamptz not null default now()`,
      `updated_at timestamptz not null default now()`.
      (Dual-role model: a user can have both is_seeker and is_scout true; current_role is the active hub — AUTH-03.)
    - A trigger to auto-insert a `profiles` row when a new `auth.users` row is created: function `public.handle_new_user()` SECURITY DEFINER inserting `id` (default current_role 'seeker'), and trigger `on auth.users after insert`.
    - Table `public.consents` (SAFE-02 — versioned, auditable):
      `id uuid primary key default gen_random_uuid()`,
      `user_id uuid not null references auth.users(id) on delete cascade`,
      `consent_type text not null check (consent_type in ('age_18plus','terms','privacy','aup','scout_code'))`,
      `doc_version text not null`,
      `accepted_at timestamptz not null default now()`,
      `jurisdiction text` (market/country at acceptance — international-ready).
    Do NOT add RLS here (0005). No USD/US-only assumptions.
  </action>
  <verify>
    <automated>cd supabase && grep -q "create table public.profiles" migrations/0002_profiles_roles_consents.sql && grep -q "is_seeker boolean" migrations/0002_profiles_roles_consents.sql && grep -q "is_scout boolean" migrations/0002_profiles_roles_consents.sql && grep -q "current_role" migrations/0002_profiles_roles_consents.sql && grep -q "create table public.consents" migrations/0002_profiles_roles_consents.sql && grep -q "age_18plus" migrations/0002_profiles_roles_consents.sql && echo PROFILES_OK</automated>
  </verify>
  <acceptance_criteria>
    - Contains `create table public.profiles` with `is_seeker`, `is_scout`, `current_role`
    - Contains `handle_new_user` trigger on `auth.users`
    - Contains `create table public.consents` with `consent_type` check including `age_18plus`, `terms`, `privacy`, `aup`, `scout_code`
    - Contains `doc_version` and `jurisdiction` columns on consents
  </acceptance_criteria>
  <done>Dual-role profile auto-created per user; versioned consents table covers SAFE-02 and future scout_code.</done>
</task>

<task type="auto">
  <name>Task 3: Market-aware catalog + core entities + check state machine (migrations 0003, 0004, 0006)</name>
  <read_first>
    - lmc-app/app/data/markets.ts (COUNTRIES, MARKETS, VENUES shapes — port to schema + seed; coords are [lon, lat])
    - .planning/phases/01-foundation-auth-persistence-event-log/01-RESEARCH.md (Pattern 4: status column as state machine; RLS table list)
    - supabase/migrations/0001_event_log.sql (log_event helper signature, written in Task 1)
  </read_first>
  <files>supabase/migrations/0003_markets_venues.sql, supabase/migrations/0004_core_entities.sql, supabase/migrations/0006_check_state_machine.sql</files>
  <action>
    Create three migrations.

    `supabase/migrations/0003_markets_venues.sql` (market-aware, MKT-ready — data, not code):
    - `public.markets`: `id text primary key`, `name text not null`, `country text not null`, `currency text not null default 'USD'`, `locale text not null default 'en-US'`, `is_live boolean not null default false`, `center geography(point,4326)`.
    - `public.venues`: `id text primary key`, `market_id text not null references public.markets(id)`, `name text not null`, `category text`, `coord geography(point,4326)`, `is_partner boolean not null default false`.
    (Do NOT hard-code USD/US — currency + locale + country are columns.)

    `supabase/migrations/0004_core_entities.sql`:
    - `create type check_status as enum ('requested','authorized','dispatching','assigned','filming','uploaded','processing','delivered','rated','cancelled','expired');`
    - `public.checks`: `id uuid pk default gen_random_uuid()`, `seeker_id uuid not null references auth.users(id)`, `scout_id uuid references auth.users(id)`, `venue_id text references public.venues(id)`, `market_id text references public.markets(id)`, `tier text not null default 'standard' check (tier in ('standard','priority'))`, `status check_status not null default 'requested'`, `currency text not null default 'USD'`, `created_at timestamptz not null default now()`, `updated_at timestamptz not null default now()`.
    - `public.saved_places`: `id uuid pk default gen_random_uuid()`, `user_id uuid not null references auth.users(id)`, `place_key text not null` (the client `SavedPlace.id`), `name text not null`, `address text`, `category text`, `coord geography(point,4326)`, `market_id text`, `saved_at timestamptz not null default now()`, `unique (user_id, place_key)`.
    - `public.recents`: `id uuid pk default gen_random_uuid()`, `user_id uuid not null references auth.users(id)`, `name text not null`, `city text`, `created_at timestamptz not null default now()`.
    - `public.recurring_checks`: `id uuid pk default gen_random_uuid()`, `user_id uuid not null references auth.users(id)`, `venue_name text not null`, `address text`, `freq text not null check (freq in ('daily','weekly','monthly'))`, `time text not null`, `market_id text`, `coord geography(point,4326)`, `active boolean not null default true`, `created_at timestamptz not null default now()`.
    - `public.payment_methods` (placeholder — NO Stripe in P1): `id uuid pk default gen_random_uuid()`, `user_id uuid not null references auth.users(id)`, `brand text not null check (brand in ('Visa','Mastercard','Amex','ApplePay'))`, `last4 text not null`, `saved_at timestamptz not null default now()`. (Stripe customer/token columns land Phase 4.)
    - `public.ratings`: `id uuid pk default gen_random_uuid()`, `check_id uuid not null references public.checks(id)`, `seeker_id uuid not null references auth.users(id)`, `stars int not null check (stars between 1 and 5)`, `created_at timestamptz not null default now()`.

    `supabase/migrations/0006_check_state_machine.sql` (DATA-02 — the ONLY writer of status):
    - `public.transition_check(p_check_id uuid, p_to check_status, p_context jsonb default '{}')` as `security definer` language plpgsql:
      reads current status, updates `checks.status = p_to` and `updated_at = now()`, then calls `perform public.log_event('check.status_changed','check',p_check_id, jsonb_build_object('from', <old>, 'to', p_to) || p_context);`.
      (Phase 1 needs only that this server-only writer exists and logs every transition; Phase 2 exercises the full lifecycle.)
    - Add a trigger or column-grant note in a comment that client roles cannot UPDATE checks.status directly — the actual lockdown is the RLS policy in 0005 (do not grant update on status/scout_id to authenticated).
  </action>
  <verify>
    <automated>cd supabase && grep -q "create type check_status as enum" migrations/0004_core_entities.sql && grep -q "create table public.checks" migrations/0004_core_entities.sql && grep -q "create table public.payment_methods" migrations/0004_core_entities.sql && grep -qi "currency" migrations/0003_markets_venues.sql && grep -qi "locale" migrations/0003_markets_venues.sql && grep -qi "security definer" migrations/0006_check_state_machine.sql && grep -q "transition_check" migrations/0006_check_state_machine.sql && echo ENTITIES_OK</automated>
  </verify>
  <acceptance_criteria>
    - `0003_markets_venues.sql` contains `currency` and `locale` columns
    - `0004_core_entities.sql` contains `create type check_status as enum` and `create table public.checks` with `status check_status` + `scout_id`
    - `0004` contains `payment_methods`, `saved_places`, `recents`, `recurring_checks`, `ratings`
    - `0006_check_state_machine.sql` contains `transition_check` with `security definer` and calls `log_event`
  </acceptance_criteria>
  <done>Catalog is market-aware; all core entities persist; status is written only by a SECURITY DEFINER server function that logs every transition.</done>
</task>

<task type="auto">
  <name>Task 4: RLS on every table (migration 0005) + seed</name>
  <read_first>
    - .planning/phases/01-foundation-auth-persistence-event-log/01-RESEARCH.md ("RLS Policy Approach" table — exact per-table read/write rules)
    - supabase/migrations/0004_core_entities.sql (table + column names to write policies against)
    - lmc-app/app/data/markets.ts (data to port into seed.sql)
  </read_first>
  <files>supabase/migrations/0005_rls_policies.sql, supabase/seed.sql</files>
  <action>
    Create `supabase/migrations/0005_rls_policies.sql` enabling RLS on EVERY table and adding policies:

    - For each of `profiles, consents, saved_places, recents, recurring_checks, payment_methods, ratings, checks, event_log, markets, venues`: `alter table public.<t> enable row level security;`
    - `profiles`: SELECT/UPDATE where `auth.uid() = id`. Do NOT allow the client to update `is_seeker`/`is_scout` via a blanket policy that would let privilege escalation — restrict the updatable surface (current_role, display_name, phone). (A column-scoped grant or a restrictive policy comment is acceptable; at minimum `current_role` updatable by owner.)
    - `saved_places, recents, recurring_checks, payment_methods`: owner-only SELECT/INSERT/DELETE where `auth.uid() = user_id`.
    - `consents`: owner SELECT + INSERT where `auth.uid() = user_id`; NO update/delete policy (insert-only).
    - `ratings`: owner (seeker_id) SELECT/INSERT.
    - `checks`: SELECT where `auth.uid() = seeker_id` (later: or assigned scout). INSERT allowed where `auth.uid() = seeker_id AND status = 'requested'`. **NO UPDATE policy for `authenticated`** — meaning the client physically cannot write `status`/`scout_id` (DATA-02). Transitions go only through `transition_check` (SECURITY DEFINER).
    - `event_log`: INSERT policy `with check (auth.uid() = actor_id)` for authenticated; NO select/update/delete policy for authenticated (read-restricted; update/delete also blocked by the 0001 trigger).
    - `markets`, `venues`: public SELECT (`using (true)`); no client write policy (catalog is service/admin-managed).

    Then create `supabase/seed.sql`: port the `COUNTRIES`/`MARKETS`/`VENUES` data from `lmc-app/app/data/markets.ts` into `insert into public.markets ...` and `insert into public.venues ...` (use the same ids; convert `[lon,lat]` coords to `ST_SetSRID(ST_MakePoint(lon,lat),4326)`). Mark live markets with `is_live = true` matching the source data.
  </action>
  <verify>
    <automated>cd supabase && grep -c "enable row level security" migrations/0005_rls_policies.sql | awk '{ if ($1 >= 11) print "RLS_COUNT_OK"; else print "RLS_COUNT_FAIL:"$1 }' && grep -q "auth.uid() = seeker_id" migrations/0005_rls_policies.sql && grep -q "auth.uid() = actor_id" migrations/0005_rls_policies.sql && test -f seed.sql && grep -qi "insert into public.markets" seed.sql && echo RLS_SEED_OK</automated>
  </verify>
  <acceptance_criteria>
    - `0005_rls_policies.sql` contains `enable row level security` at least 11 times (one per table)
    - Contains an owner policy keyed on `auth.uid() = seeker_id` for checks and `auth.uid() = actor_id` for event_log
    - Contains NO `for update` policy granting `authenticated` write to `checks.status` (grep for an update policy on checks returns none, OR a comment documents the deliberate omission)
    - `seed.sql` exists and contains `insert into public.markets`
  </acceptance_criteria>
  <done>RLS is on for all 11 tables; checks.status/scout_id are unreachable by clients; catalog seeded from markets.ts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: SQL/RLS security tests (the negative tests that prove the invariants)</name>
  <read_first>
    - .planning/phases/01-foundation-auth-persistence-event-log/01-RESEARCH.md (Validation Architecture — Phase Requirements -> Test Map; Pitfall 5)
    - supabase/migrations/0005_rls_policies.sql (policies under test)
    - supabase/migrations/0001_event_log.sql (immutability trigger under test)
  </read_first>
  <files>supabase/tests/event_log_immutable.test.sql, supabase/tests/check_status_server_only.test.sql, supabase/tests/rls_isolation.test.sql</files>
  <behavior>
    - UPDATE on event_log → fails with the immutability exception.
    - DELETE on event_log → fails.
    - As Seeker A, attempt to UPDATE a check's status directly → fails / 0 rows affected (no policy).
    - As Seeker A, SELECT Seeker B's saved_places → returns 0 rows (cross-user isolation).
    - transition_check(...) called server-side DOES move status and writes a check.status_changed event.
  </behavior>
  <action>
    Create pgTAP/SQL tests under `supabase/tests/` runnable via `supabase test db` against the local stack:

    - `supabase/tests/event_log_immutable.test.sql`: insert one event row; assert `UPDATE event_log ...` throws; assert `DELETE FROM event_log ...` throws. Use pgTAP `throws_ok`.
    - `supabase/tests/check_status_server_only.test.sql`: create a check as a seeker role (set `request.jwt.claims`/`role` to simulate an authenticated user); assert a direct `UPDATE public.checks SET status='delivered'` either throws or affects 0 rows under RLS; then assert calling `select public.transition_check(<id>,'authorized')` succeeds and a `check.status_changed` row appears in event_log.
    - `supabase/tests/rls_isolation.test.sql`: insert saved_places for user A and user B; set the session to A's uid; assert `select count(*) from saved_places` returns only A's rows (`is(...)`).
    Make these runnable with `supabase test db` (pgTAP). Wire `npm run test:db` to invoke it (already added in Task 0).
  </action>
  <verify>
    <automated>cd supabase && test -f tests/event_log_immutable.test.sql && test -f tests/check_status_server_only.test.sql && test -f tests/rls_isolation.test.sql && grep -qi "throws_ok" tests/event_log_immutable.test.sql && grep -qi "transition_check" tests/check_status_server_only.test.sql && echo SQLTESTS_OK</automated>
  </verify>
  <acceptance_criteria>
    - All three files exist under `supabase/tests/`
    - `event_log_immutable.test.sql` contains `throws_ok` for both update and delete
    - `check_status_server_only.test.sql` references `transition_check` and asserts the direct UPDATE path is blocked
    - `rls_isolation.test.sql` asserts a user reads only their own rows
  </acceptance_criteria>
  <done>The three security invariants (immutable log, server-only status, cross-user isolation) have automated negative tests.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 6: [BLOCKING] Push schema to live Supabase + run the SQL tests</name>
  <read_first>
    - .planning/phases/01-foundation-auth-persistence-event-log/01-RESEARCH.md (Migrations Workflow — push + gen types + local test)
    - supabase/config.toml
  </read_first>
  <files>supabase/migrations/*, lmc-app/app/lib/database.types.ts (generated)</files>
  <what-built>
    Six migrations (event log first), seed, and three SQL security tests. Nothing is real until the schema is pushed to a live Postgres — a build/type check passes WITHOUT the push, so this step is mandatory to avoid a false-positive verification.
  </what-built>
  <action>
    This task requires Troy's Supabase project + access token (see user_setup). Run, in order:

    1. Local hermetic run first (proves migrations apply clean + tests pass):
       `cd projects/let-me-check && supabase start && supabase db reset` (applies all migrations + seed.sql) then `supabase test db` (runs the pgTAP tests). All tests must pass.
    2. Link + push to the live project:
       `supabase link --project-ref <PROJECT_REF>` then `SUPABASE_ACCESS_TOKEN=<token> supabase db push --linked` (non-interactive).
    3. Generate types for the client (consumed by Plan 02):
       `supabase gen types typescript --linked > lmc-app/app/lib/database.types.ts` (creates lmc-app/app/lib/ if needed).
    If `supabase start` needs Docker and Docker is unavailable on this machine, fall back to running `db push` against a cloud staging project and `supabase test db` against it; flag this to Troy.
  </action>
  <how-to-verify>
    1. `supabase test db` (local or staging) reports all pgTAP tests passing.
    2. `supabase migration list --linked` shows 0001–0006 applied on the remote.
    3. `lmc-app/app/lib/database.types.ts` exists and contains a `checks` and `event_log` type.
  </how-to-verify>
  <verify>
    <automated>test -f lmc-app/app/lib/database.types.ts && grep -qi "event_log" lmc-app/app/lib/database.types.ts && echo TYPES_OK || echo "TYPES_MISSING — run the push/gen-types step"</automated>
  </verify>
  <acceptance_criteria>
    - `supabase test db` exits 0 (all pgTAP security tests pass)
    - Remote shows migrations 0001–0006 applied (`supabase migration list --linked`)
    - `lmc-app/app/lib/database.types.ts` exists and contains `event_log` and `checks`
  </acceptance_criteria>
  <done>Migrations 0001-0006 applied to the live project; pgTAP security tests pass; database.types.ts generated for the client.</done>
  <resume-signal>Type "pushed" once migrations are applied to the live project and database.types.ts is generated, or paste any error.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (anon/authenticated JWT) → Postgres | Every client read/write crosses RLS; the anon key is public, so RLS is the only access control |
| client → checks.status | Untrusted client must never advance the workflow itself |
| any role → event_log | The audit trail must be tamper-evident even against an authenticated user |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Elevation/Tampering | checks.status / checks.scout_id | mitigate | NO update policy for `authenticated` on checks; only `transition_check` (SECURITY DEFINER) writes status. Verified by `tests/check_status_server_only.test.sql` |
| T-01-02 | Information Disclosure | saved_places/recents/profiles/checks | mitigate | Owner-only RLS keyed on `auth.uid() = user_id`/`seeker_id` on every table. Verified by `tests/rls_isolation.test.sql` |
| T-01-03 | Repudiation/Tampering | event_log | mitigate | BEFORE UPDATE/DELETE trigger raises exception (0001); read-restricted RLS. Verified by `tests/event_log_immutable.test.sql` |
| T-01-04 | Information Disclosure / Elevation | service-role key | mitigate | Service role key NEVER in any client file or migration; only anon key is client-side (set in Plan 02). Migrations contain no secrets |
| T-01-05 | Compliance/Repudiation | consents (underage/AUP bypass) | mitigate | Versioned `consents` row (doc_version + accepted_at + jurisdiction), insert-only RLS; emitted to event_log by Plan 03 |
| T-01-06 | Tampering | RLS forgotten on a new table | mitigate | 0005 enables RLS on all 11 tables; acceptance asserts `enable row level security` count >= 11 |
</threat_model>

<verification>
- `cd lmc-app && npm run test` exits 0 (Vitest harness works).
- `cd projects/let-me-check && supabase test db` — all three pgTAP security tests pass.
- `supabase db reset` applies migrations 0001–0006 + seed cleanly (no SQL errors).
- 0001 is the lowest-numbered migration (event log first).
- `lmc-app/app/lib/database.types.ts` generated and contains `event_log` + `checks`.
</verification>

<success_criteria>
- Immutable, append-only event_log exists and is migration 0001.
- RLS enabled on every one of the 11 tables.
- Client cannot write `checks.status`/`scout_id`; `transition_check` is the sole server-side writer and logs every transition.
- No user can read another user's rows (proven by SQL test).
- Versioned consents table covers SAFE-02 (acceptance recorded in Plan 03).
- Markets carry country/currency/locale as data (no hard-coded USD/US-only).
- Schema pushed to live Supabase; types generated for the client.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-auth-persistence-event-log/01-01-SUMMARY.md`.
</output>
