---
phase: 06-privacy-anti-fraud-hardening-on-device-face-plate-blur-befor
plan: 04
type: execute
wave: 4
depends_on: [01, 02, 03]
files_modified:
  - lmc-app/app/lib/database.types.ts
autonomous: true
requirements: [SCH-01, BLUR-01, BLUR-02, BLUR-03, BLUR-04, BLUR-05, FRAUD-01, FRAUD-02]
category: A
must_haves:
  truths:
    - "0014 migration is applied live (clips + market_config columns + blur_review enum exist in the live DB)"
    - "live pgTAP (supabase test db) passes for 0014 (SCH-01)"
    - "face-blur-check + fraud-eval Edge Functions are deployed; mux-webhook + mux-upload-url redeployed with the Phase-6 changes"
    - "blur_enabled stays FALSE in every live market_config row (launch posture D-07 — gate dormant)"
    - "database.types.ts regenerated so blur_status/fraud_* + market_config flags are typed (removes Plan-02/03 as-any casts where practical)"
  artifacts:
    - path: "lmc-app/app/lib/database.types.ts"
      provides: "regenerated types including blur/fraud columns"
      contains: "blur_status"
  key_links:
    - from: "supabase db push"
      to: "live database"
      via: "0014 applied"
      pattern: "0014_privacy_fraud_signals"
    - from: "supabase functions deploy"
      to: "live edge runtime"
      via: "face-blur-check + fraud-eval + mux-webhook + mux-upload-url"
      pattern: "face-blur-check"
---

<objective>
[BLOCKING] Take Phase-6 Category-A live: push 0014, run live pgTAP, deploy the two new Edge Functions + redeploy the two changed ones, confirm the launch posture (blur_enabled=false), and regenerate database.types.ts. The Google Vision key is already set (Phase 5) — reuse it; NO new key checkpoint. The orchestrator (Guy) runs every command here itself; this plan is autonomous.

Purpose: every Category-A behaviour is now offline-tested (Plans 01-03); this plan makes it real on the live Supabase project (ref cawqasszfbzvbtunamda) without flipping any feature on (blur stays dormant; fraud stays flag-only).
Output: applied migration, deployed functions, regenerated types, confirmed dormant launch posture.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-privacy-anti-fraud-hardening-on-device-face-plate-blur-befor/06-RESEARCH.md

<read_first>
- .planning/phases/05-*/05-06-PLAN.md (if present) — the Phase-5 [BLOCKING] live-deploy pattern: db push, deploy --no-verify-jwt for webhooks vs verify_jwt for user/service functions, types regen via supabase gen types. Mirror it exactly.
- STATE.md decisions: webhooks deployed --no-verify-jwt; verify-clip/signage-check/face-blur-check/fraud-eval are server-to-server service-role invokes (no --no-verify-jwt needed — service-role invokes bypass the JWT gate). mux-webhook is a real webhook (--no-verify-jwt, sig-verified in code). mux-upload-url is a user function (keeps Supabase JWT).
- supabase/config.toml / project ref — live project is cawqasszfbzvbtunamda (Phase 5 already linked).
</read_first>
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] db push 0014 + live pgTAP (SCH-01)</name>
  <files>(no source file — live database)</files>
  <action>
Run, from the repo root (orchestrator executes; this is the BLOCKING live step):
1. `supabase db push` (applies 0014_privacy_fraud_signals.sql to the live project). If push reports the migration is already applied, that is fine (idempotent).
2. `supabase test db` (runs ALL pgTAP including supabase/tests/0014_privacy_fraud_signals.test.sql). Confirm 0014 asserts pass (blur_status default 'pending', blur_enabled default false, fraud_strictness 'flag', blur_review enum present).
If db push fails on the enum-add ordering, re-read Pitfall 4 — the enum ALTER must precede the is_valid_check_transition replace WITHIN 0014; fix the migration (Plan 01 file) and re-push.
  </action>
  <verify>
    <automated>supabase migration list 2>/dev/null | grep -q "0014" && supabase test db 2>&1 | grep -qiE "ok|pass" && echo MIGRATION-LIVE || echo "CHECK: confirm 0014 applied + pgTAP green in command output"</automated>
  </verify>
  <done>0014 applied live; `supabase test db` green for the 0014 pgTAP (SCH-01).</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] deploy Edge Functions (2 new + 2 changed)</name>
  <files>(no source file — live edge runtime)</files>
  <action>
Deploy (orchestrator executes):
1. `supabase functions deploy face-blur-check` (service-to-service; no --no-verify-jwt needed — invoked by mux-webhook service role).
2. `supabase functions deploy fraud-eval` (same; service-to-service).
3. `supabase functions deploy mux-webhook --no-verify-jwt` (real webhook; sig-verified in code — MUST keep --no-verify-jwt, matching Phase 3/5).
4. `supabase functions deploy mux-upload-url` (user function; KEEP Supabase JWT — do NOT pass --no-verify-jwt).
Confirm GOOGLE_VISION_API_KEY is already set: `supabase secrets list | grep -i GOOGLE_VISION_API_KEY`. If missing (it should be present from Phase 5), that is the ONLY thing requiring Troy in the morning — note it in the SUMMARY; do NOT block (blur_enabled=false means face-blur-check no-ops regardless).
  </action>
  <verify>
    <automated>supabase functions list 2>/dev/null | grep -q "face-blur-check" && supabase functions list 2>/dev/null | grep -q "fraud-eval" && echo FUNCTIONS-DEPLOYED || echo "CHECK: confirm 4 functions deployed in command output"</automated>
  </verify>
  <done>face-blur-check + fraud-eval deployed; mux-webhook redeployed --no-verify-jwt; mux-upload-url redeployed with JWT; Vision key presence confirmed (or noted for AM).</done>
</task>

<task type="auto">
  <name>Task 3: confirm dormant launch posture + regenerate types</name>
  <files>lmc-app/app/lib/database.types.ts</files>
  <action>
1. Confirm launch posture in the live DB with an explicit assertion: `supabase db execute --query "select count(*) from public.market_config where blur_enabled = true;"` MUST return 0 (and confirm fraud_strictness is 'flag'/'off' for all rows — NOT 'hold'/'reject'). If the count is non-zero, force it dormant: `supabase db execute --query "update public.market_config set blur_enabled = false;"` then re-assert 0 (the gate MUST ship dormant — D-07; on-device blur is unverified overnight, Plan 05).
2. Regenerate types: `supabase gen types typescript --project-id cawqasszfbzvbtunamda > lmc-app/app/lib/database.types.ts` (use the same command Phase 5 used; check STATE.md / package.json for the exact invocation). Confirm blur_status/fraud_signals/fraud_flag/fraud_score + market_config.blur_enabled/fraud_strictness now appear.
3. Where Plan 02/03 used `as any` casts solely because the columns weren't typed, leave them if removal risks breakage, but prefer removing the now-unnecessary casts. Run `cd lmc-app && npx tsc --noEmit` to confirm the regenerated types don't break the client.
  </action>
  <verify>
    <automated>test "$(supabase db execute --query "select count(*) from public.market_config where blur_enabled = true;" 2>/dev/null | grep -oE '[0-9]+' | tail -1)" = "0" && echo DORMANT-OK || echo "BLOCKER: a market has blur_enabled=true — force false (D-07)"; grep -q "blur_status" lmc-app/app/lib/database.types.ts && grep -q "fraud_strictness" lmc-app/app/lib/database.types.ts && cd lmc-app && npx tsc --noEmit && echo TYPES-REGEN-OK</automated>
  </verify>
  <done>blur_enabled=false confirmed across live market_config (launch dormant, D-07); types regenerated with blur/fraud columns; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deploy → live edge runtime | webhook (--no-verify-jwt + code sig) vs user fn (JWT) vs service-to-service (service role) must be deployed with the correct auth posture |
| feature flag → production | blur_enabled must NOT be flipped true at deploy (gate dormant until on-device blur confirmed) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-17 | Elevation | mux-webhook deployed WITH jwt verify | mitigate | Deploy --no-verify-jwt (sig-verified in code); a jwt-required webhook would break Mux callbacks (matches Phase 3/5) |
| T-06-18 | Privacy / premature enablement | blur_enabled=true at launch | mitigate | Task 3 explicitly confirms + forces blur_enabled=false across all market_config rows (D-07); on-device blur is unverified (Plan 05) |
| T-06-19 | Information Disclosure | Vision key | accept | Reuse the existing Phase-5 secret; no new key handled; never logged |
</threat_model>

<verification>
- `supabase migration list` shows 0014; `supabase test db` green.
- `supabase functions list` shows face-blur-check + fraud-eval.
- live market_config: blur_enabled=false everywhere; fraud_strictness='flag'.
- types regenerated; tsc clean.
</verification>

<success_criteria>
- Phase-6 Category-A is live and dormant: schema applied, functions deployed, blur gate present-but-off, fraud flag-only, types in sync. No feature visibly changes for users until blur_enabled is flipped (Troy's call, after Plan 05 device verification).
</success_criteria>

<output>
After completion, create `.planning/phases/06-privacy-anti-fraud-hardening-on-device-face-plate-blur-befor/06-04-SUMMARY.md`
</output>
