---
phase: 06-privacy-anti-fraud-hardening-on-device-face-plate-blur-befor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0014_privacy_fraud_signals.sql
  - supabase/tests/0014_privacy_fraud_signals.test.sql
  - supabase/functions/face-blur-check/index.test.ts
  - supabase/functions/fraud-eval/index.test.ts
  - lmc-app/app/lib/fraud-signals.test.ts
autonomous: true
requirements: [BLUR-01, BLUR-02, BLUR-03, FRAUD-01, FRAUD-02, FRAUD-03, SCH-01]
category: A
must_haves:
  truths:
    - "clips.blur_status defaults to 'pending'; clips.fraud_flag defaults to false (SCH-01)"
    - "market_config.blur_enabled defaults to FALSE (launch posture D-07 — gate dormant)"
    - "market_config.fraud_strictness defaults to 'flag' (D-04 flag-only, tunable)"
    - "check_status gains 'blur_review'; entry: filming -> blur_review; exits: blur_review -> delivered (ops approve) / dispatching (re-dispatch) / cancelled (D-03)"
    - "RED Deno + Vitest + pgTAP scaffolds exist and fail until Plans 02/03 implement (Wave 0)"
  artifacts:
    - path: "supabase/migrations/0014_privacy_fraud_signals.sql"
      provides: "blur_status/fraud_signals/fraud_flag/fraud_score on clips; blur_enabled/fraud_strictness on market_config; blur_review enum + transition edges"
      contains: "blur_review"
    - path: "supabase/tests/0014_privacy_fraud_signals.test.sql"
      provides: "pgTAP schema asserts (SCH-01)"
    - path: "supabase/functions/face-blur-check/index.test.ts"
      provides: "RED Deno scaffold for BLUR-01/02/03"
    - path: "supabase/functions/fraud-eval/index.test.ts"
      provides: "RED Deno scaffold for FRAUD-01/02"
    - path: "lmc-app/app/lib/fraud-signals.test.ts"
      provides: "RED Vitest scaffold for FRAUD-03"
  key_links:
    - from: "0014 migration"
      to: "market_config"
      via: "ALTER TABLE ADD COLUMN blur_enabled/fraud_strictness"
      pattern: "blur_enabled BOOLEAN NOT NULL DEFAULT FALSE"
    - from: "0014 migration"
      to: "is_valid_check_transition"
      via: "CREATE OR REPLACE adding blur_review edges (enum-add BEFORE function replace)"
      pattern: "blur_review"
---

<objective>
Lay the Phase-6 SQL spine in one migration (0014) and write the Wave-0 RED test scaffolds that Plans 02/03 turn green. This plan ONLY touches new schema + new test files — it does not touch any Edge Function source or mux-webhook, so it has zero file overlap with later waves.

Purpose: Plans 02/03 must build against fixed schema contracts (the same discipline 0012 used for Phase 5), and every Category-A behaviour must have a failing test BEFORE its implementation lands (Nyquist / TDD).
Output: 0014 migration + pgTAP test + three RED scaffolds (Deno x2, Vitest x1).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06-privacy-anti-fraud-hardening-on-device-face-plate-blur-befor/06-CONTEXT.md
@.planning/phases/06-privacy-anti-fraud-hardening-on-device-face-plate-blur-befor/06-RESEARCH.md

<read_first>
- supabase/migrations/0012_dispatch_verification_spine.sql — THE pattern to mirror: market_config ALTER, additive clip columns, is_valid_check_transition CREATE OR REPLACE with new edges, enum-add discipline. Copy its style (comments, ::text comparison, idempotency). NOTE: 0012's re-dispatch edges show the check is in `filming` (or uploaded/processing) when a clip-stage gate fires — the GPS reject path uses `filming -> dispatching`. The Phase-6 blur gate fires at the SAME point (see Plan 03 step 6c, before the uploaded/processing chain), so its entry edge is `filming -> blur_review`.
- supabase/migrations/0008_clips_location.sql + 0010_clips_mux.sql — how check_status enum values were added historically (ALTER TYPE ... ADD VALUE IF NOT EXISTS, then CREATE OR REPLACE the transition fn). 0007/0010 prove enum-add-then-function-replace in one file is safe (::text comparison, no create-time enum resolution).
- supabase/tests/0012_geo_spatial.test.sql + supabase/tests/clips_mux.test.sql — pgTAP style (plan(N), has_column, col_default_is, finish()).
- supabase/functions/signage-check/index.test.ts — the Deno mockSvc + visionReturning pattern to copy for face-blur-check scaffold.
- supabase/functions/fraud-eval — does NOT exist yet (Plan 02 creates index.ts); only write the test scaffold here.
- lmc-app/app/lib/scout-location.test.ts — existing Vitest style to copy for fraud-signals.test.ts.
</read_first>

<interfaces>
The contracts these scaffolds pin (Plans 02/03 implement against them):

face-blur-check (Plan 02 creates index.ts):
```typescript
export interface BlurDeps {
  svc: Svc;                                   // service-role supabase client (mocked)
  vision: (img: { content?: string; imageUri?: string }) => Promise<{ faces: number }>;
  apiKeyPresent: boolean;
  blurEnabled: boolean;                       // read from market_config.blur_enabled
}
export async function handleFaceBlurCheck(
  checkId: string, deps: BlurDeps,
): Promise<{ action: 'pass' | 'hold'; faces_detected: number }>;
```

fraud-eval (Plan 02 creates index.ts):
```typescript
export interface FraudDeps { svc: Svc; strictness: 'off' | 'flag' | 'hold' | 'reject'; }
export async function handleFraudEval(
  checkId: string, deps: FraudDeps,
): Promise<{ fraud_flag: boolean; fraud_score: number; is_teleport: boolean }>;
```

fraud-signals.ts (Plan 02 creates):
```typescript
export interface FraudSignals {
  accuracy_is_exact: boolean;     // accuracy != null && accuracy <= 1.0
  location_accuracy_m: number | null;
  collection_ts: string;          // ISO
  is_simulated_by_software: null;  // not available without native module (Pitfall 6)
}
export function collectFraudSignals(accuracy: number | null | undefined): FraudSignals;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write 0014 migration (schema spine + blur_review enum + transition edges)</name>
  <files>supabase/migrations/0014_privacy_fraud_signals.sql</files>
  <action>
Create the Phase-6 migration mirroring 0012's style (header comment block explaining each section, ::text comparison in the transition fn, idempotent ALTERs). Sections, in this exact order (enum-add BEFORE function replace — Pitfall 4):

1. **clips additive columns** (nullable, do not affect existing rows):
   ```sql
   alter table public.clips
     add column if not exists blur_status   text default 'pending',
     add column if not exists fraud_signals jsonb,
     add column if not exists fraud_flag    boolean default false,
     add column if not exists fraud_score   smallint;
   ```
   Comment blur_status with its enum-in-text vocabulary: 'pending' | 'no_faces' | 'blurred' | 'faces_detected_unblurred' | 'blur_check_failed' (D-03). Note blur_status/fraud_flag/fraud_score are SERVICE-ROLE-ONLY writes (DATA-02) — no new client UPDATE policy added (state that explicitly in a comment).

2. **market_config additive columns** (per-market tunable, like film_fence_max_m):
   ```sql
   alter table public.market_config
     add column if not exists blur_enabled     boolean not null default false,  -- D-07 LAUNCH POSTURE: gate dormant until on-device blur visually confirmed
     add column if not exists fraud_strictness text    not null default 'flag';  -- D-04: off | flag | hold | reject (TUNABLE)
   ```
   CRITICAL: blur_enabled DEFAULT **false** (not true — research draft said true; CONTEXT D-07 + launch posture require dormant-until-confirmed). Add a CHECK constraint on fraud_strictness in ('off','flag','hold','reject'). Comment both columns heavily (cite D-04/D-07, "never hard-code in app/edge code — read from this table").

3. **check_status enum value 'blur_review'** (ADD FIRST, before the function replace):
   ```sql
   alter type check_status add value if not exists 'blur_review';
   ```

4. **is_valid_check_transition() CREATE OR REPLACE** — copy the FULL 0012 body verbatim (all existing edges preserved exactly) and ADD blur_review edges. The blur gate (Plan 03 step 6c) fires while the check is in `filming` (BEFORE the webhook drives uploaded->processing->delivered), mirroring the Phase-5 GPS reject which uses `filming -> dispatching`. So the edges are:
   - ENTRY: `filming -> blur_review` (the blur gate holds a faces-detected clip while still in filming). NOT `processing -> blur_review` — at gate time the check has not yet been transitioned to processing.
   - EXIT `blur_review -> delivered` (ops manual approve after blur confirmed).
   - EXIT `blur_review -> dispatching` (re-dispatch, like a GPS reject — a held clip can be re-shot).
   - EXIT `blur_review -> cancelled` (ops/seeker abandons).
   Write them in ::text style, e.g. `when p_from::text='filming' and p_to::text='blur_review' then true` and `when p_from::text='blur_review' and p_to::text in ('delivered','dispatching','cancelled') then true`. Only reference check_status values that EXIST (`rejected` is a clips.status, NOT a check_status — do NOT add a blur_review->rejected edge). Grep the enum first to confirm: `grep -rn "create type check_status\|add value" supabase/migrations/`.

   Document in a comment why `filming -> blur_review` is the entry edge (mux-webhook holds AFTER clip finalize/GPS pass but BEFORE the uploaded/processing/delivered chain — the check is still in `filming` at that instant; see Plan 03 step 6c).

Keep file < 500 lines. End with a DATA-02 confirmation comment (no new client write policy on clips/market_config beyond existing read policies).
  </action>
  <verify>
    <automated>grep -q "blur_review" supabase/migrations/0014_privacy_fraud_signals.sql && grep -qE "filming.*blur_review" supabase/migrations/0014_privacy_fraud_signals.sql && ! grep -qE "processing.*->.*blur_review|'processing'.*'blur_review'" supabase/migrations/0014_privacy_fraud_signals.sql && grep -q "blur_enabled boolean not null default false" supabase/migrations/0014_privacy_fraud_signals.sql && grep -q "fraud_strictness" supabase/migrations/0014_privacy_fraud_signals.sql && grep -q "add value if not exists 'blur_review'" supabase/migrations/0014_privacy_fraud_signals.sql && echo OK</automated>
  </verify>
  <done>0014 migration exists; blur_enabled defaults false; fraud_strictness with CHECK constraint; blur_review enum added before the transition-fn replace; ENTRY edge is `filming -> blur_review` (NOT processing); exits are delivered/dispatching/cancelled only (no blur_review->rejected); DATA-02 comment present.</done>
</task>

<task type="auto">
  <name>Task 2: Write pgTAP schema test (SCH-01)</name>
  <files>supabase/tests/0014_privacy_fraud_signals.test.sql</files>
  <action>
Create a pgTAP test mirroring supabase/tests/clips_mux.test.sql. Assert:
- `clips.blur_status` exists and `col_default_is('clips','blur_status','pending')`.
- `clips.fraud_flag` exists and default is false.
- `clips.fraud_signals` exists (jsonb) and `clips.fraud_score` exists (smallint).
- `market_config.blur_enabled` exists and `col_default_is('market_config','blur_enabled','false')`.
- `market_config.fraud_strictness` exists and default is 'flag'.
- `'blur_review'` is a valid check_status enum label (query pg_enum joined to pg_type where typname='check_status', assert the label is present).
- (Optional but recommended) `is_valid_check_transition('filming','blur_review')` returns true and `is_valid_check_transition('processing','blur_review')` returns false — pins the corrected entry edge.
Wrap in plan(N) / finish(). Run inside a transaction (begin/rollback) like the existing pgTAP files.
  </action>
  <verify>
    <automated>grep -q "blur_status" supabase/tests/0014_privacy_fraud_signals.test.sql && grep -q "blur_enabled" supabase/tests/0014_privacy_fraud_signals.test.sql && grep -q "blur_review" supabase/tests/0014_privacy_fraud_signals.test.sql && grep -q "fraud_strictness" supabase/tests/0014_privacy_fraud_signals.test.sql && echo OK</automated>
  </verify>
  <done>pgTAP file asserts all SCH-01 columns + defaults + the blur_review enum label (+ the filming->blur_review entry edge). (Runs live in Plan 04 via `supabase test db` after db push; offline it is a static contract.)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write RED Wave-0 test scaffolds (BLUR-01/02/03, FRAUD-01/02/03)</name>
  <files>supabase/functions/face-blur-check/index.test.ts, supabase/functions/fraud-eval/index.test.ts, lmc-app/app/lib/fraud-signals.test.ts</files>
  <behavior>
    face-blur-check (BLUR-01/02/03):
    - BLUR-01: vision returns faces>0 AND blur_status != 'blurred' AND blurEnabled=true -> action='hold', faces_detected>0; clips.blur_status set to 'faces_detected_unblurred'.
    - BLUR-02: vision returns faces=0 AND blurEnabled=true -> action='pass'; clips.blur_status set to 'no_faces'.
    - BLUR-03: blurEnabled=false -> action='pass' regardless of faces (no-op gate); vision is NOT even consulted, OR result ignored; NO transition implied.
    - Invariant: handleFaceBlurCheck NEVER throws (mirrors signage degrade-to-null policy); on missing playback id -> blur_status='blur_check_failed', action='pass' (fail-open like signage; the gate-error pass-through is intentional — D-03 HOLD applies only to confirmed faces).
    fraud-eval (FRAUD-01/02):
    - FRAUD-01: a fraud_signals row whose server-computed velocity exceeds the teleport threshold -> is_teleport=true.
    - FRAUD-02: with strictness='flag' and an anomaly present -> fraud_flag=true, fraud_score>0, and a 'check.fraud_flagged' event logged via svc.rpc('log_event', ...). With strictness='off' -> fraud_flag=false even on anomaly.
    fraud-signals.ts (FRAUD-03):
    - collectFraudSignals(0.5) -> accuracy_is_exact=true, location_accuracy_m=0.5.
    - collectFraudSignals(8.5) -> accuracy_is_exact=false, location_accuracy_m=8.5.
    - collectFraudSignals(null) -> accuracy_is_exact=false, location_accuracy_m=null, is_simulated_by_software=null.
  </behavior>
  <action>
Write the three test files importing the not-yet-existing symbols from the interfaces block (handleFaceBlurCheck, handleFraudEval, collectFraudSignals). They are RED: the imports resolve to modules Plan 02 creates. Use:
- Deno tests: copy the mockSvc + visionReturning helpers from signage-check/index.test.ts; adapt vision to return { faces: n } instead of { text: [] }. Use `import { assert, assertEquals } from "https://deno.land/std@0.224.0/assert/mod.ts";`.
- Vitest test: copy the import/describe/it style from lmc-app/app/lib/scout-location.test.ts.
Each test must assert the D-06-style invariant where relevant (face-blur-check NEVER calls reset_check_for_redispatch — only mux-webhook orchestrates hold; the function itself only writes blur_status + returns an action). Add a comment at the top of each file: "RED Wave-0 scaffold; Plan 02 turns this green."
  </action>
  <verify>
    <automated>test -f supabase/functions/face-blur-check/index.test.ts && test -f supabase/functions/fraud-eval/index.test.ts && test -f lmc-app/app/lib/fraud-signals.test.ts && grep -q "handleFaceBlurCheck" supabase/functions/face-blur-check/index.test.ts && grep -q "handleFraudEval" supabase/functions/fraud-eval/index.test.ts && grep -q "collectFraudSignals" lmc-app/app/lib/fraud-signals.test.ts && echo RED-SCAFFOLDS-OK</automated>
  </verify>
  <done>Three test files exist importing the Plan-02 symbols; they fail (RED) because index.ts/fraud-signals.ts do not exist yet. Behaviours above are pinned as assertions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → market_config | Authenticated users can READ market_config (radius/flag hints) but must NOT write blur_enabled/fraud_strictness |
| client → clips.blur_status/fraud_flag | Client must never set its own blur verdict or clear its own fraud flag |
| migration ordering → enum | enum-add racing with function replace can fail (Pitfall 4) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | clips.blur_status | mitigate | No new client UPDATE policy in 0014; blur_status writable only by service role (face-blur-check). DATA-02 comment asserts this. |
| T-06-02 | Tampering | clips.fraud_flag | mitigate | fraud_flag/fraud_score service-role-only; client-supplied fraud_signals is stored as provenance but the verdict is computed server-side (Plan 02 fraud-eval) |
| T-06-03 | Tampering | market_config.blur_enabled | mitigate | Existing market_config RLS is read-only for authenticated; 0014 adds NO write policy |
| T-06-04 | DoS/Integrity | check_status enum migration | mitigate | enum ADD VALUE IF NOT EXISTS placed BEFORE is_valid_check_transition CREATE OR REPLACE; ::text comparison (Pitfall 4) |
| T-06-05 | Elevation | blur_review transitions | accept | New edges only reach existing states (filming entry; delivered/dispatching/cancelled exits); transition_check actor-authz (0012) still gates who may drive them — ops/service role only in practice |
</threat_model>

<verification>
- `grep` checks in each task pass (migration columns, defaults, enum, filming->blur_review entry edge, no processing->blur_review, test files).
- Live pgTAP + enum verification deferred to Plan 04 (`supabase test db` after db push).
- No Edge Function source touched in this plan (zero overlap with Wave 2/3).
</verification>

<success_criteria>
- 0014 migration creates all blur/fraud schema with blur_enabled=false launch default and fraud_strictness='flag'.
- blur_review enum + transition edges added safely (enum before function replace; ENTRY filming->blur_review; exits delivered/dispatching/cancelled; existing-values-only).
- pgTAP + two Deno + one Vitest RED scaffolds exist, pinning every Category-A behaviour.
</success_criteria>

<output>
After completion, create `.planning/phases/06-privacy-anti-fraud-hardening-on-device-face-plate-blur-befor/06-01-SUMMARY.md`
</output>
