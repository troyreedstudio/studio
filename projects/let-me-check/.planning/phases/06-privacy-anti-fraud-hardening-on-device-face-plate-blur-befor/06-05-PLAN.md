---
phase: 06-privacy-anti-fraud-hardening-on-device-face-plate-blur-befor
plan: 05
type: execute
wave: 5
depends_on: [04]
files_modified:
  - lmc-app/package.json
  - lmc-app/package-lock.json
  - lmc-app/app.config.js
  - lmc-app/app/lib/blur-config.ts
  - lmc-app/app/(scout)/_filming-blur-overlay.tsx
  - lmc-app/app/(scout)/filming.tsx
autonomous: true
requirements: [BLUR-NATIVE-01]
category: B
must_haves:
  truths:
    - "the on-device blur native stack (worklets-core + vision-camera-face-detector + Skia) is installed and behind a BLUR_NATIVE_ENABLED flag that defaults FALSE"
    - "a SkiaCamera face-blur overlay component exists, wired into filming.tsx only when the flag is on"
    - "with the flag OFF, filming.tsx behaves EXACTLY as before (no native blur code path runs) — tsc clean, existing camera flow unchanged"
    - "the device build compiles and the app boots (the ONLY overnight verification for Category B; visual blur correctness is Troy's AM check)"
    - "New-Architecture compatibility is called out explicitly (prior bites: createUploadTask, google-signin)"
  artifacts:
    - path: "lmc-app/app/lib/blur-config.ts"
      provides: "BLUR_NATIVE_ENABLED feature flag (default false) + blur config constants"
      contains: "BLUR_NATIVE_ENABLED"
    - path: "lmc-app/app/(scout)/_filming-blur-overlay.tsx"
      provides: "SkiaCamera + face-detector frame processor blur overlay (flag-gated)"
    - path: "lmc-app/app/(scout)/filming.tsx"
      provides: "conditional render of the blur overlay when BLUR_NATIVE_ENABLED"
  key_links:
    - from: "filming.tsx"
      to: "_filming-blur-overlay.tsx"
      via: "BLUR_NATIVE_ENABLED ? <BlurViewfinder/> : <CameraViewfinder/>"
      pattern: "BLUR_NATIVE_ENABLED"
    - from: "_filming-blur-overlay.tsx"
      to: "react-native-vision-camera-face-detector"
      via: "useFaceDetector frame processor + Skia blur rect"
      pattern: "useFaceDetector|FaceDetector"
---

<objective>
[CATEGORY B — DEVICE-BUILD-VERIFIABLE ONLY] Scaffold the on-device face-blur path: install the native stack (react-native-worklets-core + react-native-vision-camera-face-detector + @shopify/react-native-skia + the vision-camera Skia bridge), build a SkiaCamera blur overlay component, and wire it into filming.tsx BEHIND a BLUR_NATIVE_ENABLED flag that defaults FALSE. Per 06-RESEARCH, this CANNOT be verified offline — the only overnight gate is "device build compiles + app boots." Visual blur correctness is explicitly Troy's morning check (Category C).

Purpose: deliver D-01's privacy-by-default on-device path (raw unblurred footage never leaves the phone) as a compilable, flag-gated scaffold — without flipping it on or destabilising the working camera flow. blur_enabled (server) + BLUR_NATIVE_ENABLED (client) both stay OFF until Troy confirms.
Output: installed native stack, blur overlay scaffold, flag-gated wiring, a fresh EAS dev build that compiles + boots.

AUTONOMOUS NOTE: this plan is fully autonomous — the orchestrator (Guy) runs the EAS dev build itself overnight (Task 3). It is NOT a human checkpoint. A compile/boot FAILURE does not block the phase: revert/defer the native additions and record the exact error for Troy's AM review. The shipping privacy mechanism is the Plans 01-04 server gate; on-device blur is the deferred enhancement.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-privacy-anti-fraud-hardening-on-device-face-plate-blur-befor/06-CONTEXT.md
@.planning/phases/06-privacy-anti-fraud-hardening-on-device-face-plate-blur-befor/06-RESEARCH.md

<read_first>
- 06-RESEARCH.md "Category B" + "Pitfall 3" + "Assumptions Log A1-A3" — the package set, versions, and the New-Arch risk. THIS IS THE GOVERNING SECTION. Packages: react-native-worklets-core@1.6.3, react-native-vision-camera-face-detector@2.0.1, @shopify/react-native-skia@2.6.6, and the vision-camera Skia integration (verify the bridge package name + a v4.7.x-compatible version at install time — A3 is unverified; if no v4-compatible Skia bridge exists, fall back to a Skia overlay drawn from face bounding boxes WITHOUT the SkiaCamera component, or document the gap and keep the flag off).
- lmc-app/app/(scout)/filming.tsx — the camera screen. The CameraViewfinder is rendered at the bottom (visible/recordSecs/cameraRef/device/hasPermission/onCameraInitialized props). The blur overlay must be a drop-in alternative gated by the flag, preserving every existing prop + the audio={false}/fresh-capture invariant.
- lmc-app/app/(scout)/_filming-viewfinder.tsx — the existing viewfinder to mirror props/shape.
- lmc-app/app.config.js — newArchEnabled: true is set. Any added native module MUST be New-Arch compatible. Phase-5 bites: createUploadTask (EventEmitter dead under New Arch) and google-signin (Old-Arch only) — call these out as the cautionary precedent in code comments.
- lmc-app/CLAUDE.md — vision-camera pinned to v4.7.x (v5 ships no Expo config plugin). Do NOT upgrade to v5.
</read_first>

<interfaces>
```typescript
// lmc-app/app/lib/blur-config.ts
export const BLUR_NATIVE_ENABLED = false;   // CATEGORY B: on-device blur OFF until device-verified + Troy AM visual check
export const BLUR_PIXEL_RADIUS = 18;        // Claude's discretion (D: blur strength); tunable
// _filming-blur-overlay.tsx exports a component with the SAME prop surface as CameraViewfinder
export function BlurViewfinder(props: ViewfinderProps): JSX.Element;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: install the on-device blur native stack + add the feature flag (Pitfall 3, New-Arch)</name>
  <files>lmc-app/package.json, lmc-app/package-lock.json, lmc-app/app.config.js, lmc-app/app/lib/blur-config.ts</files>
  <action>
1. Install (pinned per 06-RESEARCH): `cd lmc-app && npx expo install react-native-worklets-core@1.6.3 react-native-vision-camera-face-detector@2.0.1 @shopify/react-native-skia@2.6.6` (use `expo install` so SDK-54-compatible versions are honored; if expo install refuses a pin, install the expo-suggested compatible version and note the delta). For the vision-camera Skia bridge: resolve the correct package name + a v4.7.x-compatible version at install time (A3 unverified) — if none exists, SKIP the SkiaCamera bridge and document it; the overlay (Task 2) then falls back to a plain Skia <Canvas> overlay positioned from face bounding boxes.
2. app.config.js: add the vision-camera frame-processor + worklets config plugin entries if required (check each package's README for an Expo config plugin). Confirm newArchEnabled stays true. Add a comment block: "Phase 6 Category B — on-device blur native stack. New-Arch-compat is UNVERIFIED for this exact combo (06-RESEARCH A1-A3); prior New-Arch bites were createUploadTask + google-signin. BLUR_NATIVE_ENABLED defaults false; do not enable until the device build boots + Troy confirms blur visually."
3. Create lib/blur-config.ts exporting BLUR_NATIVE_ENABLED=false + BLUR_PIXEL_RADIUS per the interfaces block, with a doc comment citing D-01/D-07 (privacy-by-default on-device path; dormant until verified).
  </action>
  <verify>
    <automated>cd lmc-app && grep -q "react-native-worklets-core" package.json && grep -q "react-native-vision-camera-face-detector" package.json && grep -q "@shopify/react-native-skia" package.json && grep -q "BLUR_NATIVE_ENABLED = false" app/lib/blur-config.ts && npx tsc --noEmit && echo INSTALL-OK</automated>
  </verify>
  <done>Three (or four incl. Skia bridge) native packages installed at the researched versions; config plugins added; BLUR_NATIVE_ENABLED flag = false; tsc clean; New-Arch caution documented.</done>
</task>

<task type="auto">
  <name>Task 2: SkiaCamera face-blur overlay component (flag-gated, drop-in)</name>
  <files>lmc-app/app/(scout)/_filming-blur-overlay.tsx, lmc-app/app/(scout)/filming.tsx</files>
  <action>
1. Create _filming-blur-overlay.tsx exporting BlurViewfinder with the SAME prop surface as the existing CameraViewfinder (_filming-viewfinder.tsx). Inside: a vision-camera frame processor using useFaceDetector (react-native-vision-camera-face-detector) to get face bounding boxes per frame, and a Skia blur/pixelate rect drawn over each face region (SkiaCamera if the v4 bridge exists, else a Skia <Canvas> absolute overlay positioned from the detected boxes). Use @shopify/react-native-skia's blur/BlurMask or a pixelate RuntimeShader (BLUR_PIXEL_RADIUS). PRESERVE the invariants: audio={false}, fresh-capture (no gallery), the cameraRef/device/onCameraInitialized wiring, the 15s cap. This is a SCAFFOLD — correctness is verified on device, not here.
2. filming.tsx: import BLUR_NATIVE_ENABLED + BlurViewfinder. Replace the single <CameraViewfinder .../> at the bottom with `{BLUR_NATIVE_ENABLED ? <BlurViewfinder {...props}/> : <CameraViewfinder {...props}/>}` passing the identical props. With the flag false (default), the original path runs unchanged. Keep filming.tsx as lean as possible (no new UI; just the conditional). If filming.tsx risks exceeding 500 lines, the overlay lives entirely in _filming-blur-overlay.tsx (it does — only the conditional changes in filming.tsx).
GREP GATE: filming.tsx references BLUR_NATIVE_ENABLED; the blur overlay file references the face detector.
  </action>
  <verify>
    <automated>cd lmc-app && grep -q "BLUR_NATIVE_ENABLED" app/(scout)/filming.tsx && grep -qE "useFaceDetector|FaceDetector|vision-camera-face-detector" app/(scout)/_filming-blur-overlay.tsx && npx tsc --noEmit && echo OVERLAY-OK</automated>
  </verify>
  <done>BlurViewfinder scaffold exists with the face-detector frame processor + Skia blur; filming.tsx conditionally renders it behind the flag; flag-off path is byte-for-byte the existing camera flow; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 3: [DEVICE BUILD] EAS dev build compiles + app boots (Category B gate)</name>
  <files>(no source file — native build of lmc-app)</files>
  <action>
The orchestrator (Guy) runs the build itself overnight — autonomous, NOT a human checkpoint. The overnight PASS condition is "compiles + boots", not visual blur correctness.
1. `cd lmc-app && eas build -p ios --profile development` (a fresh dev build that links the new native modules). If EAS auth fails, create an auth gate and note it for AM (do NOT hand-roll a workaround).
2. Confirm the build reports "Build Succeeded" / compiled — the three+ native modules (worklets-core, vision-camera-face-detector, Skia, optional Skia bridge) linked under New Architecture without a pod/link error. COMPILING is the overnight pass.
3. Boot check: install + launch the build (device or simulator). The app must BOOT to the splash with no immediate native crash (all three modules load). With BLUR_NATIVE_ENABLED=false the blur path never runs, so a clean boot confirms the natives link without being activated.
4. FAILURE HANDLING (the A1/A2/A3 New-Arch risk): do NOT block the phase. Either revert the Task 1+2 native additions, or leave them in place with the flag off IF they compile-but-unused. Record the precise failure (which package, the exact pod/link/boot error) in the SUMMARY for Troy's AM decision. The Plans 01-04 server gate is the shipping privacy mechanism; on-device blur is the deferred enhancement.
VISUAL blur correctness (faces actually blurred in the viewfinder) is Troy's morning check (Category C) — explicitly NOT part of this overnight gate.
  </action>
  <verify>
    <automated>cd lmc-app && eas build -p ios --profile development --non-interactive 2>&1 | tee /tmp/lmc-eas-06.log | grep -qiE "Build Succeeded|finished|completed" && echo DEVICE-BUILD-OK || echo "DEVICE-BUILD-FAILED — record exact error from /tmp/lmc-eas-06.log in SUMMARY; revert/defer natives; do NOT block phase"</automated>
  </verify>
  <done>A fresh EAS dev build compiles and the app boots (New-Arch link confirmed) with BLUR_NATIVE_ENABLED=false — OR a precise compile/boot-failure report is recorded in the SUMMARY and the natives are reverted/deferred without blocking the phase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| native module → New Arch runtime | a New-Arch-incompatible native module crashes the app on boot |
| on-device blur → upload | when enabled, blurred frames must be what's recorded/uploaded (raw never leaves device) |
| feature flag → production | BLUR_NATIVE_ENABLED must ship false until device + visual verified |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-20 | Availability | New-Arch-incompatible native module | mitigate | Flag defaults false (modules link but don't activate); device-build gate catches a compile/boot failure before any user impact; failure -> revert/defer, server gate still protects privacy |
| T-06-21 | Privacy | unblurred raw footage leaving device | accept (deferred) | On-device blur is the future privacy-by-default path; at launch the SERVER detect-and-hold gate (Plans 02-03, blur_enabled) is the privacy mechanism. Until BLUR_NATIVE_ENABLED + blur_enabled are both flipped (post-verification), the held-clip path (D-03) is the guarantee |
| T-06-22 | Tampering | client self-asserting blur_status='blurred' | mitigate | (cross-ref T-06-09) when on-device blur lands, blur_status='blurred' must be set via a server-attested path, not a raw client write; documented as a follow-up before enabling |
</threat_model>

<verification>
- tsc clean with the flag off (Tasks 1-2).
- grep gates: BLUR_NATIVE_ENABLED in filming.tsx + blur-config.ts; face detector in the overlay.
- Device build compiles + app boots (Task 3 — the Category-B overnight gate, orchestrator-run).
- Visual blur correctness deferred to Troy AM (Category C).
</verification>

<success_criteria>
- On-device blur stack installed + scaffolded behind a default-false flag; the working camera flow is untouched when the flag is off.
- A fresh EAS dev build compiles and the app boots (New-Arch link confirmed) — OR a precise compile-failure report is left for Troy without blocking the phase.
- Nothing is enabled: BLUR_NATIVE_ENABLED=false (client) + blur_enabled=false (server) both ship dormant.
</success_criteria>

<output>
After completion, create `.planning/phases/06-privacy-anti-fraud-hardening-on-device-face-plate-blur-befor/06-05-SUMMARY.md`

Also append a "Category C — Troy AM review" section listing: (1) confirm D-01 on-device vs server-side for v1, (2) confirm D-03 hold-vs-soft-flag, (3) confirm D-04 flag-only vs auto-reject, (4) confirm D-06 signage stays server-side, (5) visual blur-correctness check on the device build, (6) decision on when to flip blur_enabled + BLUR_NATIVE_ENABLED true.
</output>
