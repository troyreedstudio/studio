---
phase: 06-privacy-anti-fraud-hardening-on-device-face-plate-blur-befor
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - supabase/functions/mux-webhook/index.ts
  - supabase/functions/mux-webhook/index.test.ts
  - supabase/functions/mux-upload-url/index.ts
  - lmc-app/app/(scout)/filming.tsx
  - lmc-app/app/lib/clips.ts
autonomous: true
requirements: [BLUR-04, BLUR-05, FRAUD-01, FRAUD-02]
category: A
must_haves:
  truths:
    - "mux-webhook: when face-blur-check returns action=hold, the check transitions to blur_review and is NOT delivered/captured (BLUR-04, D-03/D-07)"
    - "mux-webhook: when face-blur-check throws or blur_enabled=false, delivery proceeds (fail-open pass-through, BLUR-05)"
    - "mux-webhook invokes fraud-eval (fire-and-forget, advisory) — a fraud flag never blocks delivery at launch (D-04 flag-only)"
    - "Scout client collects fraud_signals at film time and forwards them through mux-upload-url onto the clips row"
    - "the blur gate sits AFTER the GPS gate + clip finalize, BEFORE the delivered transition"
  artifacts:
    - path: "supabase/functions/mux-webhook/index.ts"
      provides: "blur gate (hold->blur_review) + fraud-eval fire-and-forget, slotted around the existing GPS gate"
      contains: "face-blur-check"
    - path: "lmc-app/app/lib/fraud-signals.ts"
      provides: "(from Plan 02) consumed by filming.tsx/clips.ts"
    - path: "supabase/functions/mux-upload-url/index.ts"
      provides: "accepts + persists fraud_signals JSON onto clips.fraud_signals"
      contains: "fraud_signals"
  key_links:
    - from: "mux-webhook/index.ts"
      to: "face-blur-check"
      via: "svc.functions.invoke('face-blur-check', { body:{ checkId } }) after GPS pass, before delivered"
      pattern: "face-blur-check"
    - from: "mux-webhook/index.ts"
      to: "transition_check(blur_review)"
      via: "on action=hold"
      pattern: "blur_review"
    - from: "filming.tsx"
      to: "clips.submit"
      via: "collectFraudSignals(accuracyM) passed through gps payload"
      pattern: "collectFraudSignals"
    - from: "mux-upload-url/index.ts"
      to: "clips.fraud_signals"
      via: "insert fraud_signals from body"
      pattern: "fraud_signals"
---

<objective>
Wire the two Plan-02 functions into the pre-delivery gate and complete the client fraud-signal capture chain. mux-webhook gains a blur gate (hold -> blur_review) immediately after the existing GPS gate and a fire-and-forget fraud-eval call; mux-upload-url persists the client fraud_signals bag; filming.tsx/clips.ts collect + forward it. This is the LAST Category-A wave and the only one that touches mux-webhook — sequenced after Plan 02 so the invoked functions exist.

Purpose: make the privacy gate real (no clip with detected faces is delivered when blur_enabled — D-03/D-07) while keeping the launch posture safe (blur_enabled=false ships dormant; fraud is flag-only — D-04). Preserve all five existing mux-webhook tests plus verify-clip GPS behaviour.
Output: gated mux-webhook + extended test; fraud_signals persisted end-to-end.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-privacy-anti-fraud-hardening-on-device-face-plate-blur-befor/06-CONTEXT.md
@.planning/phases/06-privacy-anti-fraud-hardening-on-device-face-plate-blur-befor/06-RESEARCH.md

<read_first>
- supabase/functions/mux-webhook/index.ts — THE file to extend. The GPS gate (step 6b, verify-clip invoke -> reset_check_for_redispatch on passed:false) is the exact pattern. The blur gate slots AFTER the GPS pass-through point and BEFORE the uploaded/processing/delivered transitions (step 7). Preserve steps 8 (stripe-capture) + 9 (signage advisory) exactly.
- supabase/functions/mux-webhook/index.test.ts — the mockSvc with `verifyClipPassed` option. Extend with a `blurAction` option (mirroring verifyClipPassed) so face-blur-check returns { data:{ action } }. ALL 5 existing tests must stay green (default blurAction undefined -> pass-through).
- supabase/functions/mux-upload-url/index.ts — accepts filmed_lat/lng/accuracy_m + inserts the clips row. Add fraud_signals to the input + the insert (validate it's an object; drop non-objects).
- lmc-app/app/(scout)/filming.tsx — stampGps() captures capturedGps (lat/lng/accuracyM). Collect fraud_signals there via collectFraudSignals(accuracyM). handleSubmit passes capturedGps to clipUpload.submit.
- lmc-app/app/lib/clips.ts — requestUploadUrl/submit currently forward gps; thread fraud_signals through to mux-upload-url's body.
- lmc-app/app/lib/fraud-signals.ts — (Plan 02) collectFraudSignals.
</read_first>

<interfaces>
mux-webhook blur gate sequencing (CRITICAL ORDER):
```
... step 6 finalize clip ...
step 6b: GPS gate (existing) -> passed:false -> reset_check_for_redispatch, return 'gps_rejected'
step 6c: BLUR gate (NEW):
   const blur = await svc.functions.invoke('face-blur-check', { body:{ checkId } })  // try/catch -> {data:null}
   if (blur?.data?.action === 'hold') {
     await svc.rpc('transition_check', { p_check_id: checkId, p_to: 'blur_review' })
     return new Response('blur_held', { status: 200 })
   }
   // any error / action!=='hold' / blur_enabled=false (function returns 'pass') -> fall through (FAIL-OPEN, BLUR-05)
step 7: uploaded -> processing -> delivered (existing)
step 8: stripe-capture (existing)
step 8b: fraud-eval (NEW, fire-and-forget AFTER delivered — advisory, never blocks; D-04):
   try { await svc.functions.invoke('fraud-eval', { body:{ checkId } }) } catch (_e) { /* advisory */ }
step 9: signage advisory (existing)
```
clips.submit signature gains fraud_signals; mux-upload-url body gains fraud_signals (jsonb).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: mux-webhook blur gate + fraud-eval (BLUR-04, BLUR-05, D-03/D-04/D-07)</name>
  <files>supabase/functions/mux-webhook/index.ts, supabase/functions/mux-webhook/index.test.ts</files>
  <behavior>
    Extend mux-webhook/index.test.ts mockSvc with a `blurAction?: 'pass'|'hold'` option (face-blur-check invoke returns { data: blurAction===undefined ? null : { action: blurAction } }). New tests:
    - BLUR-04: blurAction='hold' -> response body 'blur_held'; transition_check p_to='blur_review' called; NO delivered transition; stripe-capture NOT invoked.
    - BLUR-05: face-blur-check throws (mock reject for that fn) -> delivery proceeds (delivered transition present, stripe-capture invoked). Fail-open.
    - blurAction='pass' (or undefined) -> existing delivered+capture flow unchanged.
    - fraud-eval is invoked on the delivered path (find invoke fn==='fraud-eval') and its failure does NOT prevent the 200/delivered (fire-and-forget).
    ALL 5 pre-existing tests stay green (verify the full transition order ["uploaded","processing","delivered"] still holds on the pass path; gps_rejected path unchanged and never reaches the blur gate).
  </behavior>
  <action>
1. In index.ts, insert step 6c (blur gate) immediately AFTER the GPS gate's pass-through point (after the `if (verifyResult?.data?.passed === false)` block, before the step-7 transition_check chain). Use try/catch -> {data:null} so an invoke error is fail-open (BLUR-05). On action==='hold': transition_check(blur_review) + return Response('blur_held', 200). Add a heavy comment citing D-03/D-07 + that blur_enabled=false makes face-blur-check return 'pass' so the gate is a structural no-op at launch.
2. Insert step 8b (fraud-eval) AFTER the stripe-capture try/catch (step 8), fire-and-forget try/catch like signage. Comment: advisory only, D-04 flag-only — a fraud flag NEVER blocks delivery at launch.
3. Update the mockSvc to add blurAction; update the functions.invoke mock to branch on 'face-blur-check' (return the action) the same way it branches on 'verify-clip'. Keep verify-clip branch intact.
4. Add the 2 new tests (BLUR-04, BLUR-05) + a fraud-eval-invoked assertion.
GREP GATE: 'blur_review' and 'face-blur-check' and 'fraud-eval' all present in index.ts.
  </action>
  <verify>
    <automated>deno test --allow-env supabase/functions/mux-webhook/index.test.ts && grep -q "blur_review" supabase/functions/mux-webhook/index.ts && grep -q "face-blur-check" supabase/functions/mux-webhook/index.ts && grep -q "fraud-eval" supabase/functions/mux-webhook/index.ts && echo OK</automated>
  </verify>
  <done>All mux-webhook tests green (5 existing + BLUR-04/BLUR-05 + fraud-eval); blur gate holds in blur_review on action=hold; fail-open on error; fraud-eval fire-and-forget after delivered.</done>
</task>

<task type="auto">
  <name>Task 2: persist fraud_signals through mux-upload-url (V5 input validation)</name>
  <files>supabase/functions/mux-upload-url/index.ts</files>
  <action>
Add `fraud_signals?: Record<string, unknown>` to the handleUploadUrl input + the live entrypoint's body parse. Validate server-side (V5): only persist when it is a plain object (typeof === 'object' && !Array.isArray && !== null); drop anything else (mirror the isFiniteNum gps validation). Add `fraud_signals` to the clips INSERT alongside the gps fields. Comment: this is RECORDED client-supplied provenance — the verdict (fraud_flag/fraud_score) is computed server-side by fraud-eval (T-06-07); the raw bag is never trusted as a verdict. Use `as any` on the insert object if the typed client complains (types regen is Plan 05).
  </action>
  <verify>
    <automated>grep -q "fraud_signals" supabase/functions/mux-upload-url/index.ts && cd supabase && deno check functions/mux-upload-url/index.ts 2>/dev/null; echo DONE</automated>
  </verify>
  <done>mux-upload-url accepts + validates + persists fraud_signals onto clips.fraud_signals; non-object values dropped; provenance-not-verdict comment present.</done>
</task>

<task type="auto">
  <name>Task 3: collect + forward fraud_signals from filming.tsx through clips.ts</name>
  <files>lmc-app/app/(scout)/filming.tsx, lmc-app/app/lib/clips.ts</files>
  <action>
1. clips.ts: thread fraud_signals through the upload path.
   - `requestUploadUrl(checkId, gps?, fraudSignals?)` -> include fraud_signals in the invokeEdgeFunction('mux-upload-url', { ... }) body.
   - `useClipUpload().submit(checkId, localPath, gps?, fraudSignals?)` -> pass fraudSignals to requestUploadUrl. Keep the existing signature backward-compatible (new param optional). Update lib/clips.test.ts if it asserts the arg shape.
2. filming.tsx: import collectFraudSignals from '../lib/fraud-signals'. In stampGps(), after capturing accuracy, also compute and store the fraud signals (e.g. capturedFraud.current = collectFraudSignals(pos.coords.accuracy)). In handleSubmit, pass capturedFraud.current to clipUpload.submit(checkId, capturedPath, capturedGps.current, capturedFraud.current). Best-effort: never block recording/submit on a missing signal.
Keep filming.tsx < 500 lines (it is ~640 already — do NOT add UI; only the ref + two small wiring lines. If it would exceed, extract nothing new but keep edits minimal). Run tsc.
  </action>
  <verify>
    <automated>cd lmc-app && grep -q "collectFraudSignals" app/(scout)/filming.tsx && grep -q "fraudSignals\|fraud_signals" app/lib/clips.ts && npx tsc --noEmit && npx vitest run app/lib/clips.test.ts && echo OK</automated>
  </verify>
  <done>filming.tsx collects fraud signals at GPS-stamp time and forwards them via clips.submit; clips.ts threads them to mux-upload-url; tsc clean; clips Vitest green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mux webhook → check state | only mux-webhook (service role, sig-verified) may drive delivered or blur_review |
| client → fraud_signals | client supplies the bag; server validates shape + recomputes verdict |
| blur gate → delivery | a held clip must not reach delivered or stripe-capture |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-12 | Privacy / unblurred delivery | mux-webhook delivered path | mitigate | blur gate (step 6c) holds in blur_review on action=hold BEFORE the delivered transition + stripe-capture; no charge, no delivery (D-03/D-07) |
| T-06-13 | Availability | face-blur-check error blocking delivery | accept | Fail-open (BLUR-05): an invoke error falls through to deliver — consistent with verify-clip/signage "can't reject what we can't verify"; the deliberate HOLD is only on confirmed faces. Acceptable because blur_enabled=false at launch |
| T-06-14 | Spoofing | client forges fraud_signals to look clean | mitigate | fraud-eval recomputes teleport server-side from scout_locations + filmed coords; the client bag cannot suppress the server verdict (D-04) |
| T-06-15 | Tampering | client driving blur_review/delivered directly | mitigate | DATA-02 unchanged: clients have no UPDATE policy on checks; transition_check actor-authz (0012) blocks non-service callers from the system transitions |
| T-06-16 | DoS | fraud-eval failure cascading | mitigate | fire-and-forget try/catch (step 8b) — a fraud-eval failure never undoes a completed delivery |
</threat_model>

<verification>
- `deno test --allow-env supabase/functions/mux-webhook/index.test.ts` green (5 existing + BLUR-04/05 + fraud-eval).
- grep: blur_review + face-blur-check + fraud-eval present in mux-webhook; fraud_signals present in mux-upload-url + clips.ts; collectFraudSignals in filming.tsx.
- `cd lmc-app && npx tsc --noEmit` clean; clips Vitest green.
</verification>

<success_criteria>
- Blur gate holds detected-face clips in blur_review (D-03/D-07) and is a structural no-op when blur_enabled=false; fail-open on error (BLUR-05).
- fraud-eval runs advisory fire-and-forget post-delivery (D-04 flag-only).
- fraud_signals captured at film time and persisted end-to-end (filming -> clips.ts -> mux-upload-url -> clips.fraud_signals).
</success_criteria>

<output>
After completion, create `.planning/phases/06-privacy-anti-fraud-hardening-on-device-face-plate-blur-befor/06-03-SUMMARY.md`
</output>
