---
phase: 11-apple-submission-readiness
plan: 02
type: execute
wave: 2
depends_on: []
files_modified:
  - app/(seeker)/profile.tsx
  - app/(scout)/profile.tsx
  - app/(seeker)/search.tsx
  - app/(seeker)/membership.tsx
  - app/(scout)/filming.tsx
autonomous: true
requirements: [D-01, D-02]
must_haves:
  truths:
    - "From the shipped build, a Seeker cannot navigate to the membership screen or the invite/referral screen (no reachable entry points)"
    - "From the shipped build, a Scout cannot navigate to the invite/referral screen"
    - "The search screen's 'Use my current location' control performs a real action (requests/uses the device location) instead of doing nothing"
    - "The fake voice-search affordance is not reachable in the shipped build"
    - "No developer 'WF' wireframe badge is reachable in the shipped build (filming + membership)"
    - "No on-device blur test buttons (SHOW_BLUR_TEST) are reachable in the shipped build"
  artifacts:
    - path: "app/(seeker)/profile.tsx"
      provides: "SETTINGS array without membership + invite entries; referral banner removed"
    - path: "app/(scout)/profile.tsx"
      provides: "Refer-a-Scout referral banner removed (invite entry point gone)"
    - path: "app/(seeker)/search.tsx"
      provides: "Wired 'use current location' button; voice search gated to __DEV__"
    - path: "app/(scout)/filming.tsx"
      provides: "SHOW_BLUR_TEST const/state/function/JSX + WF badge removed; file under 500 lines"
    - path: "app/(seeker)/membership.tsx"
      provides: "WF wireframe badge removed from header"
  key_links:
    - from: "app/(seeker)/search.tsx"
      to: "app/state/location.ts requestUserLocation/getUserCoords"
      via: "onPress handler on the location button"
      pattern: "requestUserLocation|getUserCoords"
---

<objective>
Eliminate every dead button / placeholder / developer artifact that is reachable in the shipped build (Apple 2.1 App Completeness, 4.2 Minimum Functionality), and hide the unfinished growth screens (D-01) without deleting their code, and remove the dev blur test button (D-02).

Purpose: Apple rejects apps with buttons that appear to do something but don't, and developer-only artifacts. This plan removes all of them client-side.

Output: edited profile screens (entry points removed), search button wired, voice search dev-gated, filming dev-blur block + WF badge removed, membership WF badge removed.

This plan is file-disjoint from Plan 03 (help.tsx + Delete-Account entry points + app.config.js) so both Wave-2 plans run in parallel. Plan 03 edits the SAME two profile files for the Delete-Account row — to keep file ownership exclusive, THIS plan owns profile.tsx growth-entry removal AND the Delete-Account row addition is moved to Plan 03 ONLY IF files are split. SINCE profile.tsx is shared, Plan 03 is sequenced to Wave 3 against this plan's profile edits. See wave assignment.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/11-apple-submission-readiness-hide-unfinished-growth-screens-pr/11-RESEARCH.md

<read_first>
- app/(seeker)/profile.tsx — SETTINGS array (lines 12-22) has 'LMC Plus / Pro' (membership) + 'Invite Friends' (invite); referral banner is lines 180-198 (a SECOND entry to /invite)
- app/(scout)/profile.tsx — Refer-a-Scout banner lines 137-155 routes to /(seeker)/invite (the scout-side invite entry point)
- app/(seeker)/search.tsx — dead 'Use my current location' TouchableOpacity (line ~303, no onPress); voice: setVoiceListening(true) at line 296, mock voice fill useEffect lines 207-216, Voice Listening Modal lines 401+
- app/(seeker)/membership.tsx — WF wireframe badge lines 106-111 (router.push('/flow-map'))
- app/(scout)/filming.tsx — DEV-ONLY import block lines 28-34, SHOW_BLUR_TEST const line 52, devBlur state/RPC lines 324-350+, WF badge lines 407-414, dev-blur JSX block lines 606-653
- app/state/location.ts — exports requestUserLocation() (async, prompts + returns {status,coords}), getUserCoords() (sync cached), useUserLocation() hook
- app/(seeker)/home.tsx lines 408-419 — the established pattern for centring on getUserCoords()
</read_first>

<interfaces>
From app/state/location.ts:
```typescript
export function getUserCoords(): Coords | null;            // cached [lon,lat]
export async function requestUserLocation(): Promise<{ status: LocationStatus; coords: Coords | null }>;
export function getUserCity(): string | null;
```
Use requestUserLocation() on tap (prompts if needed), then on a granted result close the search and proceed exactly as a normal location selection does (route to home centred on coords, or call the existing handleSelect with the resolved place). Match home.tsx's usage. Do NOT invent a new geo module — lib/geo.ts does NOT exist.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hide growth-screen entry points (seeker + scout profile)</name>
  <files>app/(seeker)/profile.tsx, app/(scout)/profile.tsx</files>
  <action>
    Seeker (app/(seeker)/profile.tsx):
    - Remove the SETTINGS entry `{ icon: 'star-outline', label: 'LMC Plus / Pro', route: '/(seeker)/membership' }` (line 14).
    - Remove the SETTINGS entry `{ icon: 'people-outline', label: 'Invite Friends', route: '/(seeker)/invite' }` (line 20).
    - Remove the entire Referral Banner block (lines 180-198, the `<View style={styles.referralBanner}>...</View>`) — it is a second entry point to /invite.
    - Leave the referralBanner/referral* styles in the StyleSheet (harmless dead styles; removing them risks churn) OR remove them — either is fine, but do NOT remove styles still referenced elsewhere. Prefer leaving the styles to minimize diff.
    - Do NOT delete membership.tsx or invite.tsx files (D-01: hide, keep for fast-follow).
    Scout (app/(scout)/profile.tsx):
    - Remove the entire Refer-a-Scout referralBanner block (lines 137-155) that routes to /(seeker)/invite.
    - SCOUT_ITEMS routes to /scout/payout, /scout/identity, /legal/code which are fast-follow stubs — leave them (out of scope for this phase; D-01 named membership/referrals/search only). If any of those routes 404 to a blank screen, note it in the SUMMARY for a follow-up, but do NOT change them here.
    Verify both files still compile (no dangling references to removed JSX).
  </action>
  <verify>
    <automated>cd /Users/troyreed/studio/projects/let-me-check/lmc-app && grep -c "/(seeker)/membership\|/(seeker)/invite" "app/(seeker)/profile.tsx" | grep -qx 0 && grep -c "/(seeker)/invite" "app/(scout)/profile.tsx" | grep -qx 0 && echo PASS || echo FAIL</automated>
  </verify>
  <done>Neither profile screen has a reachable route to membership or invite; grep returns 0 for both. (D-01)</done>
</task>

<task type="auto">
  <name>Task 2: Wire the search location button + dev-gate the fake voice search</name>
  <files>app/(seeker)/search.tsx</files>
  <action>
    'Use my current location' button (the TouchableOpacity around line 303):
    - Add an onPress that calls requestUserLocation() (import from '../state/location'). On a granted result with coords, dismiss the search and route the user to home centred on their real location — mirror home.tsx's getUserCoords() centring pattern, e.g. router.replace({ pathname: '/(seeker)/home' }) (home already re-centres on getUserCoords on load). If the user denies, fall back gracefully (keep the screen open; optionally surface getUserCity()). NO Alert that says "in production this would..." — it must do a real thing.
    - Keep the subtitle showing the resolved city/market (it already reads activeMarket — that's fine).
    Voice search (Apple 2.1 — it fakes a result):
    - Gate the voice mic TouchableOpacity (line 296, setVoiceListening) behind `{__DEV__ && (...)}` so it is NOT reachable in Release. The Voice Listening Modal and the mock-fill useEffect (lines 207-216) can stay (they only fire when voiceListening is true, which can no longer happen in Release) — but to be safe, also guard the mock-fill useEffect body with `if (!__DEV__) return;` at its top so no fake query is ever injected in Release.
    Confirm tsc clean.
  </action>
  <verify>
    <automated>cd /Users/troyreed/studio/projects/let-me-check/lmc-app && grep -q "requestUserLocation\|getUserCoords" "app/(seeker)/search.tsx" && grep -q "__DEV__" "app/(seeker)/search.tsx" && echo PASS || echo FAIL</automated>
  </verify>
  <done>The location button performs a real location action; the fake voice search is __DEV__-gated and never injects a mock query in Release. (D-01 search)</done>
</task>

<task type="auto">
  <name>Task 3: Remove the dev blur block + both WF wireframe badges</name>
  <files>app/(scout)/filming.tsx, app/(seeker)/membership.tsx</files>
  <action>
    app/(scout)/filming.tsx — surgical deletion (do NOT touch the surrounding decisionCard JSX, submit/retake buttons, or the live CameraViewfinder):
    - Delete the DEV-ONLY import block (lines 28-34): the `// ---- DEV-ONLY ...` comment, `import { VideoView, useVideoPlayer } from 'expo-video';`, `import { blurFaces } from '../lib/blur-native';`, `import type { BlurResult, BlurMode } from '../lib/blur-native';` and the trailing dashed comment line.
    - Delete the `SHOW_BLUR_TEST` const + its TEMP comment (lines 50-52).
    - Delete the devBlur state + RPC block (the `// ---- DEV-ONLY ...` comment, `devBlurBusy`, `devBlurResult`, `devPlayer`, and the `runDevBlur` async function — lines ~324 through the end of runDevBlur).
    - Delete the WF badge TouchableOpacity (lines 407-414, the `<TouchableOpacity style={styles.wireframeBadge} onPress={() => router.push('/flow-map')}>...WF...</TouchableOpacity>`).
    - Delete the dev-blur JSX block (lines 606-653, the `{SHOW_BLUR_TEST && (...)}` View and its contents incl. the VideoView).
    - If `expo-video` / `blur-native` are now unused elsewhere in the file, the imports are already removed above. Confirm no remaining reference to VideoView, useVideoPlayer, blurFaces, BlurResult, BlurMode, devBlur*, runDevBlur, SHOW_BLUR_TEST, wireframeBadge in filming.tsx.
    - Leave styles.wireframeBadge/wireframeBadgeText in _filming-styles.ts (harmless) OR remove if trivially safe.
    app/(seeker)/membership.tsx:
    - Delete the WF wireframe badge block (lines 106-111, router.push('/flow-map')). Leave the wireframeBadge styles.
    Confirm tsc clean and filming.tsx is now under 500 lines.
  </action>
  <verify>
    <automated>cd /Users/troyreed/studio/projects/let-me-check/lmc-app && test "$(grep -c "SHOW_BLUR_TEST\|runDevBlur\|devBlur\|flow-map" "app/(scout)/filming.tsx")" = 0 && test "$(grep -c "flow-map" "app/(seeker)/membership.tsx")" = 0 && test "$(wc -l < "app/(scout)/filming.tsx")" -lt 500 && echo PASS || echo FAIL</automated>
  </verify>
  <done>filming.tsx has no SHOW_BLUR_TEST/devBlur/runDevBlur/flow-map references and is under 500 lines; membership.tsx has no flow-map WF badge. (D-02 + Apple 2.1)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App Review build -> user | A reviewer can tap any reachable control |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-07 | Information disclosure | WF badge -> /flow-map dev screen | mitigate | Both WF badges removed; /flow-map no longer reachable from shipped screens |
| T-11-08 | Tampering (perception) | hidden screen still reachable via deep link | accept | membership/invite files remain but have NO in-app entry point; deep-link access requires a malicious scheme link a reviewer won't construct; low risk for v1 (D-01 hide-not-delete) |
| T-11-09 | Integrity | dev blur test ships in Release | mitigate | SHOW_BLUR_TEST const + block fully deleted (was force-shown in Release, __DEV__ would not have hidden it) |
</threat_model>

<verification>
- grep: no membership/invite routes from either profile; no flow-map from filming/membership; no SHOW_BLUR_TEST/devBlur in filming.
- search.tsx references requestUserLocation/getUserCoords and __DEV__.
- `cd lmc-app && npx tsc --noEmit` clean.
- filming.tsx under 500 lines.
</verification>

<success_criteria>
- Zero reachable dead buttons / dev artifacts in the seeker + scout flows touched here.
- Growth screens unreachable; their code preserved.
- tsc clean.
</success_criteria>

<output>
After completion, create `.planning/phases/11-apple-submission-readiness-hide-unfinished-growth-screens-pr/11-02-SUMMARY.md`
</output>
