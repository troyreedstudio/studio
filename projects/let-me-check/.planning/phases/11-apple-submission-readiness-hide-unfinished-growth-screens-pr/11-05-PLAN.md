---
phase: 11-apple-submission-readiness
plan: 05
type: execute
wave: 5
depends_on: [11-01, 11-02, 11-03, 11-04]
files_modified:
  - .planning/APPLE-SUBMISSION-CHECKLIST.md
  - .planning/PRIVACY-POLICY-DRAFT.md
  - scripts/seed-demo-account.sql
autonomous: true
requirements: [H-01, H-02, H-03, H-04, H-05, H-06, H-07, H-08, H-09, H-10]
must_haves:
  truths:
    - "Troy has one precise, ordered checklist covering every human step from App Store Connect record to App Review submission"
    - "Troy has a privacy-policy template draft he can review, host, and link (resolving the in-app placeholder URLs)"
    - "Troy has a way to seed a demo Seeker (with a pre-delivered check) + demo Scout for the Apple reviewer"
    - "The checklist includes the exact EAS build + submit commands, APNs/version notes, screenshot sizes, App Privacy labels, and the Stripe real-world-service review-note explanation"
  artifacts:
    - path: ".planning/APPLE-SUBMISSION-CHECKLIST.md"
      provides: "Step-by-step human submission checklist (H-01..H-10) with copy-paste review notes + commands"
    - path: ".planning/PRIVACY-POLICY-DRAFT.md"
      provides: "A reviewable privacy-policy template covering account/location/video/push data + Stripe/Supabase/Mux/Expo processors"
    - path: "scripts/seed-demo-account.sql"
      provides: "SQL to seed a demo Seeker with a delivered check + a demo Scout for the reviewer"
  key_links:
    - from: ".planning/APPLE-SUBMISSION-CHECKLIST.md"
      to: "scripts/seed-demo-account.sql + .planning/PRIVACY-POLICY-DRAFT.md"
      via: "checklist references both as inputs to H-05 and H-06"
      pattern: "seed-demo-account|PRIVACY-POLICY"
---

<objective>
Produce the HUMAN submission package: the precise step-by-step checklist Troy follows in App Store Connect + on his device, plus the two supporting artifacts the checklist needs (a privacy-policy draft to host, and a demo-account seed script for the reviewer). No app/runtime code here, this is the doc-production wave.

Purpose: Everything before this wave makes the build submittable; this wave gives the non-technical human (Troy) an unambiguous path to actually submit, and resolves the two research open-Qs that were "Troy must provide" (privacy policy content + demo account) by giving him a ready draft + a one-shot seed script.

Output: APPLE-SUBMISSION-CHECKLIST.md, PRIVACY-POLICY-DRAFT.md, scripts/seed-demo-account.sql.

Write for a NON-TECHNICAL reader (studio CLAUDE.md rule): plain English first, one idea per line, label any unavoidable command/ID in a single clearly-marked block. No em-dashes in prose. Use "video" not "clip".
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/11-apple-submission-readiness-hide-unfinished-growth-screens-pr/11-RESEARCH.md
@.planning/phases/11-apple-submission-readiness-hide-unfinished-growth-screens-pr/11-CONTEXT.md

<read_first>
- 11-RESEARCH.md sections H-01 through H-10 (the canonical human steps), G (rejection reasons), E (Stripe exemption), F (permissions), and the Assumptions/Open-Questions log
- supabase/migrations/0004_core_entities.sql + 0008_clips_location.sql + 0010_clips_mux.sql + 0011_payments.sql — the exact tables/columns the seed script must populate (checks, clips, payments) for a "delivered" demo check
- supabase/migrations/0006_check_state_machine.sql / 0007_check_transitions.sql — the delivered status + that status is server-only (the seed script writes as service role / SQL editor, bypassing the client transition guard)
- eas.json — appleId, ascAppId 6764298662, appleTeamId YNCLWQN2B8, ascApiKeyPath/Id/Issuer, autoIncrement, production profile (for the exact build+submit commands)
- app.config.js — bundleIdentifier Com.BlackMalibuinc.letmecheck, version 1.0.0, permission strings (post Plan 03: camera + location only, no photo)
</read_first>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Demo-account seed script (resolves open-Q 2)</name>
  <files>scripts/seed-demo-account.sql</files>
  <action>
    Write a SQL script Troy can paste into the Supabase dashboard SQL editor (it runs as a privileged role, bypassing the client-only transition guard). It must:
    - Create / upsert a demo Seeker profile and a demo Scout profile against existing auth.users rows (Troy first signs both demo accounts in via Apple/Google or creates the auth.users rows; the script keys off their uids, which Troy pastes into two clearly-marked variables at the top, e.g. `\set seeker_uid '...'` or a DO block with declared uids). Do NOT hardcode fake uids; use placeholders Troy fills in.
    - Insert ONE delivered check for the demo Seeker: a checks row (status 'delivered', a real venue_id/market_id from the seeded markets, tier 'standard'), a matching clips row (status 'ready' with a playable Mux asset/playback id OR a note that the reviewer sees the delivery UI even if playback needs a real asset), and a payments row (status 'captured' or 'transferred', sensible cents) so History shows a real completed check to replay.
    - Be idempotent (on conflict do nothing / guarded) so Troy can re-run it safely.
    - Include inline comments in plain English explaining each block and exactly what Troy must replace.
    Reference the precise column names from 0004/0008/0010/0011 (read them first). If a real Mux playback id is needed for the video to actually play, say so plainly and tell Troy where it comes from (a previously delivered real check) rather than inventing one.
  </action>
  <verify>
    <automated>cd /Users/troyreed/studio/projects/let-me-check && test -f scripts/seed-demo-account.sql && grep -qi "delivered" scripts/seed-demo-account.sql && grep -qi "payments" scripts/seed-demo-account.sql && echo PASS || echo FAIL</automated>
  </verify>
  <done>A runnable, idempotent, plain-English-commented seed script that gives the reviewer a demo Seeker with a delivered check + a demo Scout, with clearly marked uid placeholders.</done>
</task>

<task type="auto">
  <name>Task 2: Privacy-policy template draft (resolves open-Q 3)</name>
  <files>.planning/PRIVACY-POLICY-DRAFT.md</files>
  <action>
    Write a plain-English privacy-policy draft Troy can review, lightly edit, and host (then paste the URL into App Store Connect + swap the in-app placeholder from Plan 03).
    Cover, in clear sections: what data is collected (name + email from Apple/Google sign-in; precise location for dispatch; device push token; videos filmed/uploaded; payment is processed by Stripe and never touches LMC servers), why each is collected, who it is shared with (processors: Supabase for data, Stripe for payments, Mux for video, Expo for push), how account deletion works (in-app Delete Account removes personal data and anonymizes financial records for legal/reconciliation retention; cite the in-app control added this phase), contact email, and effective date.
    Add a top note: "DRAFT, review with a lawyer before public launch; this satisfies Apple's requirement that a policy exists and is accurate." No em-dashes. This is not legal advice; it is a starting template.
  </action>
  <verify>
    <automated>cd /Users/troyreed/studio/projects/let-me-check && test -f .planning/PRIVACY-POLICY-DRAFT.md && grep -qi "Stripe" .planning/PRIVACY-POLICY-DRAFT.md && grep -qi "delete" .planning/PRIVACY-POLICY-DRAFT.md && echo PASS || echo FAIL</automated>
  </verify>
  <done>A reviewable privacy-policy draft covering all collected data + processors + the account-deletion mechanism, flagged as a draft.</done>
</task>

<task type="auto">
  <name>Task 3: The Apple submission checklist (H-01..H-10)</name>
  <files>.planning/APPLE-SUBMISSION-CHECKLIST.md</files>
  <action>
    Write the ordered, non-technical checklist. Structure it as numbered steps with checkboxes, grouped to match the research H-items, each in plain English with any commands/IDs isolated in a single labelled block:
    1. App Store Connect record (H-01): app name 'Let Me Check', bundle id Com.BlackMalibuinc.letmecheck, primary language English (US), category Lifestyle (secondary Navigation), age rating questionnaire answers (-> 4+), privacy policy URL (link to the hosted PRIVACY-POLICY-DRAFT), support URL/email.
    2. Metadata (H-02): description (do NOT mention membership/referrals which are hidden), keywords, promo text, what's-new, copyright. Note: copy must reflect "video" not "clip".
    3. Screenshots (H-03): one size covers all iPhones, 6.9-inch iPhone 16 Pro Max 1320x2868 portrait, 3-10 shots of the core flow (home/map, venue, payment, waiting, delivery-with-video). Reminder: take them AFTER this phase's code changes so no WF badge / dev section / dead button appears.
    4. App Privacy nutrition labels (H-04): the data-type table (name, email, user id, precise location, device push token, videos). Payment info NOT collected by LMC (Stripe owns it). No photo-library (the permission was removed this phase). No crash data.
    5. Host privacy + terms + support (H-05): host PRIVACY-POLICY-DRAFT, create a terms page + a support page/email, then swap the in-app placeholder URLs in help.tsx (point to where: Plan 03 wired https://lmc.app/privacy + /terms placeholders + mailto help@letmecheck.com).
    6. Demo reviewer account + review notes (H-06): how to create the two demo logins, run scripts/seed-demo-account.sql to give the Seeker a delivered check, and paste the review-notes block. INCLUDE the full review-notes text verbatim (demo logins placeholders, the location-dependent explanation, AND the §3.1.3 real-world-service Stripe-exemption paragraph).
    7. APNs key (H-07): note it is already configured (Phase 10 Vault + eas.json), no action beyond accepting the APNs key prompt during build.
    8. EAS build + submit (H-08): the exact commands from eas.json production profile, in a single labelled block:
         (from lmc-app/)  eas build -p ios --profile production    then    eas submit -p ios --profile production --latest
       Note version 1.0.0, buildNumber auto-increments, credentials come from the .p8 key path in eas.json (no manual entry).
    9. TestFlight first (H-09): distribute to Troy + 2-3 friends to catch device-only permission prompts + launch crashes before App Review.
    10. Stripe LIVE flip (H-10): gated on the Delaware LLC + EIN; the app can ship to TestFlight and even App Review in Stripe TEST mode (reviewer uses the seeded demo, no live money); flip STRIPE keys in Supabase Vault + EAS env when the LLC lands, no code change.
    Add a short "What's already done for you (code)" summary at the top listing the Plan 01-04 outcomes so Troy knows the code side is finished and these are the only remaining human steps.
    Reminder block: Apple is three separate logins (Apple Developer vs Expo vs GitHub); pushing to GitHub is not shipping a build.
  </action>
  <verify>
    <automated>cd /Users/troyreed/studio/projects/let-me-check && test -f .planning/APPLE-SUBMISSION-CHECKLIST.md && grep -q "eas submit" .planning/APPLE-SUBMISSION-CHECKLIST.md && grep -q "1320" .planning/APPLE-SUBMISSION-CHECKLIST.md && grep -qi "3.1.3" .planning/APPLE-SUBMISSION-CHECKLIST.md && grep -qi "seed-demo-account" .planning/APPLE-SUBMISSION-CHECKLIST.md && echo PASS || echo FAIL</automated>
  </verify>
  <done>A complete, ordered, non-technical checklist covering H-01..H-10 with the exact build/submit commands, screenshot sizes, App Privacy labels, the verbatim review-notes block (incl. Stripe §3.1.3 exemption), and references to the seed script + privacy draft.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation -> human action | An incomplete checklist leads to a rejected submission |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-17 | Information disclosure | demo reviewer credentials committed to git | mitigate | Seed script uses placeholders; the checklist tells Troy to keep real passwords out of the repo and put them only in App Store Connect review notes |
| T-11-18 | Integrity | screenshots taken before dev artifacts removed | mitigate | Checklist explicitly says take screenshots only after this phase's code changes |
| T-11-19 | Repudiation | reviewer rejects Stripe as IAP violation | mitigate | Review-notes block includes the §3.1.3 real-world-service exemption explanation verbatim |
</threat_model>

<verification>
- All three files exist.
- Checklist contains: eas submit command, 1320 screenshot size, §3.1.3 note, seed-demo-account reference, App Privacy label table.
- Privacy draft covers Stripe + account deletion.
- Seed script produces a delivered demo check + demo scout, idempotent.
</verification>

<success_criteria>
- Troy can follow one document end-to-end to submit, with the privacy draft + seed script as ready inputs.
- The two research "Troy must provide" open-Qs (privacy content, demo account) are resolved with concrete artifacts.
</success_criteria>

<output>
After completion, create `.planning/phases/11-apple-submission-readiness-hide-unfinished-growth-screens-pr/11-05-SUMMARY.md`
</output>
