---
phase: 11-apple-submission-readiness
plan: 03
type: execute
wave: 3
depends_on: [11-01, 11-02]
files_modified:
  - app/(seeker)/profile.tsx
  - app/(scout)/profile.tsx
  - app/(seeker)/help.tsx
  - app/lib/account.ts
  - app.config.js
autonomous: true
requirements: [D-03, D-05]
must_haves:
  truths:
    - "A Seeker can find and tap a 'Delete Account' control in their profile, confirm a destructive prompt, and have their account deleted"
    - "A Scout can find and tap a 'Delete Account' control in their profile, confirm, and have their account deleted"
    - "After a confirmed deletion the app signs out and returns to the entry screen"
    - "A Seeker can open the in-app Privacy Policy, Terms of Service, and Support contact (they open a real URL / mail, not dead buttons)"
    - "The app no longer declares a photo-library permission it never uses"
  artifacts:
    - path: "app/lib/account.ts"
      provides: "deleteMyAccount() client helper that invokes the delete-account Edge Function (mirrors payments.ts invokeEdgeFunction pattern) then signOut()"
      contains: "delete-account"
    - path: "app/(seeker)/profile.tsx"
      provides: "Delete Account destructive row below Sign Out, two-step Alert confirm -> deleteMyAccount()"
    - path: "app/(scout)/profile.tsx"
      provides: "Delete Account destructive row below Sign Out, same confirm flow"
    - path: "app/(seeker)/help.tsx"
      provides: "Contact rows wired with Linking.openURL (mailto + privacy + terms + support); dev error-state section gated to __DEV__"
    - path: "app.config.js"
      provides: "NSPhotoLibraryUsageDescription removed (no photo-library usage in codebase)"
  key_links:
    - from: "app/(seeker)/profile.tsx + app/(scout)/profile.tsx"
      to: "delete-account Edge Function"
      via: "deleteMyAccount() in app/lib/account.ts"
      pattern: "deleteMyAccount"
    - from: "app/(seeker)/help.tsx"
      to: "hosted privacy/terms/support URLs"
      via: "Linking.openURL"
      pattern: "Linking.openURL"
---

<objective>
Add the user-facing side of account deletion (D-03 entry points + confirm flow), wire the in-app privacy/terms/support links (D-05), and remove the unused photo-library permission. This depends on Plan 01 (the Edge Function must exist to call) and is sequenced after Plan 02 because it edits the same two profile.tsx files (exclusive file ownership across waves).

Purpose: Apple requires the delete-account option to be easy to find (5.1.1(v)) and a working in-app privacy policy link (5.1.1). Requesting an unused permission risks rejection (5.1.1 minimum permissions).

Output: a deleteMyAccount() client helper, Delete-Account rows in both profile screens with a confirm, wired help links, dev section gated, app.config.js photo-lib key removed.

RESOLVED OPEN-Q (verified by codebase grep, no MediaLibrary/CameraRoll/saveToPhotos/ImagePicker anywhere): NSPhotoLibraryUsageDescription is safe to remove.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/11-apple-submission-readiness-hide-unfinished-growth-screens-pr/11-RESEARCH.md
@.planning/phases/11-apple-submission-readiness-hide-unfinished-growth-screens-pr/11-01-PLAN.md

<read_first>
- app/(seeker)/profile.tsx — handleSignOut + Sign Out button; add the destructive Delete Account row AFTER it. Plan 02 already removed the membership/invite entries + referral banner, so read the CURRENT file state.
- app/(scout)/profile.tsx — handleSignOut + Sign Out button; Plan 02 already removed the Refer-a-Scout banner.
- app/(seeker)/help.tsx — CONTACT_OPTIONS (lines 15-20) with email/terms/privacy; contact TouchableOpacity rows (lines 50-63) currently have NO onPress; DEV error-state section (lines 66-85).
- app/lib/payments.ts (lines 25-95) — invokeEdgeFunction(name, body): the EXACT plain-fetch-with-30s-timeout pattern to reuse (NOT supabase.functions.invoke, which hangs in Hermes Release). Copy or export this.
- app/lib/auth.ts — signOut() (line 147); call it after a successful delete.
- app/lib/config.ts — SUPABASE_URL / SUPABASE_ANON_KEY exports used by invokeEdgeFunction.
- app.config.js — infoPlist NSPhotoLibraryUsageDescription (lines 35-36).
</read_first>

<interfaces>
From app/lib/payments.ts (reuse this exact mechanism):
authenticated POST to an Edge Function with a 30s AbortController timeout; uses the session access_token, Authorization Bearer + apikey headers, plain fetch (NOT functions.invoke).
From app/lib/auth.ts: export async function signOut(): Promise<void>
delete-account contract (Plan 01): POST { reason?: string } with the caller's JWT returns 200 { ok: true } | 401 | 500.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: deleteMyAccount() client helper</name>
  <files>app/lib/account.ts</files>
  <action>
    Create app/lib/account.ts exporting async function deleteMyAccount(reason?: string): Promise&lt;void&gt;.
    Reuse the SAME authenticated-fetch-with-30s-timeout mechanism as app/lib/payments.ts invokeEdgeFunction (do NOT use supabase.functions.invoke, it hangs in Hermes Release builds, a logged project lesson). Either export invokeEdgeFunction from payments.ts and import it, or copy the helper into account.ts. Prefer the export approach if it only needs adding an export keyword.
    deleteMyAccount: POST to 'delete-account' with body { reason } (truncate reason to 500 chars); on success call signOut() from '../lib/auth'; let errors propagate so the caller can alert.
    tsc clean.
  </action>
  <verify>
    <automated>cd /Users/troyreed/studio/projects/let-me-check/lmc-app && grep -q "delete-account" app/lib/account.ts && grep -q "deleteMyAccount" app/lib/account.ts && echo PASS || echo FAIL</automated>
  </verify>
  <done>account.ts exports deleteMyAccount that calls the delete-account Edge fn via the Release-safe fetch helper then signs out.</done>
</task>

<task type="auto">
  <name>Task 2: Delete Account entry points in both profile screens</name>
  <files>app/(seeker)/profile.tsx, app/(scout)/profile.tsx</files>
  <action>
    In BOTH profiles, add a destructive 'Delete Account' control AFTER the existing Sign Out button (its own row, red/muted styling, clearly separated; Apple wants it findable, not buried).
    Handler: on tap show a two-step Alert.alert (title 'Delete Account', message that this permanently deletes the account and data and cannot be undone, a cancel button, and a destructive 'Delete' button style: 'destructive'). On confirm call deleteMyAccount() from '../lib/account'; on success router.replace('/index'); on error Alert.alert('Could not delete', message) and stay. Use a busy state so the row shows 'Deleting...' and is disabled during the call.
    Add minimal styles (deleteAccountBtn / deleteAccountText, red ~#ff5a5a) to each StyleSheet. Copy says "data" / "account" plainly; no em-dashes (project copy rule). tsc clean for both files.
  </action>
  <verify>
    <automated>cd /Users/troyreed/studio/projects/let-me-check/lmc-app && grep -q "deleteMyAccount" "app/(seeker)/profile.tsx" && grep -q "deleteMyAccount" "app/(scout)/profile.tsx" && echo PASS || echo FAIL</automated>
  </verify>
  <done>Both profile screens have a findable Delete Account control with a destructive confirm that calls deleteMyAccount() and returns to /index on success. (D-03)</done>
</task>

<task type="auto">
  <name>Task 3: Wire help.tsx links + gate dev section; remove unused photo permission</name>
  <files>app/(seeker)/help.tsx, app.config.js</files>
  <action>
    app/(seeker)/help.tsx:
    - import { Linking } from 'react-native'.
    - Give each CONTACT_OPTIONS row a real action. Add an href to each entry: Email Support -> 'mailto:help@letmecheck.com'; Terms of Service -> 'https://lmc.app/terms' (PLACEHOLDER, Troy swaps when hosted, D-05); Privacy Policy -> 'https://lmc.app/privacy' (PLACEHOLDER); Live Chat can stay informational or fall back to the mailto. Wire onPress to Linking.openURL(href), guarded so a missing href is a no-op.
    - Add a code comment by the two placeholder URLs: PLACEHOLDER, swap for the hosted URL before submission (D-05).
    - Gate the entire 'DEV PREVIEW ERROR STATES' section (the sectionLabel + devList View, lines 66-85) behind {__DEV__ &amp;&amp; (...)} so it is not reachable in Release.
    app.config.js:
    - Remove the NSPhotoLibraryUsageDescription key (lines 35-36) from ios.infoPlist. The app uses no photo-library API (verified). Leave NSCameraUsageDescription, NSLocationWhenInUseUsageDescription, ITSAppUsesNonExemptEncryption untouched. version stays '1.0.0'; buildNumber is EAS-managed.
    tsc clean.
  </action>
  <verify>
    <automated>cd /Users/troyreed/studio/projects/let-me-check/lmc-app && grep -q "Linking.openURL" "app/(seeker)/help.tsx" && grep -q "__DEV__" "app/(seeker)/help.tsx" && test "$(grep -c NSPhotoLibraryUsageDescription app.config.js)" = 0 && echo PASS || echo FAIL</automated>
  </verify>
  <done>Help contact rows open real URLs/mailto; the dev error-state section is __DEV__-gated; NSPhotoLibraryUsageDescription is gone. (D-05 + Apple 5.1.1)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> delete-account Edge fn | User initiates irreversible deletion |
| app -> external URLs | Linking opens hosted privacy/terms/support |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-10 | Tampering | accidental deletion | mitigate | Two-step destructive Alert confirm before deleteMyAccount() is called |
| T-11-11 | Information disclosure | unused photo-library permission prompt | mitigate | NSPhotoLibraryUsageDescription removed (no photo API used) |
| T-11-12 | Spoofing | malicious URL injection in help links | mitigate | URLs are hardcoded constants, not user input; Linking.openURL on a fixed allow-list |
| T-11-13 | Repudiation | delete reason | accept | Reason is optional UX text; the authoritative audit (account_deletions + event_log) is server-side (Plan 01) |
</threat_model>

<verification>
- grep: deleteMyAccount referenced in both profiles; Linking.openURL + __DEV__ in help; no NSPhotoLibraryUsageDescription in app.config.js.
- cd lmc-app && npx tsc --noEmit clean.
</verification>

<success_criteria>
- Delete Account is findable in both profiles and runs the full server delete + sign-out.
- Privacy/terms/support links open real targets.
- No unused permission declared.
- tsc clean.
</success_criteria>

<output>
After completion, create `.planning/phases/11-apple-submission-readiness-hide-unfinished-growth-screens-pr/11-03-SUMMARY.md`
</output>
