---
phase: 11-apple-submission-readiness
plan: 04
type: execute
wave: 4
depends_on: [11-01, 11-02, 11-03]
files_modified:
  - app/lib/database.types.ts
autonomous: true
requirements: [D-03, D-04]
must_haves:
  truths:
    - "Migration 0021 is applied to the live database and the deletion pgTAP test passes GREEN live"
    - "The delete-account Edge Function is deployed live with verify_jwt=TRUE (user-initiated, needs the caller's JWT)"
    - "market_config.dispatch_timeout_s is 300 in the live database (reset from the 3600 testing value)"
    - "database.types.ts reflects the new account_deletions table and delete_my_account RPC"
    - "The whole app type-checks clean (tsc --noEmit) against the regenerated types"
  artifacts:
    - path: "app/lib/database.types.ts"
      provides: "Regenerated Supabase types including account_deletions + delete_my_account"
  key_links:
    - from: "live database"
      to: "migration 0021"
      via: "supabase db push"
      pattern: "db push"
    - from: "live edge runtime"
      to: "delete-account function"
      via: "supabase functions deploy delete-account"
      pattern: "functions deploy"
---

<objective>
[BLOCKING] Take the Plan 01 server work live and reset the testing-only dispatch timeout (D-04). This is the wave that actually makes account deletion work in production and turns the RED tests GREEN.

Purpose: A migration + Edge Function that exist in the repo but are not deployed do nothing for Apple review. This wave pushes them live, verifies the cascade-safe behavior against the live DB, deploys the function with the correct JWT mode, and corrects the dispatch timeout that was bumped to 3600 during testing.

Output: live migration 0021, live delete-account function (verify_jwt=true), dispatch_timeout_s=300 live, regenerated types, tsc clean.

PROJECT LESSON (do not violate): Webhooks (mux-webhook, stripe-webhook, send-push) are deployed --no-verify-jwt (server-to-server). delete-account is the OPPOSITE: it is USER-initiated and must verify the caller's JWT, so deploy it with verify_jwt=TRUE (the default), matching the stripe-refund pattern. This phase must NOT touch any webhook.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/11-apple-submission-readiness-hide-unfinished-growth-screens-pr/11-01-PLAN.md

<read_first>
- supabase/migrations/0021_account_deletion.sql — what gets pushed
- supabase/tests/0021_account_deletion.test.sql — the test to turn GREEN live
- supabase/functions/delete-account/index.ts — what gets deployed
- supabase/migrations/0015_sla_deadline.sql — already sets dispatch_timeout_s=300; the live value may have been manually bumped to 3600 during Phase 7/8 testing (STATE/CONTEXT note)
- STATE.md Phase 09 note — pgTAP runs via `supabase db query --linked` (Docker unavailable on this machine), and 0017 needed `--include-all` because of remote history ordering; expect the same here
</read_first>

<commands>
Linked project ref: cawqasszfbzvbtunamda (from app.config.js supabaseUrl).
- Push migration: `cd /Users/troyreed/studio/projects/let-me-check && supabase db push --include-all` (use --include-all if the remote history rejects a plain push, per the 0017 lesson).
- Run pgTAP live (Docker-free path): `supabase db query --linked` with the test SQL, OR `supabase test db` if Docker is available. The 0021 test must report all assertions passing.
- Deploy function (USER-initiated, verify JWT): `supabase functions deploy delete-account` (verify_jwt defaults TRUE; do NOT pass --no-verify-jwt).
- Reset dispatch timeout: check then fix:
    `supabase db query --linked "SELECT dispatch_timeout_s FROM public.market_config LIMIT 1;"`
    if not 300: `supabase db query --linked "UPDATE public.market_config SET dispatch_timeout_s = 300;"`
- Regen types: `supabase gen types typescript --linked > lmc-app/app/lib/database.types.ts`
</commands>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Push migration 0021 live + turn the deletion pgTAP GREEN</name>
  <files>supabase/migrations/0021_account_deletion.sql</files>
  <action>
    From the project root, push migration 0021 to the live DB (`supabase db push`; if the remote history rejects it, retry with `--include-all` per the 0017 lesson). 0021 is additive + idempotent (CREATE TABLE IF NOT EXISTS / CREATE OR REPLACE FUNCTION / on-conflict sentinel insert), so a re-push is safe.
    Then run the 0021 pgTAP assertions against the live DB. If Docker is available use `supabase test db`; otherwise run the test SQL via `supabase db query --linked` (the 0017/0009 Docker-free path) and confirm every assertion passes. The financial-anonymize assertion (payments row survives, parent check seeker_id = sentinel) and the no-FK-violation assertion are the critical ones.
    If any assertion fails, fix 0021 (re-push, idempotent) until GREEN. Do NOT mark done until the deletion behavior is verified live.
  </action>
  <verify>
    <automated>cd /Users/troyreed/studio/projects/let-me-check && supabase db query --linked "SELECT to_regclass('public.account_deletions') IS NOT NULL AS t, to_regprocedure('public.delete_my_account(text)') IS NOT NULL AS f;" 2>&1 | tail -5</automated>
  </verify>
  <done>0021 is live (account_deletions table + delete_my_account RPC exist) and the pgTAP deletion assertions pass GREEN against the live DB.</done>
</task>

<task type="auto">
  <name>Task 2: Deploy delete-account (verify_jwt=TRUE) + smoke test</name>
  <files>supabase/functions/delete-account/index.ts</files>
  <action>
    Deploy the function user-initiated: `supabase functions deploy delete-account` (verify_jwt defaults TRUE; do NOT pass --no-verify-jwt). Confirm the deploy succeeds.
    Smoke test the auth gate (without actually deleting a real account): a POST with NO Authorization header must return 401 (verify_jwt rejects). Example:
      `curl -s -o /dev/null -w "%{http_code}" -X POST https://cawqasszfbzvbtunamda.supabase.co/functions/v1/delete-account` -> expect 401.
    Do NOT run a real authenticated delete here (that destroys a user); the real authed delete is exercised on-device by Troy via the demo account in Plan 05's checklist.
    Do NOT redeploy or touch any webhook (mux-webhook/stripe-webhook/send-push keep --no-verify-jwt; this phase must not alter them).
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}\n" -X POST https://cawqasszfbzvbtunamda.supabase.co/functions/v1/delete-account</automated>
  </verify>
  <done>delete-account is deployed with verify_jwt=true; an unauthenticated POST returns 401. No webhook was touched.</done>
</task>

<task type="auto">
  <name>Task 3: Reset dispatch_timeout_s to 300 + regen types + tsc</name>
  <files>app/lib/database.types.ts</files>
  <action>
    Reset D-04: query the live value `SELECT dispatch_timeout_s FROM public.market_config LIMIT 1;`. If it is not 300, run `UPDATE public.market_config SET dispatch_timeout_s = 300;` (it may be 3600 from testing). Confirm it reads 300 after.
    Regenerate types so the client knows about account_deletions + delete_my_account: `supabase gen types typescript --linked > lmc-app/app/lib/database.types.ts`.
    Run the full type check: `cd lmc-app && npx tsc --noEmit`. Resolve any type errors introduced by the regen (e.g. the account.ts RPC/insert typings). It must be clean.
  </action>
  <verify>
    <automated>cd /Users/troyreed/studio/projects/let-me-check && supabase db query --linked "SELECT dispatch_timeout_s FROM public.market_config LIMIT 1;" 2>&1 | grep -q 300 && cd lmc-app && npx tsc --noEmit && echo "TSC-CLEAN-AND-300"</automated>
  </verify>
  <done>dispatch_timeout_s is 300 live; database.types.ts regenerated; tsc --noEmit clean across the app. (D-04)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deploy pipeline -> live runtime | A misconfigured JWT mode could expose or break deletion |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-14 | Elevation of privilege | delete-account deployed --no-verify-jwt by mistake | mitigate | Deploy with default verify_jwt=TRUE; smoke test asserts unauthenticated POST returns 401 |
| T-11-15 | Denial of service | accidental webhook redeploy flips its JWT mode | mitigate | This phase deploys ONLY delete-account; no webhook is touched (explicit constraint) |
| T-11-16 | Tampering | dispatch_timeout left at 3600 -> Seekers wait too long in prod | mitigate | D-04 reset to 300 verified live by query |
</threat_model>

<verification>
- Live: account_deletions + delete_my_account exist; pgTAP GREEN.
- Live: unauthenticated delete-account POST returns 401 (verify_jwt on).
- Live: dispatch_timeout_s = 300.
- tsc --noEmit clean after type regen.
</verification>

<success_criteria>
- Account deletion works end-to-end live (server side verified).
- Correct JWT mode; no webhook disturbed.
- D-04 timeout reset; types regenerated; app type-clean.
</success_criteria>

<output>
After completion, create `.planning/phases/11-apple-submission-readiness-hide-unfinished-growth-screens-pr/11-04-SUMMARY.md`
</output>
