---
phase: 03-video-pipeline
plan: 04
type: execute
wave: 2
depends_on: [01, 02, 03]
files_modified:
  - lmc-app/app/lib/database.types.ts
autonomous: false
requirements: [VID-03, VID-04, CHECK-04]
user_setup:
  - service: mux
    why: "Troy must create the Mux account and provide API token + signing key before the functions can run live"
    env_vars:
      - name: MUX_TOKEN_ID
        source: "Mux Dashboard -> Settings -> API Access Tokens"
      - name: MUX_TOKEN_SECRET
        source: "Mux Dashboard -> Settings -> API Access Tokens"
      - name: MUX_WEBHOOK_SECRET
        source: "Mux Dashboard -> Settings -> Webhooks (signing secret)"
      - name: MUX_SIGNING_KEY_ID
        source: "Mux Dashboard -> Settings -> Signing Keys"
      - name: MUX_SIGNING_PRIVATE_KEY
        source: "Mux Dashboard -> Settings -> Signing Keys (base64 private key)"
    dashboard_config:
      - task: "Register the webhook URL at the deployed mux-webhook function; subscribe to video.asset.ready + video.asset.errored"
        location: "Mux Dashboard -> Settings -> Webhooks"
      - task: "Create a signing key for signed playback"
        location: "Mux Dashboard -> Settings -> Signing Keys"

must_haves:
  truths:
    - "Migration 0010 is applied to the live Supabase project (clips Mux columns + new edges + ready-guard + service-actor)"
    - "The 3 Edge Functions are deployed live with all MUX_* secrets set"
    - "The Mux webhook is registered in the Mux dashboard pointing at the deployed mux-webhook function"
    - "database.types.ts is regenerated and contains the new clips Mux columns"
    - "A fresh EAS dev build containing react-native-vision-camera exists for on-device capture"
    - "The full automated suite (Vitest + pgTAP + Deno) is green against the live project"
  artifacts:
    - path: "lmc-app/app/lib/database.types.ts"
      provides: "Regenerated types incl. mux_playback_id / mux_asset_id / duration_secs on clips"
      contains: "mux_playback_id"
  key_links:
    - from: "Mux dashboard webhook"
      to: "deployed mux-webhook function URL"
      via: "registered webhook subscription"
      pattern: "mux-webhook"
---

<objective>
Deploy the entire server side of the phase to the LIVE Supabase project and stand up the Mux account, then regenerate types and cut a fresh native dev build. This is the [BLOCKING] gate: build/tsc pass WITHOUT the deploy, so without this plan the phase would falsely look complete. It is `autonomous: false` because it pushes to live infrastructure, needs Troy's Mux account, and needs secrets only Troy can supply.

Purpose: 03-01/02/03 author and offline-verify everything (migration, Edge Functions, client lib). Nothing is real until 0010 is pushed, the functions are deployed with secrets, the Mux webhook is registered, types are regenerated (so the client type-checks against the Mux columns), and a fresh EAS build carries the native camera. The live project is `cawqasszfbzvbtunamda` (already linked from Phase 1/2).

Output: live 0010 + deployed functions + Mux dashboard wired + regenerated `database.types.ts` + a fresh dev build.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-video-pipeline/03-RESEARCH.md

<interfaces>
Live project ref: `cawqasszfbzvbtunamda` (linked). Phase-2 migrations 0007-0009 must already be pushed (the 02-02 [BLOCKING] checkpoint) — confirm before pushing 0010, which depends on the 0007 functions and 0008 clips table.

Edge-Function secrets to set (NEVER committed):
MUX_TOKEN_ID, MUX_TOKEN_SECRET, MUX_WEBHOOK_SECRET, MUX_SIGNING_KEY_ID, MUX_SIGNING_PRIVATE_KEY
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Troy creates the Mux account + supplies secrets</name>
  <action>Pause for Troy to create the Mux account and supply the five MUX_* secret values (or set them himself via supabase secrets set). Guy does not proceed until they are available.</action>
  <what-built>The 3 Edge Functions (03-02) need a live Mux account: an API access token, a webhook signing secret, and a signing key for signed playback. Only Troy can create the account.</what-built>
  <how-to-verify>
    1. Create a Mux account (mux.com).
    2. Settings -> API Access Tokens -> create a token -> note MUX_TOKEN_ID + MUX_TOKEN_SECRET.
    3. Settings -> Signing Keys -> create a key -> note MUX_SIGNING_KEY_ID + the base64 MUX_SIGNING_PRIVATE_KEY.
    4. Settings -> Webhooks -> we will register the URL in Task 4 AFTER the function deploys; for now note the signing secret -> MUX_WEBHOOK_SECRET.
    5. Paste the five values to Guy (or set them yourself via `supabase secrets set`). Do NOT commit them.
  </how-to-verify>
  <resume-signal>Reply "mux ready" with the five secret values (or confirm you've set them), then Guy proceeds.</resume-signal>
  <verify>
    <automated>MISSING — human action (external account). Verified downstream by functions returning 200 in Task 4.</automated>
  </verify>
  <done>Mux account exists; the five MUX_* values are available to set as Edge-Function secrets.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [BLOCKING] Push 0010 live + deploy the 3 Edge Functions + set secrets</name>
  <action>After 0007-0009 are confirmed live, run supabase db push (applies 0010), supabase secrets set for the five MUX_* values, and supabase functions deploy for the three functions. Confirm via supabase functions list.</action>
  <what-built>Migration 0010 and the three Edge Functions, authored and offline-verified in 03-01/02. This applies them to the live project.</what-built>
  <how-to-verify>
Guy runs (after confirming 0007-0009 are already live from 02-02):
```bash
cd /Users/troyreed/studio/projects/let-me-check
supabase db push                                   # applies 0010 to cawqasszfbzvbtunamda
supabase secrets set MUX_TOKEN_ID=... MUX_TOKEN_SECRET=... MUX_WEBHOOK_SECRET=... \
  MUX_SIGNING_KEY_ID=... MUX_SIGNING_PRIVATE_KEY=...
supabase functions deploy mux-upload-url mux-webhook mux-playback-token
```
Confirm: `supabase functions list` shows all three; `supabase db push` reported 0010 applied. If `supabase` is not logged in / not linked, authenticate first (`supabase login`, `supabase link --project-ref cawqasszfbzvbtunamda`).
  </how-to-verify>
  <resume-signal>Reply "deployed" (or paste any error from db push / functions deploy).</resume-signal>
  <verify>
    <automated>supabase functions list 2>/dev/null | grep -q "mux-webhook" && echo OK</automated>
  </verify>
  <done>0010 is live; the three functions are deployed with secrets set.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Regenerate database.types.ts from the live schema + run the live test suite</name>
  <action>Run supabase gen types typescript --linked into lmc-app/app/lib/database.types.ts, then run tsc, supabase test db (pgTAP), deno test on the functions, and vitest. All must be green against the live project.</action>
  <what-built>The generated types file is stale until 0010 lands; lib/clips.ts and the screens type-check against the new Mux columns.</what-built>
  <how-to-verify>
```bash
cd /Users/troyreed/studio/projects/let-me-check
supabase gen types typescript --linked > lmc-app/app/lib/database.types.ts
cd lmc-app && npx tsc --noEmit                      # must pass with the new columns
cd .. && supabase test db                           # pgTAP incl. clips_mux.test.sql
deno test --allow-env supabase/functions/           # Edge Function logic
cd lmc-app && npx vitest run                         # client units incl. clips
```
All must be green.
  </how-to-verify>
  <resume-signal>Reply "types + tests green" (or paste failures).</resume-signal>
  <verify>
    <automated>grep -q "mux_playback_id" lmc-app/app/lib/database.types.ts && cd lmc-app && npx tsc --noEmit && echo OK</automated>
  </verify>
  <done>Types contain the Mux columns; tsc + pgTAP + Deno + Vitest are all green against the live project.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: Register the Mux webhook + cut a fresh EAS dev build (native camera)</name>
  <action>Register the deployed mux-webhook function URL in the Mux dashboard (subscribe to video.asset.ready + video.asset.errored), reconcile MUX_WEBHOOK_SECRET, then run eas build -p ios --profile development and install the build on Troy device.</action>
  <what-built>The webhook can only be registered once the function has a live URL; and react-native-vision-camera is a NATIVE dep absent from Build 9, so on-device capture needs a fresh build.</what-built>
  <how-to-verify>
    1. Webhook: in the Mux dashboard, register the webhook URL = the deployed mux-webhook function URL (`https://cawqasszfbzvbtunamda.supabase.co/functions/v1/mux-webhook`), subscribe to `video.asset.ready` and `video.asset.errored`. Confirm the signing secret matches MUX_WEBHOOK_SECRET (re-set the secret if Mux generated a new one).
    2. Fresh dev build (the camera cannot run on a simulator or on Build 9):
```bash
cd lmc-app && eas build -p ios --profile development
```
    3. Install the new build on Troy's device (needed for the 03-05 on-device walk-through).
  </how-to-verify>
  <resume-signal>Reply "webhook registered + build N installed".</resume-signal>
  <verify>
    <automated>MISSING — external dashboard + EAS build. Confirmed by the 03-05 on-device walk-through (a Mux asset.ready event reaching the live webhook flips the check to delivered).</automated>
  </verify>
  <done>The Mux webhook points at the live function; a fresh dev build with the native camera is on Troy's device.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local repo -> live Supabase | A live schema/function push is irreversible-ish; gate it behind human confirmation. |
| secrets -> Edge Function env | Secrets are set via `supabase secrets set`, never committed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-11 | Tampering | false-positive "phase complete" without a live deploy | mitigate | this [BLOCKING] autonomous:false plan is the gate; build/tsc pass without it, so the gate is the only proof the server side is real. |
| T-03-12 | Information Disclosure | secrets committed to git | mitigate | secrets set via CLI only; no MUX_* literal in repo (asserted by 03-03 invariants + 03-02 grep gate). |
</threat_model>

<verification>
- `supabase functions list` shows the three functions.
- `database.types.ts` contains `mux_playback_id`; tsc passes.
- pgTAP (clips_mux) + Deno + Vitest green against live.
- Mux webhook registered; fresh dev build installed.
</verification>

<success_criteria>
- The full server side is LIVE and the live automated suite is green.
- Types regenerated; native build ready for the on-device walk-through.
</success_criteria>

<output>
After completion, create `.planning/phases/03-video-pipeline/03-04-SUMMARY.md`.
</output>
