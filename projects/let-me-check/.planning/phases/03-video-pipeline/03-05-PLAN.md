---
phase: 03-video-pipeline
plan: 05
type: execute
wave: 3
depends_on: [03, 04]
files_modified:
  - lmc-app/app/(scout)/filming.tsx
  - lmc-app/app/(scout)/submitted.tsx
  - lmc-app/app/(seeker)/delivery.tsx
  - lmc-app/app/lib/clips.ts
autonomous: false
requirements: [VID-01, VID-02, VID-04, CHECK-04]
user_setup: []

must_haves:
  truths:
    - "A Scout films a live 15-second clip in-app from the real camera; recording auto-stops at 15s"
    - "No gallery/camera-roll import path exists; the only clip source is the live recorder's onRecordingFinished path"
    - "Audio is never recorded (audio={false}); no mic permission prompt appears"
    - "On submit, the clip uploads to Mux via the signed URL with resilient retry; the screen shows processing and does NOT mark delivered"
    - "The Seeker's screen flips to delivered via the existing Realtime subscription when the webhook lands"
    - "delivery.tsx plays the real signed Mux HLS stream; a second account cannot watch it"
    - "filming.tsx stays under 500 lines — recorder/upload orchestration lives in lib/clips.ts"
  artifacts:
    - path: "lmc-app/app/(scout)/filming.tsx"
      provides: "Real vision-camera capture (audio off, 15s), markFilming retained, submit -> requestUploadUrl + uploadWithRetry via the lib helper"
      contains: "react-native-vision-camera"
    - path: "lmc-app/app/lib/clips.ts"
      provides: "recordAndUpload / useClipUpload — the extracted recorder+upload orchestration the thin screen calls"
      contains: "uploadWithRetry"
    - path: "lmc-app/app/(seeker)/delivery.tsx"
      provides: "Real expo-video player on the signed Mux HLS URL"
      contains: "useVideoPlayer"
  key_links:
    - from: "lmc-app/app/(scout)/filming.tsx"
      to: "lib/clips.ts uploadWithRetry"
      via: "submit handler after onRecordingFinished"
      pattern: "uploadWithRetry|recordAndUpload|useClipUpload"
    - from: "lmc-app/app/(seeker)/delivery.tsx"
      to: "stream.mux.com HLS + getPlaybackToken"
      via: "useVideoPlayer fed the signed URL"
      pattern: "stream.mux.com"
---

<objective>
Wire the three prototype screens to the real pipeline (brownfield — extend, don't rebuild). Replace `filming.tsx`'s simulated viewfinder + fake upload animation with a real `<Camera audio={false}>` capture and a real Mux upload via `lib/clips.ts`; keep the existing visual chrome and the `markFilming` call. Replace `delivery.tsx`'s placeholder play button with a real `expo-video` player on the signed Mux HLS stream. Reflect real upload/processing status on `submitted.tsx` (no earnings — Phase 4). Ends with the on-device walk-through checkpoint.

Purpose: This is the payoff — a genuine, audio-free clip the Seeker can actually watch. The state flow itself is unchanged from Phase 2 (the existing Realtime subscription already routes waiting->delivery on the `delivered` UPDATE); only WHAT fills the clip and WHAT the screen plays changes. The webhook (03-02) owns the `delivered` transition, so the client's job ends at "upload succeeded -> show processing". `filming.tsx` is ~1290 lines today — the single biggest churn risk in the phase. To land it under 500 lines safely, this plan does the extraction FIRST (Task 1a, mechanical, drops the line count) and only THEN wires the real camera against a now-thin screen (Task 1b). The illegal client `markDelivered` wrapper itself was already removed in 03-03 Task 2 — AND 03-03 Task 2 already removed filming.tsx's dangling `markDelivered` import + call (replacing the call with a temporary no-op + a `// TODO(03-05)` breadcrumb) so the Wave-2 full-app tsc passes. So filming.tsx arrives at this plan ALREADY markDelivered-free; this plan does NOT need to touch the import again — it replaces the temporary no-op + the fake upload `setInterval` block with the real lib helper + real camera.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-video-pipeline/03-RESEARCH.md
@.planning/phases/03-video-pipeline/03-VALIDATION.md

<interfaces>
<!-- The exact existing seams in the three screens (read live; do not rebuild). -->

From (scout)/filming.tsx (state AFTER 03-03 Task 2 — markDelivered already removed):
- `import { markFilming } from '../lib/checks';` — markDelivered was ALREADY dropped from this import in 03-03 Task 2. KEEP markFilming. Do NOT re-remove the import (Wave 1 did it).
- `handleToggleRecord` (~line 97) fires `markFilming(checkId)` once via the `filmingMarked` ref on record-start — KEEP.
- `handleSubmit` (~line 112) currently does `setUploading(true)` then a `// TODO(03-05)` no-op (the markDelivered call was removed in Wave 1). It still flips into the existing FAKE upload `setInterval` block (~lines 124-157) that fakes `setUploadPct`/`setUploadStage` and routes to /(scout)/submitted — REPLACE the no-op + fake animation with: requestUploadUrl(checkId) -> uploadWithRetry(localPath, uploadUrl, onProgress) -> on success route to submitted with status 'processing'. NEVER transition to delivered.
- The fake upload progression is the `useEffect` at ~lines 126-157 whose `setInterval` advances `setUploadPct` / `setUploadStage` and finally `router.replace('/(scout)/submitted')`. The TWO OTHER setIntervals in this file are LEGITIMATE countdowns that MUST stay: the delivery countdown (~line 66, `setSecondsLeft`) and the 15s recording auto-stop (~line 84, `setRecordSecs`). Only the fake-upload-progress interval (the one driving `setUploadPct`/`setUploadStage`) is removed.
- `CameraViewfinder` (~line 471) is the simulated dark-placeholder modal — swap its placeholder feed for a real `<Camera>` preview; keep the REC pill / GPS pill / 15s ring chrome.

From (seeker)/delivery.tsx:
- `import { getCheck, getCheckClip, rateCheck } from '../lib/checks';` — clip now carries mux_playback_id + duration_secs.
- Video Placeholder block (~lines 84-98): `<View styles.videoBox>` with a fake play button — REPLACE with a real expo-video VideoView fed `https://stream.mux.com/${clip.mux_playback_id}.m3u8?token=${token}` where token = await getPlaybackToken(checkId). KEEP the AI VERDICT line + crowd tags STATIC (Phase 6). KEEP the rating block.

vision-camera (research Pattern 1):
```tsx
import { Camera, useCameraDevice, useCameraPermission } from 'react-native-vision-camera';
<Camera ref video={true} audio={false} isActive device={device} />
cam.startRecording({ onRecordingFinished: v => handle(v.path, v.duration), onRecordingError });
setTimeout(() => cam.stopRecording(), 15_000);   // 15s cap
```

expo-video (already used by venue.tsx, research Pattern 5):
```tsx
import { useVideoPlayer, VideoView } from 'expo-video';
const player = useVideoPlayer(src, p => { p.loop = false; });
<VideoView player={player} style={...} allowsFullscreen contentFit="cover" />
```

lib/clips.ts (03-03): requestUploadUrl, uploadWithRetry, getPlaybackToken — plus the recorder+upload orchestration (recordAndUpload / useClipUpload) this plan adds in Task 1a.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1a: Extract the recorder + upload orchestration out of filming.tsx into lib/clips.ts (mechanical — drops the line count first)</name>
  <read_first>
    - lmc-app/app/(scout)/filming.tsx (FULL — handleSubmit's `// TODO(03-05)` no-op + the fake upload `setInterval` useEffect ~lines 124-157 that drives `setUploadPct`/`setUploadStage` then `router.replace('/(scout)/submitted')`, and the upload-progress state/UI it drives; identify exactly what is orchestration vs presentation. The delivery-countdown setInterval ~line 66 and the 15s recording-auto-stop setInterval ~line 84 are LEGITIMATE — leave them.)
    - lmc-app/app/lib/clips.ts (requestUploadUrl, uploadWithRetry, getPlaybackToken from 03-03 — the seam the extracted helper composes)
    - .planning/phases/03-video-pipeline/03-RESEARCH.md Open Question 3 (extract logic to keep the screen <500 lines)
  </read_first>
  <action>
This task is purely the mechanical extraction — it does NOT yet add the real camera. The goal is to move the upload orchestration OUT of the screen so Task 1b wires the camera against a thin file.
   - In `lmc-app/app/lib/clips.ts`, add a `useClipUpload()` hook (or a `recordAndUpload`/`submitClip` helper) that owns the submit orchestration: given `(checkId, localPath, onProgress?)`, it calls `requestUploadUrl(checkId)` then `uploadWithRetry(localPath, uploadUrl, 4, onProgress)`, exposes `{ progress, status: 'idle'|'uploading'|'processing'|'error', submit, reset }`, and NEVER transitions the check (no delivered). This is the logic currently faked in filming.tsx's `handleSubmit` no-op + the fake upload-progress `setInterval` block, made real and testable in lib.
   - In `(scout)/filming.tsx`, REPLACE the inline FAKE upload-progress `setInterval` useEffect (the one driving `setUploadPct`/`setUploadStage` and finally `router.replace('/(scout)/submitted')`, ~lines 124-157) with a call into the new lib helper, driving the existing progress UI from the helper's `progress`/`status`. Do NOT add the camera yet — keep the existing simulated viewfinder for this task. Do NOT touch the legit countdown setIntervals at ~line 66 (delivery countdown) and ~line 84 (15s recording auto-stop) — they stay. The `markDelivered` import + call were ALREADY removed in 03-03 Task 2, so there is nothing to remove here for that (just confirm it's still absent).
   - This must measurably DROP filming.tsx's line count (the orchestration now lives in lib). Target: filming.tsx already under 500 lines at the end of THIS task, before the camera is added.
  </action>
  <acceptance_criteria>
    - The orchestration lives in lib: `grep -q "useClipUpload\|recordAndUpload\|submitClip" lmc-app/app/lib/clips.ts`
    - filming.tsx calls it: `grep -q "useClipUpload\|recordAndUpload\|submitClip" "lmc-app/app/(scout)/filming.tsx"`
    - the FAKE upload-progress block is gone (assert the fake-progress state setters that drove it are no longer present — these are the symbols unique to the fake animation, NOT the two legit countdown setIntervals): `! grep -q "setUploadPct" "lmc-app/app/(scout)/filming.tsx" && ! grep -q "setUploadStage" "lmc-app/app/(scout)/filming.tsx"`
    - the two legit countdown setIntervals still exist (the gate must NOT have removed them): `grep -q "setSecondsLeft" "lmc-app/app/(scout)/filming.tsx" && grep -q "setRecordSecs" "lmc-app/app/(scout)/filming.tsx"`
    - markDelivered still absent (removed in Wave 1): `! grep -q "markDelivered" "lmc-app/app/(scout)/filming.tsx"`
    - filming.tsx already under 500 lines after the extraction: `[ $(wc -l < "lmc-app/app/(scout)/filming.tsx") -lt 500 ]`
    - lib clips tests still green + tsc: `cd lmc-app && npx vitest run app/lib/clips.test.ts && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>grep -q "useClipUpload\|recordAndUpload\|submitClip" lmc-app/app/lib/clips.ts && grep -q "useClipUpload\|recordAndUpload\|submitClip" "lmc-app/app/(scout)/filming.tsx" && ! grep -q "setUploadPct" "lmc-app/app/(scout)/filming.tsx" && ! grep -q "setUploadStage" "lmc-app/app/(scout)/filming.tsx" && grep -q "setSecondsLeft" "lmc-app/app/(scout)/filming.tsx" && grep -q "setRecordSecs" "lmc-app/app/(scout)/filming.tsx" && ! grep -q "markDelivered" "lmc-app/app/(scout)/filming.tsx" && [ $(wc -l < "lmc-app/app/(scout)/filming.tsx") -lt 500 ] && cd lmc-app && npx vitest run app/lib/clips.test.ts && npx tsc --noEmit && echo OK</automated>
  </verify>
  <done>The upload orchestration is extracted into lib/clips.ts; filming.tsx calls the lib helper instead of the fake upload-progress setInterval (the fake-progress setters setUploadPct/setUploadStage are gone, while the two legit countdown setIntervals remain), still has no markDelivered, and is already under 500 lines — the camera wiring (1b) now lands against a thin file.</done>
</task>

<task type="auto">
  <name>Task 1b: Wire filming.tsx — real vision-camera capture (audio off, 15s, fresh-only) against the now-thin screen</name>
  <read_first>
    - lmc-app/app/(scout)/filming.tsx (the post-1a thin screen — handleToggleRecord/markFilming, the lib-helper submit, CameraViewfinder chrome)
    - lmc-app/app/lib/clips.ts (the useClipUpload/recordAndUpload helper from 1a; requestUploadUrl, uploadWithRetry)
    - .planning/phases/03-video-pipeline/03-RESEARCH.md Pattern 1 (vision-camera capture) + Open Question 4 (capture GPS stamp onto the clip via expo-location, do NOT verify)
  </read_first>
  <action>
Now add the REAL camera to the thin screen (presentation only — orchestration already lives in lib from 1a):
   - Add `react-native-vision-camera` imports; request camera permission (`useCameraPermission`) on mount; pick the back device (`useCameraDevice('back')`).
   - In `CameraViewfinder`, render a real `<Camera ref video={true} audio={false} isActive device={device} style={StyleSheet.absoluteFill} />` UNDER the existing REC/GPS/15s-ring chrome. Remove only the simulated dark-placeholder feed.
   - On record start: keep the existing `markFilming(checkId)` call (via the `filmingMarked` ref). Call `cam.startRecording({ onRecordingFinished: v => setCapturedPath(v.path), onRecordingError })`. Enforce the 15s cap with `setTimeout(() => cam.stopRecording(), 15000)` driven by the existing 15s timer.
   - Capture the GPS stamp at record time via `expo-location` (already a dependency) into local state `{lat,lng}` — passed to the upload so the clip provenance trail can hold it. Do NOT verify/reject on it (Phase 5).
   - Submit feeds the captured real path into the lib `useClipUpload`/`recordAndUpload` helper from 1a; on success `router.replace({ pathname: '/(scout)/submitted', params: { checkId, tier } })`; on failure drop out of uploading so the Scout can retry. NEVER mark delivered.
   - There must be NO gallery / image-picker / "choose from library" affordance anywhere. filming.tsx stays under 500 lines.
  </action>
  <acceptance_criteria>
    - `grep -q "react-native-vision-camera" "lmc-app/app/(scout)/filming.tsx"`
    - `grep -q "audio={false}" "lmc-app/app/(scout)/filming.tsx"`
    - submit still routes through the lib helper: `grep -q "useClipUpload\|recordAndUpload\|submitClip\|uploadWithRetry" "lmc-app/app/(scout)/filming.tsx"`
    - markDelivered still absent: `! grep -q "markDelivered" "lmc-app/app/(scout)/filming.tsx"`
    - filming.tsx under 500 lines: `[ $(wc -l < "lmc-app/app/(scout)/filming.tsx") -lt 500 ]`
    - No gallery path / no client-delivered: `bash lmc-app/scripts/check-video-invariants.sh` exits 0
    - `cd lmc-app && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>grep -q "audio={false}" "lmc-app/app/(scout)/filming.tsx" && ! grep -q "markDelivered" "lmc-app/app/(scout)/filming.tsx" && [ $(wc -l < "lmc-app/app/(scout)/filming.tsx") -lt 500 ] && bash lmc-app/scripts/check-video-invariants.sh && cd lmc-app && npx tsc --noEmit && echo OK</automated>
  </verify>
  <done>filming.tsx records a real audio-free 15s clip and submits it through the lib upload helper, never marks delivered, has no gallery path, and is under 500 lines.</done>
</task>

<task type="auto">
  <name>Task 2: Wire delivery.tsx — real signed Mux player + light submitted.tsx status</name>
  <read_first>
    - lmc-app/app/(seeker)/delivery.tsx (FULL — the getCheckClip read, the Video Placeholder block ~84-98, the AI VERDICT + rating blocks to keep)
    - lmc-app/app/(seeker)/venue.tsx (how useVideoPlayer/VideoView is already used in this app — mirror it)
    - lmc-app/app/lib/clips.ts getPlaybackToken (03-03)
    - lmc-app/app/(scout)/submitted.tsx (the de-moneyed success screen — reflect upload/processing, keep // TODO(phase-4), no earnings)
  </read_first>
  <action>
Edit `(seeker)/delivery.tsx`:
   - After loading the clip (`getCheckClip`), if `clip.mux_playback_id` exists, `const token = await getPlaybackToken(checkId)` and build `const src = ` followed by the template `https://stream.mux.com/${clip.mux_playback_id}.m3u8?token=${token}`.
   - Replace the Video Placeholder `<View styles.videoBox>` + fake play button with `useVideoPlayer(src)` + `<VideoView player={player} style={styles.videoBox} allowsFullscreen contentFit="cover" />` (mirror venue.tsx's usage). Show a small "processing" state if the clip isn't ready yet (no playback id).
   - KEEP the AI VERDICT line, the CROWD REPORT tags (STATIC — Phase 6), the filmed-time line, and the rating block unchanged.

Edit `(scout)/submitted.tsx` (LIGHT touch): reflect real upload/processing status if available (e.g. "Uploaded — processing" vs the prior copy); KEEP the `// TODO(phase-4)` discipline and add NO earnings credit. Do not change any money/earnings logic.
  </action>
  <acceptance_criteria>
    - `grep -q "useVideoPlayer" lmc-app/app/(seeker)/delivery.tsx && grep -q "VideoView" lmc-app/app/(seeker)/delivery.tsx`
    - `grep -q "stream.mux.com" lmc-app/app/(seeker)/delivery.tsx`
    - `grep -q "getPlaybackToken" lmc-app/app/(seeker)/delivery.tsx`
    - submitted.tsx keeps its deferral marker: `grep -q "TODO(phase-4)" lmc-app/app/(scout)/submitted.tsx`
    - `cd lmc-app && npx tsc --noEmit` exits 0 ; `bash lmc-app/scripts/check-video-invariants.sh` exits 0
  </acceptance_criteria>
  <verify>
    <automated>grep -q "useVideoPlayer" "lmc-app/app/(seeker)/delivery.tsx" && grep -q "stream.mux.com" "lmc-app/app/(seeker)/delivery.tsx" && grep -q "getPlaybackToken" "lmc-app/app/(seeker)/delivery.tsx" && cd lmc-app && npx tsc --noEmit && echo OK</automated>
  </verify>
  <done>delivery.tsx plays the real signed Mux HLS clip; submitted.tsx reflects processing with no earnings credit.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: On-device end-to-end walk-through (camera + weak network + delivery + scoped playback)</name>
  <action>On the fresh dev build, run the full loop on a real device with a Seeker + Scout session: record a live 15s clip, toggle airplane mode mid-upload, confirm the Seeker screen auto-flips to delivered and plays the Mux clip, and confirm a second account is denied playback. Report pass or the specific failure.</action>
  <what-built>The full Phase-3 loop wired end to end: a real audio-free 15s capture, a resilient Mux upload, the webhook-driven delivery, real CDN playback scoped to the buyer. The camera and weak-network behaviour can only be proven on real hardware (the simulator has no camera).</what-built>
  <how-to-verify>
On the fresh dev build from 03-04, with a live check that has reached `filming` (use a Seeker + Scout session per Phase 2):
   1. CAPTURE: as the Scout, record a clip. Confirm it auto-stops at 15s, and that NO microphone-permission prompt ever appears, and there is NO "choose from library" option.
   2. WEAK NETWORK: toggle airplane mode mid-upload, then back on. Confirm the upload retries/resumes and the Scout screen stays in "uploading/processing" — it must NOT jump to delivered locally.
   3. DELIVERY: wait for Mux to finish. Confirm the Seeker's screen flips to delivered ON ITS OWN (via the existing Realtime subscription, no app restart) and plays the real clip smoothly from the Mux CDN.
   4. SCOPED PLAYBACK: sign in as a SECOND account (not the buyer) and attempt to load the same clip — confirm it is DENIED (no playback token / 403). Optionally confirm the asset is not playable without a token.
   5. AUDIO: confirm the delivered clip has NO audio track.
  </how-to-verify>
  <resume-signal>Reply "walk-through passed" or describe any failure (mic prompt, phantom delivery, second account could watch, audio present, no auto-flip).</resume-signal>
  <verify>
    <automated>MISSING — real-device + two-account end-to-end (no simulator camera; live Mux). Automated suite (Vitest+pgTAP+Deno) already green from 03-04; this checkpoint covers the hardware/network/CDN behaviour only.</automated>
  </verify>
  <done>On a real device: a live audio-free 15s clip survives a weak network, the Seeker's screen auto-flips to delivered and plays the real CDN clip, and a second account cannot watch it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| device camera -> clip | The only legitimate source is the live recorder; no gallery path. |
| client upload-complete -> delivery | The client must not infer delivery; the webhook is the only authority. |
| second account -> another buyer's clip | Playback must be scoped to the owning Seeker. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Spoofing | client fakes delivered (phantom delivery on weak net) | mitigate | markDelivered already removed in 03-03 (import + call); filming.tsx never references it; the screen shows processing and waits for the webhook-driven Realtime flip. On-device weak-network test proves it. HIGH. |
| T-03-05 | Information Disclosure | second account watches another buyer's clip | mitigate | delivery.tsx fetches a per-Seeker token (03-02 owner guard); signed-playback asset rejects token-less access. On-device second-account test proves it. HIGH. |
| T-03-09 | Tampering | gallery import sneaks into the wired screen | mitigate | no image-picker affordance added; check-video-invariants.sh re-run as an acceptance gate on both screen tasks. HIGH. |
| T-03-10 | Repudiation/Legal | audio recorded (VID-02 felony exposure) | mitigate | `audio={false}` on the real Camera; on-device test confirms no mic prompt + no audio track. HIGH. |
</threat_model>

<verification>
- tsc clean; check-video-invariants.sh green on both screen tasks.
- filming.tsx < 500 lines after Task 1a's extraction; markDelivered absent from filming (removed in Wave 1).
- Task 1a removed only the FAKE upload-progress setInterval (setUploadPct/setUploadStage gone) while the two legit countdown setIntervals (setSecondsLeft, setRecordSecs) remain.
- delivery.tsx plays signed Mux HLS via getPlaybackToken.
- On-device walk-through: capture (15s, no mic, no gallery), weak-network resilience, auto-flip delivery, scoped playback, no audio track.
</verification>

<success_criteria>
- A Seeker can watch a genuine, audio-free, recent 15-second clip filmed live by a Scout and streamed from Mux — and only the buyer can watch it. CHECK-04 + VID-01/02/04 are real on a device.
</success_criteria>

<output>
After completion, create `.planning/phases/03-video-pipeline/03-05-SUMMARY.md`.
</output>
</content>
