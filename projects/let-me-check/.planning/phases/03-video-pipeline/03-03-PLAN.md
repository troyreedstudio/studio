---
phase: 03-video-pipeline
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - lmc-app/app/lib/clips.ts
  - lmc-app/app/lib/checks.ts
  - lmc-app/app/lib/checks.test.ts
  - lmc-app/app/(scout)/filming.tsx
  - lmc-app/app.config.js
  - lmc-app/package.json
autonomous: true
requirements: [VID-01, VID-02, VID-03]
user_setup: []

must_haves:
  truths:
    - "lib/clips.ts requests a Mux upload URL, uploads the local recorded file with a resumable PUT, retries on a weak network, and NEVER transitions a check to delivered"
    - "lib/clips.ts fetches a per-Seeker playback token for delivery playback"
    - "the client has NO p_to:'delivered' path anywhere — markDelivered is removed from lib/checks.ts and its stub-delivery tests are gone from checks.test.ts (the webhook owns delivered)"
    - "filming.tsx no longer imports or calls markDelivered (so checks.ts and its only client consumer stay consistent within Wave 1) — the submit call site is a temporary no-op until 03-05 wires the real upload"
    - "audio is disabled at the camera config (no mic permission requested); NSMicrophoneUsageDescription is removed"
    - "no gallery / image-picker / launchImageLibrary import path exists anywhere in the app"
    - "no Mux secret exists anywhere in the RN bundle"
  artifacts:
    - path: "lmc-app/app/lib/clips.ts"
      provides: "requestUploadUrl, uploadClip, uploadWithRetry, getPlaybackToken — the client upload/playback seam"
      contains: "uploadWithRetry"
    - path: "lmc-app/app/lib/checks.ts"
      provides: "markDelivered RETIRED — no client-side delivered transition remains"
      contains: "markFilming"
    - path: "lmc-app/app/(scout)/filming.tsx"
      provides: "markDelivered import + call removed (temporary no-op); markFilming retained — keeps the screen compiling after Wave 1 so the Wave-2 full-app tsc passes"
      contains: "markFilming"
    - path: "lmc-app/app.config.js"
      provides: "vision-camera plugin (enableMicrophonePermission:false); mic usage string removed"
      contains: "react-native-vision-camera"
  key_links:
    - from: "lmc-app/app/lib/clips.ts"
      to: "mux-upload-url Edge Function"
      via: "supabase.functions.invoke"
      pattern: "functions.invoke\\('mux-upload-url'"
    - from: "lmc-app/app/lib/clips.ts"
      to: "expo-file-system createUploadTask"
      via: "resumable PUT"
      pattern: "createUploadTask"
---

<objective>
Build the device-side upload/playback library and prepare native capture config — without yet touching the screens' presentation. Add `lib/clips.ts` (request upload URL, resumable retried upload, fetch playback token), RETIRE the now-illegal client-side `markDelivered` from `lib/checks.ts` (the webhook owns `delivered`) AND remove its now-dangling import + call from its only client consumer `(scout)/filming.tsx` (so the whole Wave-1 surface stays internally consistent and compiles), wire `app.config.js` for vision-camera (audio OFF) and remove the now-false microphone usage string, and install the native deps. Turn the Wave-0 Vitest scaffold (03-01) green. All offline-verifiable (tsc/vitest/grep); the native build itself is the [BLOCKING] 03-04 plan.

Purpose: The client's job is narrow and honest — record (screen, 03-05), hand the file to Mux via a signed URL, retry on weak networks, and STOP at "upload PUT returned success". It must NEVER mark the check delivered (the webhook owns that). The locked decision is "the client CANNOT mark delivered" — so the residual `markDelivered` wrapper (which inserts a stub clip and calls `transition_check delivered` from the device) must be REMOVED here, not merely left uncalled. Removing it is also what lets this plan's own invariants gate (`! grep -rn "p_to: 'delivered'" lmc-app/app`) pass honestly. CRITICAL — because the Wave-2 03-04 Task 3 runs a FULL-APP `npx tsc --noEmit` BEFORE Wave-3 03-05 rewires the screen, deleting `markDelivered` from checks.ts here would leave filming.tsx's `import { markFilming, markDelivered }` + `markDelivered(...)` call dangling and break the Wave-2 tsc. So this plan also fixes filming.tsx's import + call site in the SAME wave (temporary no-op + TODO), keeping checks.ts and its sole consumer consistent within Wave 1. Fresh-capture is enforced by ABSENCE: this plan must not add any gallery/image-picker path, and must assert none exists. Audio-off is enforced at the camera config so the mic is never opened (VID-02, a hard legal requirement).

Output: `lib/clips.ts`, a `markDelivered`-free `lib/checks.ts` + `checks.test.ts`, a `markDelivered`-free `filming.tsx` (temporary no-op submit), an updated `app.config.js`, native deps in `package.json`, green clips + checks Vitest, full-app tsc clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-video-pipeline/03-RESEARCH.md
@.planning/phases/03-video-pipeline/03-VALIDATION.md

<interfaces>
<!-- The existing client patterns clips.ts mirrors, and the Edge Functions it calls. -->

From lmc-app/app/lib/checks.ts — the module style to mirror (typed wrappers, supabase from './supabase', NO business logic; requireUserId pattern):
```ts
import { supabase } from './supabase';
import type { Database } from './database.types';
export type ClipRow = Database['public']['Tables']['clips']['Row'];
async function requireUserId(): Promise<string> { /* auth.getUser or throw */ }
```

The `markDelivered` wrapper being RETIRED (checks.ts ~lines 114-139) inserts a `status:'stub'` clip row then calls `rpc('transition_check', { p_to: 'delivered' })` from the client — this is exactly the illegal path the webhook now owns. Its stale tests live in checks.test.ts: the whole `describe('lib/checks markDelivered', ...)` block (~lines 240-286), the header comment line (~line 7), and the `await checks.markDelivered(...)` call inside the DATA-02 invariant test (~line 355).

From (scout)/filming.tsx (the ONLY client consumer of markDelivered — fixed in this plan, Task 2):
- line 15: `import { markFilming, markDelivered } from '../lib/checks';` — must become `import { markFilming } from '../lib/checks';`
- line 118: `markDelivered(checkId, new Date().toISOString()).catch(...)` inside `handleSubmit` — must become a temporary no-op (drop the call; keep `setUploading(true)`) with a `// TODO(03-05): real upload via lib/clips kicks off here; webhook drives delivered` comment. Lines 66 + 84 are legit countdown setIntervals and 03-05's fake-upload removal — do NOT touch them here.

Edge Functions (03-02) invoked via supabase.functions.invoke:
```ts
supabase.functions.invoke('mux-upload-url',     { body: { checkId } }) -> { uploadUrl, uploadId }
supabase.functions.invoke('mux-playback-token', { body: { checkId } }) -> { token }
```

expo-file-system resumable upload (Mux's official RN method — NOT UpChunk, NOT TUS):
```ts
import * as FileSystem from 'expo-file-system';
const task = FileSystem.createUploadTask(uploadUrl, localPath,
  { httpMethod: 'PUT', uploadType: FileSystem.FileSystemUploadType.BINARY_CONTENT },
  (p) => onProgress?.(p.totalBytesSent / p.totalBytesExpectedToSend));
const res = await task.uploadAsync();
```

app.config.js TODAY (lines to change): infoPlist has NSMicrophoneUsageDescription (REMOVE) and NSCameraUsageDescription (KEEP). plugins array has expo-video, mapbox, etc. (ADD vision-camera plugin entry).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: lib/clips.ts — request URL, resumable retried upload, playback token (no client delivered transition)</name>
  <read_first>
    - lmc-app/app/lib/checks.ts (module style: typed wrappers, supabase import, requireUserId, NO direct table UPDATE)
    - lmc-app/app/lib/clips.test.ts (the Wave-0 scaffold from 03-01 to turn green — names uploadWithRetry, requestUploadUrl, getPlaybackToken)
    - .planning/phases/03-video-pipeline/03-RESEARCH.md Patterns 2 + 3 (createUploadTask PUT + bounded backoff retry; "DO NOT mark delivered here")
  </read_first>
  <behavior>
    - requestUploadUrl(checkId) -> invokes 'mux-upload-url', returns { uploadUrl, uploadId }.
    - uploadClip(localPath, uploadUrl, onProgress?) -> createUploadTask PUT; throws on HTTP >= 300; returns on success; does NOT transition the check.
    - uploadWithRetry(localPath, uploadUrl, max=4) -> retries with exponential backoff (1s,2s,4s,8s), throws after max attempts.
    - getPlaybackToken(checkId) -> invokes 'mux-playback-token', returns the token string.
    - The module contains NO transition_check delivered call (guard test from the scaffold).
  </behavior>
  <action>
Create `lmc-app/app/lib/clips.ts` mirroring `lib/checks.ts` conventions:
   - `requestUploadUrl(checkId: string): Promise<{ uploadUrl: string; uploadId: string }>` via `supabase.functions.invoke('mux-upload-url', { body: { checkId } })` (throw on error).
   - `uploadClip(localPath, uploadUrl, onProgress?)` using `FileSystem.createUploadTask` (PUT, BINARY_CONTENT); throw if `res.status >= 300`. Add a header comment: "DO NOT mark delivered here — the webhook owns it (VID-03)."
   - `uploadWithRetry(localPath, uploadUrl, max = 4)` — bounded exponential backoff loop around uploadClip.
   - `getPlaybackToken(checkId: string): Promise<string>` via `supabase.functions.invoke('mux-playback-token', { body: { checkId } })`.
   - Do NOT import or reference any image-picker / gallery API. Do NOT call `rpc('transition_check', ... 'delivered')`.
Keep < 200 lines. Make the 03-01 clips.test.ts scaffold pass (mock supabase.functions.invoke + expo-file-system).
  </action>
  <acceptance_criteria>
    - `grep -q "uploadWithRetry" lmc-app/app/lib/clips.ts && grep -q "requestUploadUrl" lmc-app/app/lib/clips.ts && grep -q "getPlaybackToken" lmc-app/app/lib/clips.ts`
    - `grep -q "createUploadTask" lmc-app/app/lib/clips.ts`
    - `grep -q "functions.invoke('mux-upload-url'" lmc-app/app/lib/clips.ts && grep -q "functions.invoke('mux-playback-token'" lmc-app/app/lib/clips.ts`
    - NO client delivered transition: `! grep -rn "p_to: ?'delivered'" lmc-app/app/lib`
    - NO gallery path: `! grep -rEi "image-picker|launchImageLibrary|MediaLibrary" lmc-app/app/lib/clips.ts`
    - Vitest green: `cd lmc-app && npx vitest run app/lib/clips.test.ts` exits 0; `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd lmc-app && npx vitest run app/lib/clips.test.ts && ! grep -rn "p_to: *'delivered'" app/lib && echo OK</automated>
  </verify>
  <done>lib/clips.ts requests the upload URL, uploads with resumable retry, fetches a playback token, and never transitions the check; the Wave-0 Vitest is green; tsc passes.</done>
</task>

<task type="auto">
  <name>Task 2: Retire client markDelivered — remove from lib/checks.ts, delete its stub-delivery tests, AND drop its dangling import + call from filming.tsx (keeps Wave 1 compiling for the Wave-2 full-app tsc)</name>
  <read_first>
    - lmc-app/app/lib/checks.ts (~lines 114-139 — the `markDelivered` wrapper that inserts a stub clip then calls `transition_check delivered`)
    - lmc-app/app/lib/checks.test.ts (~lines 240-286 the `describe('lib/checks markDelivered', ...)` block; ~line 7 the header comment; ~line 355 the `checks.markDelivered(...)` call inside the DATA-02 invariant test)
    - lmc-app/app/(scout)/filming.tsx (line 15 the `import { markFilming, markDelivered }`; line 118 the `markDelivered(checkId, ...)` call inside handleSubmit; lines 66 + 84 are legit countdown setIntervals — DO NOT touch them)
    - .planning/phases/03-video-pipeline/03-VALIDATION.md (the locked decision: the client CANNOT mark delivered; the webhook owns it)
  </read_first>
  <action>
Remove the client's ability to mark a check delivered (locked decision — the mux-webhook in 03-02 is the sole `delivered` driver), AND fix its only consumer in the same wave so the full app still compiles for the Wave-2 03-04 full-app tsc:
   - In `lmc-app/app/lib/checks.ts`, DELETE the entire `markDelivered` function (its doc comment + body, ~lines 114-139, including the `clips` stub insert and the `transition_check { p_to: 'delivered' }` RPC). After this edit there must be NO `p_to: 'delivered'` and NO export named `markDelivered` anywhere in the client. Leave `markFilming`, `rateCheck`, `cancelCheck`, etc. untouched.
   - In `lmc-app/app/lib/checks.test.ts`:
       a. DELETE the whole `describe('lib/checks markDelivered', () => { ... })` block (~lines 240-286) — its three stub-delivery assertions test a path the server 0010 ready-guard now rejects, so it must not run against live (Plan 04 Task 3).
       b. REMOVE the `await checks.markDelivered('id', '2026-06-20T12:00:00Z');` line from the `DATA-02 invariant` test (~line 355) so that test no longer references the deleted wrapper; the remaining wrappers in that test still prove DATA-02.
       c. UPDATE the header comment (~line 7): delete the `markDelivered inserts a stub clip BEFORE transitioning to 'delivered'` bullet (that contract no longer exists client-side).
   - In `lmc-app/app/(scout)/filming.tsx` (its ONLY client consumer — fix it HERE in Wave 1, not 03-05, so the Wave-2 full-app tsc passes):
       a. Line 15: change `import { markFilming, markDelivered } from '../lib/checks';` to `import { markFilming } from '../lib/checks';` (keep markFilming; drop markDelivered).
       b. Line 118 (inside `handleSubmit`): REMOVE the `markDelivered(checkId, new Date().toISOString()).catch(...)` call. Keep the surrounding `setUploading(true)` so the screen still flips into its existing (fake-for-now) upload state and COMPILES. Replace the deleted call with a comment: `// TODO(03-05): real upload via lib/clips kicks off here; webhook drives delivered`. Do NOT alter the legit countdown setIntervals at lines 66 + 84, and do NOT remove the fake-upload setInterval block (~lines 124-157) — that is 03-05 Task 1a's job; this task only makes the screen markDelivered-free and compiling.
   - This removes the only client `p_to:'delivered'` path AND the only dangling reference to the deleted export, so a FULL-APP `npx tsc --noEmit` passes after Wave 1 (which is what 03-04 Task 3 runs in Wave 2, before 03-05 rewires the screen in Wave 3).
  </action>
  <acceptance_criteria>
    - markDelivered fully gone from the client lib: `! grep -q "markDelivered" lmc-app/app/lib/checks.ts && ! grep -q "markDelivered" lmc-app/app/lib/checks.test.ts`
    - markDelivered gone from filming.tsx (import + call): `! grep -q "markDelivered" "lmc-app/app/(scout)/filming.tsx"`
    - markFilming still imported by filming.tsx: `grep -q "markFilming" "lmc-app/app/(scout)/filming.tsx"`
    - the TODO breadcrumb for 03-05 exists: `grep -q "TODO(03-05)" "lmc-app/app/(scout)/filming.tsx"`
    - NO client delivered transition remains: `! grep -rn "p_to: *'delivered'" lmc-app/app/lib`
    - The DATA-02 invariant test still exists and still asserts zero checks updates: `grep -q "DATA-02 invariant" lmc-app/app/lib/checks.test.ts`
    - FULL-APP tsc passes (no dangling markDelivered import anywhere) so the Wave-2 03-04 tsc will pass: `cd lmc-app && npx tsc --noEmit` exits 0
    - Remaining lib tests green: `cd lmc-app && npx vitest run app/lib/checks.test.ts app/lib/clips.test.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd lmc-app && ! grep -q "markDelivered" app/lib/checks.ts && ! grep -q "markDelivered" app/lib/checks.test.ts && ! grep -q "markDelivered" "app/(scout)/filming.tsx" && grep -q "markFilming" "app/(scout)/filming.tsx" && ! grep -rn "p_to: *'delivered'" app/lib && npx tsc --noEmit && npx vitest run app/lib/checks.test.ts app/lib/clips.test.ts && echo OK</automated>
  </verify>
  <done>The client can no longer mark a check delivered: `markDelivered` is removed from checks.ts, all of its stub-delivery tests are gone from checks.test.ts, and its dangling import + call are removed from filming.tsx (replaced by a temporary no-op + 03-05 TODO). No `p_to:'delivered'` path remains under app/lib; a FULL-APP tsc passes (so the Wave-2 03-04 tsc will pass); the remaining lib Vitest is green.</done>
</task>

<task type="auto">
  <name>Task 3: app.config.js — vision-camera plugin (audio off), remove microphone usage string; install native deps</name>
  <read_first>
    - lmc-app/app.config.js (FULL — the plugins array + ios.infoPlist; KEEP NSCameraUsageDescription, REMOVE NSMicrophoneUsageDescription)
    - .planning/phases/03-video-pipeline/03-RESEARCH.md Pitfall 2 + Standard Stack install block (vision-camera plugin enableMicrophonePermission:false)
  </read_first>
  <action>
Edit `lmc-app/app.config.js`:
   - REMOVE the `NSMicrophoneUsageDescription` entry from `ios.infoPlist` (we never open the mic — VID-02). Leave `NSCameraUsageDescription`. Leave `NSPhotoLibraryUsageDescription` as-is (it grants WRITE for saving, not a gallery read path; fresh-capture is enforced by absence of any picker code, not by this string).
   - ADD a `react-native-vision-camera` config-plugin entry to the `plugins` array:
```js
[ 'react-native-vision-camera', { enableMicrophonePermission: false } ],
```
Then install the native deps (pins the SDK-54-compatible versions):
```bash
cd lmc-app && npx expo install react-native-vision-camera expo-file-system
```
Do NOT add expo-image-picker or any gallery library. (expo-video is already installed.)
Note in the SUMMARY that these are NATIVE deps -> a fresh EAS dev build is required before on-device capture (handled in 03-04).
  </action>
  <acceptance_criteria>
    - `! grep -q "NSMicrophoneUsageDescription" lmc-app/app.config.js`
    - `grep -q "react-native-vision-camera" lmc-app/app.config.js && grep -q "enableMicrophonePermission: false" lmc-app/app.config.js`
    - `grep -q "react-native-vision-camera" lmc-app/package.json && grep -q "expo-file-system" lmc-app/package.json`
    - NO gallery dep added: `! grep -q "expo-image-picker" lmc-app/package.json`
    - `cd lmc-app && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>! grep -q "NSMicrophoneUsageDescription" lmc-app/app.config.js && grep -q "enableMicrophonePermission: false" lmc-app/app.config.js && grep -q "react-native-vision-camera" lmc-app/package.json && ! grep -q "expo-image-picker" lmc-app/package.json && echo OK</automated>
  </verify>
  <done>vision-camera is configured with the mic disabled, the false mic usage string is gone, native deps are installed, and no gallery library exists.</done>
</task>

<task type="auto">
  <name>Task 4: Phase-wide enforcement gates — no gallery path, no audio-on, no client-delivered, no Mux secret in the bundle</name>
  <read_first>
    - .planning/phases/03-video-pipeline/03-RESEARCH.md Pitfall 2 + Anti-Patterns (the exact grep gates)
    - lmc-app/app.config.js + lmc-app/app/lib/clips.ts + lmc-app/app/lib/checks.ts (the files these gates scan — checks.ts must be markDelivered-free by Task 2 for the p_to gate to pass)
  </read_first>
  <action>
Add a small repeatable verification script `lmc-app/scripts/check-video-invariants.sh` (executable) that runs the phase's hard invariants and exits non-zero on any violation:
   - `! grep -rEi "image-picker|launchImageLibrary" lmc-app/app`  (VID-01 fresh-capture by absence)
   - `! grep -rEi "audio=\{true\}|enableAudio: ?true|enableMicrophonePermission: ?true" lmc-app/app lmc-app/app.config.js`  (VID-02 audio off)
   - `! grep -rn "p_to: *'delivered'" lmc-app/app`  (client never delivers — webhook owns it; depends on Task 2 having removed markDelivered)
   - `! grep -rEi "MUX_TOKEN_SECRET|MUX_SIGNING_PRIVATE_KEY|mux.*tokenSecret" lmc-app/app lmc-app/app.config.js`  (no Mux secret in the RN bundle)
Echo "video-invariants OK" on success. This script is the per-commit gate the executor and 03-05 re-run. It MUST pass now because Task 2 has already removed the only client `p_to:'delivered'` path.
  </action>
  <acceptance_criteria>
    - `test -x lmc-app/scripts/check-video-invariants.sh`
    - Running it passes now: `bash lmc-app/scripts/check-video-invariants.sh` exits 0 and prints "video-invariants OK"
    - The script literally contains all four gates: `grep -q "image-picker" lmc-app/scripts/check-video-invariants.sh && grep -q "MUX_TOKEN_SECRET\|MUX_SIGNING_PRIVATE_KEY" lmc-app/scripts/check-video-invariants.sh && grep -q "p_to" lmc-app/scripts/check-video-invariants.sh && grep -q "audio=" lmc-app/scripts/check-video-invariants.sh`
  </acceptance_criteria>
  <verify>
    <automated>bash lmc-app/scripts/check-video-invariants.sh && echo OK</automated>
  </verify>
  <done>A single script enforces fresh-capture-by-absence, audio-off, no-client-delivered (passing because Task 2 removed markDelivered), and no-Mux-secret-in-bundle; it passes now and is re-run after the screens are wired (03-05).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RN bundle (shipped to devices) | Anything in the bundle is public; no secret may live here. |
| device camera -> upload | The only legitimate clip source is the live recorder; a gallery path would defeat fresh-capture. |
| client -> delivered | The client must NOT be able to transition a check to delivered; the webhook is the sole authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Information Disclosure | Mux secret in the RN bundle | mitigate | clips.ts only invokes Edge Functions; the check-video-invariants gate asserts NO MUX_TOKEN_SECRET / signing key string in app or app.config.js. HIGH. |
| T-03-09 | Tampering | gallery import defeats fresh-capture (VID-01) | mitigate | no image-picker dependency or import added; invariants gate fails the build if one appears. HIGH. |
| T-03-10 | Repudiation/Legal | audio recorded (all-party-consent felony, VID-02) | mitigate | enableMicrophonePermission:false + mic usage string removed; invariants gate fails on audio={true}/enableAudio. HIGH. |
| T-03-01 | Spoofing | client fakes delivered on upload-complete | mitigate | markDelivered REMOVED from checks.ts AND from its only consumer filming.tsx (Task 2); clips.ts has no transition_check delivered; invariants gate fails on `p_to: 'delivered'` anywhere in app. HIGH. |
</threat_model>

<verification>
- Vitest clips + checks green; FULL-APP tsc clean (filming.tsx's dangling markDelivered import is removed in Task 2, so the Wave-2 03-04 full-app tsc will pass).
- markDelivered removed from checks.ts + checks.test.ts AND from filming.tsx (import + call); no `p_to:'delivered'` under app/lib.
- check-video-invariants.sh passes (no gallery, audio off, no client-delivered, no Mux secret).
- vision-camera plugin + native deps present; no image-picker dep.
- Offline only — the native build is the [BLOCKING] 03-04 plan.
</verification>

<success_criteria>
- The client upload/playback seam exists and is honest (no delivered transition, resumable retry).
- The client can no longer mark a check delivered — markDelivered is fully retired from checks.ts and its only consumer (filming.tsx), and the invariants gate proves it; a full-app tsc passes so Wave 2 won't break.
- Capture config is audio-off; fresh-capture is enforced by absence and guarded by a reusable invariants script.
- No Mux secret can reach the device.
</success_criteria>

<output>
After completion, create `.planning/phases/03-video-pipeline/03-03-SUMMARY.md`.
</output>
</content>
</invoke>
