---
phase: 09-verified-badge-scout-identity-quick-win-reconnects-surface-r
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - lmc-app/app/(seeker)/notifications.tsx
  - lmc-app/app/(seeker)/preferred-cities.tsx
  - lmc-app/app/(seeker)/profile.tsx
autonomous: true
requirements: [D-04]
must_haves:
  truths:
    - "Notification toggle changes persist to the Seeker's profile and reload after restart"
    - "Preferred-city selections persist to the Seeker's profile and reload after restart"
    - "The profile screen shows the real display name + member-since date (not 'Troy R.')"
    - "The profile stats show real check count, total spent, and avg rating (not '14 / $245 / 4.8')"
  artifacts:
    - path: "lmc-app/app/(seeker)/notifications.tsx"
      provides: "Persisted notification_prefs (load on mount, save on toggle)"
      contains: "notification_prefs"
    - path: "lmc-app/app/(seeker)/preferred-cities.tsx"
      provides: "Persisted preferred_cities (load on mount, save on toggle)"
      contains: "preferred_cities"
    - path: "lmc-app/app/(seeker)/profile.tsx"
      provides: "Real name, member-since, check count, spent, avg rating"
      contains: "getProfile"
  key_links:
    - from: "notifications.tsx / preferred-cities.tsx"
      to: "profiles.notification_prefs / preferred_cities"
      via: "supabase.from('profiles').update().eq('id', uid)"
      pattern: "from\\('profiles'\\).update"
    - from: "profile.tsx"
      to: "getProfile() + listMyChecks() + ratings"
      via: "useEffect on mount"
      pattern: "getProfile"
---

<objective>
Reconnect the three remaining mock seeker screens to the real backend (D-04). The state-module-backed screens (saved, recurring, recurring-setup, payment-methods list) are ALREADY wired — research confirmed — so they are explicitly OUT of scope here. This plan only touches the three screens that still use raw `useState` with no persistence:

1. **notifications.tsx** — load `profile.notification_prefs` on mount; persist toggle changes to `profiles.notification_prefs` (new column from Plan 01's 0017).
2. **preferred-cities.tsx** — load `profile.preferred_cities` on mount (string[] -> Set); persist selections to `profiles.preferred_cities`.
3. **profile.tsx** — replace hardcoded "TR" / "Troy R." / "Member since January 2026" / "14 / $245 / 4.8★" with real data: `getProfile()` (name + created_at), `listMyChecks()` (count + spent), and an owned-rows ratings query (avg rating).

Purpose: No fake data on the seeker profile surface (Apple-review safety + honesty).
Output: Three files changed; tsc clean.

RESOLVED OPEN QUESTION (profile avg-rating scope): INCLUDE avg rating. The `ratings` table has `seeker_id` + `stars`, and the `ratings_select_own` RLS policy (0005 line 70-71) lets a Seeker read their OWN rating rows. So avg rating is a cheap, RLS-safe direct query — no new RPC, no second heavy round-trip. (Verified against 0004 + 0005.) The three profile stats become: count (= listMyChecks length, excluding cancelled/no_scout), spent (sum of tier price for paid checks), avg rating (avg of own ratings.stars).

This plan runs in parallel with Plan 02 (zero file overlap). It depends on Plan 01 because notification_prefs/preferred_cities must exist in the migration; live push + type regen is Plan 04, so writes use the untyped `(supabase as any)` cast meanwhile.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-verified-badge-scout-identity-quick-win-reconnects-surface-r/09-RESEARCH.md

# Files being edited:
@lmc-app/app/(seeker)/notifications.tsx
@lmc-app/app/(seeker)/preferred-cities.tsx
@lmc-app/app/(seeker)/profile.tsx

<interfaces>
<!-- Verified facts — no exploration needed. -->

api.ts:
  getProfile(): Promise<ProfileRow | null>  // selects * from profiles where id = uid; ProfileRow includes display_name, created_at (+ notification_prefs/preferred_cities after Plan 04 regen)
checks.ts:
  listMyChecks(): Promise<CheckRow[]>  // own checks, newest first. CheckRow has: status (enum), tier (string 'standard'|'priority'), created_at. NO stars.
supabase: `import { supabase } from '../lib/supabase'`

ratings table (0004): seeker_id, stars int (1..5). RLS ratings_select_own (0005):
  seeker can SELECT where auth.uid() = seeker_id. So a Seeker can read their own ratings.
  Query: `supabase.from('ratings').select('stars').eq('seeker_id', uid)` then average client-side.

profiles_update_own RLS (0005): row-level (auth.uid() = id) — covers the new columns.

UNTYPED CAST: notification_prefs / preferred_cities are NOT in database.types.ts until
Plan 04 regen. Use `(supabase as any).from('profiles').update({ notification_prefs: ... })`
to avoid tsc errors meanwhile (mirrors checks.ts `as any` idiom). After Plan 04 regen the
cast can stay (harmless) — do NOT block on it.

notifications.tsx current: SETTINGS array of {id,label,sub,defaultValue}; values is
  Record<string,boolean> seeded from defaults; onValueChange does setValues only.
preferred-cities.tsx current: CITIES array {id,name,region,scouts}; selected is
  Set<string> seeded with ['mia','nyc']; toggle does setSelected only. (The hardcoded
  `scouts` counts per city are deferred — do NOT try to make them real this phase.)
profile.tsx current: zero useEffect; "TR", "Troy R.", "Member since January 2026",
  stats 14 / $245 / 4.8★ all hardcoded JSX.

Tier pricing (project pricing model): standard = $15.00 seeker pays, priority = $20.00.
  Use these for the "spent" sum (cents or dollars — render as `$<int>`).

How to get uid: `const { data } = await supabase.auth.getUser(); const uid = data.user?.id;`
  (getProfile already does this internally; for the profiles.update you need the uid explicitly.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Persist notification prefs (notifications.tsx) — load on mount + save on toggle</name>
  <files>lmc-app/app/(seeker)/notifications.tsx</files>
  <read_first>
    - lmc-app/app/(seeker)/notifications.tsx (full)
    - 09-RESEARCH.md "Notification prefs persistence" code example + Pitfall 3 (untyped cast)
    - lmc-app/app/lib/api.ts getProfile
  </read_first>
  <action>
    Add `import { useEffect } from 'react';` (extend the existing react import) and
    `import { supabase } from '../lib/supabase';` and `import { getProfile } from '../lib/api';`.

    On mount (useEffect, [] deps): call getProfile(); if `profile?.notification_prefs` is present,
    merge it over the default values: `setValues(prev => ({ ...prev, ...(profile.notification_prefs as Record<string, boolean>) }));`. Wrap in `.catch(() => {})` so failure keeps defaults.

    Replace the inline `onValueChange={(v) => setValues(...)}` with a handler that (a) optimistically updates local state and (b) persists the FULL next map to the profile:
    ```tsx
    const handleToggle = (id: string, v: boolean) => {
      setValues(prev => {
        const next = { ...prev, [id]: v };
        (async () => {
          const { data } = await supabase.auth.getUser();
          const uid = data.user?.id;
          if (!uid) return;
          // notification_prefs not in generated types until Plan 04 regen
          await (supabase as any).from('profiles').update({ notification_prefs: next }).eq('id', uid);
        })().catch(() => {});
        return next;
      });
    };
    ```
    Wire `onValueChange={(v) => handleToggle(s.id, v)}`. Offline-safe: ignore persist errors (local toggle still reflects the choice). Keep the file under 500 lines.
  </action>
  <acceptance_criteria>
    <automated>cd lmc-app && grep -q "notification_prefs" "app/(seeker)/notifications.tsx" && grep -q "getProfile" "app/(seeker)/notifications.tsx" && grep -q "from('profiles').update" "app/(seeker)/notifications.tsx" && grep -q "useEffect" "app/(seeker)/notifications.tsx"</automated>
  </acceptance_criteria>
  <verify>
    <automated>cd lmc-app && npx tsc --noEmit</automated>
  </verify>
  <done>Prefs load from profile on mount (merged over defaults) and persist to profiles.notification_prefs on every toggle; failures are silent; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Persist preferred cities (preferred-cities.tsx) — load Set on mount + save array on toggle</name>
  <files>lmc-app/app/(seeker)/preferred-cities.tsx</files>
  <read_first>
    - lmc-app/app/(seeker)/preferred-cities.tsx (full)
    - 09-RESEARCH.md Pitfall 4 (text[] <-> Set conversion) + Pitfall 3 (untyped cast)
  </read_first>
  <action>
    Add `import { useEffect } from 'react';`, `import { supabase } from '../lib/supabase';`, `import { getProfile } from '../lib/api';`.

    Change the initial state to an empty Set (`new Set<string>()`) instead of the hardcoded `['mia','nyc']` — the real saved value loads on mount.

    On mount (useEffect, []): getProfile(); `setSelected(new Set((profile?.preferred_cities as string[] | null) ?? []));` wrapped in `.catch(() => {})`.

    In `toggle`, after computing the `next` Set, persist `Array.from(next)`:
    ```tsx
    const toggle = (id: string) => {
      setSelected(prev => {
        const next = new Set(prev);
        next.has(id) ? next.delete(id) : next.add(id);
        (async () => {
          const { data } = await supabase.auth.getUser();
          const uid = data.user?.id;
          if (!uid) return;
          await (supabase as any).from('profiles').update({ preferred_cities: Array.from(next) }).eq('id', uid);
        })().catch(() => {});
        return next;
      });
    };
    ```
    Leave the hardcoded `scouts` per-city counts as-is (real supply counts are deferred per CONTEXT). Keep the file under 500 lines.
  </action>
  <acceptance_criteria>
    <automated>cd lmc-app && grep -q "preferred_cities" "app/(seeker)/preferred-cities.tsx" && grep -q "getProfile" "app/(seeker)/preferred-cities.tsx" && grep -q "Array.from" "app/(seeker)/preferred-cities.tsx" && ! grep -q "new Set<string>(\['mia'" "app/(seeker)/preferred-cities.tsx"</automated>
  </acceptance_criteria>
  <verify>
    <automated>cd lmc-app && npx tsc --noEmit</automated>
  </verify>
  <done>Initial selection loads from profiles.preferred_cities (string[] -> Set); toggles persist Array.from(Set) to the DB; no hardcoded mia/nyc seed; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 3: Real profile header + stats (profile.tsx) — name, member-since, count, spent, avg rating</name>
  <files>lmc-app/app/(seeker)/profile.tsx</files>
  <read_first>
    - lmc-app/app/(seeker)/profile.tsx (full)
    - 09-RESEARCH.md "Profile stats wiring" code example + Open Question 2 (resolved: include avg rating via owned ratings query)
    - lmc-app/app/lib/api.ts getProfile, lmc-app/app/lib/checks.ts listMyChecks
  </read_first>
  <action>
    Add `import { useState, useEffect } from 'react';`, `import { getProfile } from '../lib/api';`,
    `import { listMyChecks } from '../lib/checks';`, `import { supabase } from '../lib/supabase';`.

    Add state: displayName (string|null), memberSince (string|null), stats `{ count: number; spent: number; avgRating: number | null }` (default { count: 0, spent: 0, avgRating: null }).

    On mount (useEffect, []), Promise.all of getProfile() + listMyChecks() + an owned-ratings query, all guarded:
    ```tsx
    const { data: u } = await supabase.auth.getUser();
    const uid = u.user?.id;
    const [profile, checks] = await Promise.all([getProfile(), listMyChecks()]);
    // avg rating from the Seeker's OWN ratings (RLS ratings_select_own)
    let avgRating: number | null = null;
    if (uid) {
      const { data: rows } = await supabase.from('ratings').select('stars').eq('seeker_id', uid);
      if (rows && rows.length) avgRating = Math.round((rows.reduce((s, r) => s + (r.stars ?? 0), 0) / rows.length) * 10) / 10;
    }
    ```
    Derive:
      - displayName from profile.display_name (fallback null -> render "Seeker").
      - initials: from display_name words (e.g. "Troy Reed" -> "TR"; single word -> first 2 chars upper; null -> "S").
      - memberSince: `profile?.created_at ? new Date(profile.created_at).toLocaleDateString('en-US',{month:'long',year:'numeric'}) : null`.
      - count: checks that are NOT 'cancelled' and NOT 'no_scout' (a real delivered/active check count). Use `checks.filter(c => c.status !== 'cancelled' && c.status !== 'no_scout').length`.
      - spent: sum over those same checks of tier price ($15 standard / $20 priority): `acc + (c.tier === 'priority' ? 20 : 15)`.
    Render:
      - avatar initials -> derived initials (fallback "S").
      - userName -> `{displayName ?? 'Seeker'}`.
      - memberSince -> `{memberSince ? \`Member since ${memberSince}\` : ' '}` (don't show a fake date if null).
      - stats row: count -> `{stats.count}`, spent -> `{\`$${stats.spent}\`}`, avg rating -> `{stats.avgRating != null ? \`${stats.avgRating}★\` : '—'}` (show an em-dash placeholder when the Seeker has no ratings yet, NOT a fabricated number).
    Leave the SETTINGS list, referral banner, switch-mode, sign-out untouched. Keep the file under 500 lines.
  </action>
  <acceptance_criteria>
    <automated>cd lmc-app && grep -q "getProfile" "app/(seeker)/profile.tsx" && grep -q "listMyChecks" "app/(seeker)/profile.tsx" && grep -q "from('ratings')" "app/(seeker)/profile.tsx" && ! grep -q "Troy R" "app/(seeker)/profile.tsx" && ! grep -q "Member since January 2026" "app/(seeker)/profile.tsx" && grep -q "useEffect" "app/(seeker)/profile.tsx"</automated>
  </acceptance_criteria>
  <verify>
    <automated>cd lmc-app && npx tsc --noEmit</automated>
  </verify>
  <done>Header shows real display name, derived initials, real member-since (or blank); stats show real count, spent ($ from tier), and avg rating (or — when none). No "Troy R."/"January 2026"/hardcoded 14/$245/4.8 remain; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> profiles UPDATE (prefs/cities) | Untrusted payload for the caller's own row |
| client -> ratings SELECT | Seeker reads own rating rows for avg |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-09 | Tampering | writing prefs/cities to another user's row | mitigate | All updates use `.eq('id', uid)` where uid = auth.getUser(); profiles_update_own RLS (0005) row-scopes the write server-side regardless. |
| T-09-10 | Information Disclosure | reading other Seekers' ratings for avg | mitigate | ratings query filters `.eq('seeker_id', uid)`; ratings_select_own RLS enforces it server-side. |
| T-09-11 | Misrepresentation | fabricated profile stats | mitigate | All stats derived from real getProfile/listMyChecks/ratings; no-data states show blank or em-dash, never a fake number. |
</threat_model>

<verification>
- grep asserts each screen now references the real backend (getProfile / notification_prefs / preferred_cities / ratings / listMyChecks) and that no hardcoded mock strings remain.
- `npx tsc --noEmit` clean (untyped casts avoid the not-yet-regenerated-types errors).
- Persistence-across-restart is a manual on-device check (09-VALIDATION.md) after Plan 04's live push + regen.
</verification>

<success_criteria>
- notifications.tsx + preferred-cities.tsx load from profile on mount and persist on change.
- profile.tsx shows real name, member-since, count, spent, avg rating with honest empty states.
- No fake data strings remain on these three screens.
- tsc clean.
</success_criteria>

<output>
After completion, create `.planning/phases/09-verified-badge-scout-identity-quick-win-reconnects-surface-r/09-03-SUMMARY.md`
</output>
