---
phase: 09-verified-badge-scout-identity-quick-win-reconnects-surface-r
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - lmc-app/app/(seeker)/delivery.tsx
autonomous: true
requirements: [D-01, D-02, D-03]
must_haves:
  truths:
    - "The ✓ Verified badge shows ONLY when the clip's gps_verified is true"
    - "The Scout card shows the REAL scout's name (not 'Jake C.') for a delivered check"
    - "The Scout card shows the real avg rating + clip count, gracefully hiding the rating when unavailable"
    - "No fake 'AI Verdict' row or 'Crowd Report' tags appear on the delivery screen"
  artifacts:
    - path: "lmc-app/app/(seeker)/delivery.tsx"
      provides: "Real verified badge + real scout identity; fake AI/crowd removed"
      contains: "get_check_scout_public"
  key_links:
    - from: "delivery.tsx"
      to: "get_check_scout_public RPC"
      via: "supabase.rpc in a useEffect keyed on checkId"
      pattern: "get_check_scout_public"
    - from: "delivery.tsx verified badge"
      to: "clip.gps_verified"
      via: "conditional render === true"
      pattern: "gps_verified === true"
---

<objective>
Make the delivery screen honest. Three changes, all in delivery.tsx (D-01, D-02, D-03):
1. **D-01 Verified badge:** render `✓ Verified` ONLY when `clip?.gps_verified === true`. Otherwise render nothing (neutral). The `clip` row is already loaded on this screen.
2. **D-02 Scout identity:** fetch the real scout via the new `get_check_scout_public` RPC (from Plan 01's migration) and render real name + avg rating + clip count, replacing the hardcoded "Jake C." / "⭐ 4.9 · 247 videos" / "J" avatar.
3. **D-03 Remove fake AI/crowd:** delete the `TAGS` constant, the AI Verdict row, and the Crowd Report block — plus their orphaned styles. CRITICAL: do NOT delete `styles.sectionLabel` (shared with "RATE YOUR CHECK").

Purpose: Apple-review safety + product honesty. No fabricated AI feature, no fake reviewer identity, no false "Verified".
Output: One file changed; tsc clean.

This plan can run in parallel with Plan 03 (zero file overlap). It depends on Plan 01 only because the RPC must exist in the migration; the live DB push happens in Plan 04. The client calls the RPC via an untyped cast (the RPC won't be in database.types.ts until Plan 04 regen), matching the existing `(supabase as any).rpc(...)` pattern in checks.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-verified-badge-scout-identity-quick-win-reconnects-surface-r/09-RESEARCH.md

# The file being edited — read in full before editing (exact line numbers below):
@lmc-app/app/(seeker)/delivery.tsx

<interfaces>
<!-- Verified facts — no exploration needed. -->

delivery.tsx already imports `type ClipRow` and stores `clip` state (line 110).
ClipRow includes `gps_verified: boolean | null` (database.types.ts line 126) and
`signage_confirmed` (advisory; leave it — keep badge binary per D-01).

`check` state is a CheckRow with `scout_id: string | null`, `status`, `seeker_id`.
The screen already calls getCheck + getCheckClip in a useEffect keyed on checkId (line 114-118).

supabase client: `import { supabase } from '../lib/supabase'` is NOT currently
imported in delivery.tsx — add it. Pattern for an untyped RPC (RPC not yet in
database.types.ts until Plan 04 regen): `(supabase as any).rpc('get_check_scout_public', { p_check_id: checkId })`
matches checks.ts line 68 `(supabase as any).rpc('is_in_no_film_zone', {...})`.

RPC return shape (from Plan 01): array of one row
  { display_name: string | null; avg_rating: number | null; clip_count: number | null }

EXACT lines to remove (verified against current delivery.tsx):
  - line 10: `const TAGS = ['Busy Tonight', 'Short Line', 'Worth It'];`
  - lines 171-174: the <View style={styles.aiVerdictRow}> ... AI VERDICT block
  - lines 176-179: <Text style={styles.sectionLabel}>CROWD REPORT</Text> + the tagRow .map block
  - Orphaned styles to delete from StyleSheet (lines ~237-244):
      aiVerdictRow, aiBadge, aiBadgeText, aiVerdictText, tagRow, tag, tagText
  - KEEP styles.sectionLabel (used by RATE YOUR CHECK at line 181).

EXACT scout card to rewire (lines 191-198): replace the "J" avatar initial,
"Jake C." name (line 194), and "⭐ 4.9 · 247 videos" (line 195). The
verifiedBadge inside the scout card (line 197) is the D-01 target — make it conditional.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove fake AI Verdict + Crowd Report (D-03)</name>
  <files>lmc-app/app/(seeker)/delivery.tsx</files>
  <read_first>
    - lmc-app/app/(seeker)/delivery.tsx (full file; confirm line numbers before deleting — they may shift)
    - 09-RESEARCH.md "D-03" + "Pitfall 1" (do NOT delete styles.sectionLabel)
  </read_first>
  <action>
    Delete, in delivery.tsx:
    1. The top-level `const TAGS = [...]` constant (line 10).
    2. The entire AI Verdict JSX block (the `<View style={styles.aiVerdictRow}>...</View>` containing the ✦ AI VERDICT badge and the "Short line · ~30 inside · medium energy" text).
    3. The Crowd Report block: the `<Text style={styles.sectionLabel}>CROWD REPORT</Text>` line AND the immediately following `<View style={styles.tagRow}>{TAGS.map(...)}</View>`.
    4. From the StyleSheet, remove ONLY these orphaned keys: aiVerdictRow, aiBadge, aiBadgeText, aiVerdictText, tagRow, tag, tagText.
    DO NOT remove `styles.sectionLabel` — it is still used by "RATE YOUR CHECK" (and any "RATE YOUR SCOUT" label). Verify after editing that exactly one `sectionLabel` definition remains and that "RATE YOUR CHECK" still references it.
    Leave the video box, rating stars, scout card, and report flow untouched in this task.
  </action>
  <acceptance_criteria>
    <automated>cd lmc-app && ! grep -q "AI VERDICT" "app/(seeker)/delivery.tsx" && ! grep -q "CROWD REPORT" "app/(seeker)/delivery.tsx" && ! grep -q "const TAGS" "app/(seeker)/delivery.tsx" && ! grep -q "aiVerdictRow" "app/(seeker)/delivery.tsx" && ! grep -q "tagRow" "app/(seeker)/delivery.tsx" && grep -q "sectionLabel" "app/(seeker)/delivery.tsx" && grep -q "RATE YOUR CHECK" "app/(seeker)/delivery.tsx"</automated>
  </acceptance_criteria>
  <verify>
    <automated>cd lmc-app && npx tsc --noEmit</automated>
  </verify>
  <done>No AI VERDICT, CROWD REPORT, TAGS, or tag/aiVerdict styles remain; styles.sectionLabel and "RATE YOUR CHECK" still present; tsc clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Real Verified badge (D-01) + real Scout identity via RPC (D-02)</name>
  <files>lmc-app/app/(seeker)/delivery.tsx</files>
  <read_first>
    - lmc-app/app/lib/checks.ts (the `(supabase as any).rpc(...)` cast idiom, line ~68)
    - lmc-app/app/lib/supabase.ts (export name: `supabase`)
    - 09-RESEARCH.md "D-01", "D-02", "Pitfall 2" (try/catch race), "Pitfall 6" (null rating)
  </read_first>
  <action>
    In delivery.tsx:

    (A) D-01 Verified badge — make the badge inside the scout card conditional. Replace the always-on
    `<View style={styles.verifiedBadge}><Text style={styles.verifiedText}>✓ Verified</Text></View>`
    with a guarded render:
    ```tsx
    {clip?.gps_verified === true && (
      <View style={styles.verifiedBadge}><Text style={styles.verifiedText}>✓ Verified</Text></View>
    )}
    ```
    (gps_verified null = not checked, false = failed — neither shows the badge, per D-01.) Do NOT add a signage advisory (keep binary per D-01 discretion).

    (B) D-02 Scout identity:
    - Add `import { supabase } from '../lib/supabase';` at the top.
    - Add state: `const [scoutProfile, setScoutProfile] = useState<{ display_name: string | null; avg_rating: number | null; clip_count: number | null } | null>(null);`
    - Add a useEffect keyed on `[checkId]` that calls the RPC with the untyped cast and try/catch-safe `.catch(() => {})` (mirror the existing useEffect style; the RPC is not in database.types.ts yet so use `(supabase as any).rpc('get_check_scout_public', { p_check_id: checkId })` and read `data?.[0]`):
    ```tsx
    useEffect(() => {
      if (!checkId) return;
      (supabase as any)
        .rpc('get_check_scout_public', { p_check_id: checkId })
        .then(({ data }: { data: { display_name: string | null; avg_rating: number | null; clip_count: number | null }[] | null }) => {
          if (data?.[0]) setScoutProfile(data[0]);
        })
        .catch(() => {});
    }, [checkId]);
    ```
    - Derive display values before the return:
      `const scoutName = scoutProfile?.display_name ?? 'Your Scout';`
      `const scoutInitial = (scoutProfile?.display_name?.trim()?.[0] ?? 'S').toUpperCase();`
      build a meta string: rating part = `scoutProfile?.avg_rating != null ? \`⭐ ${scoutProfile.avg_rating}\` : null`; clips part = `scoutProfile?.clip_count != null ? \`${scoutProfile.clip_count} videos\` : null`; `const scoutMeta = [ratingPart, clipsPart].filter(Boolean).join(' · ');`
    - In the scout card JSX: avatar text -> `{scoutInitial}`; name -> `{scoutName}`; the rating line -> render `{scoutMeta ? <Text style={styles.scoutRating}>{scoutMeta}</Text> : null}` (hide the line entirely when no rating AND no clips, per Pitfall 6).

    Keep copy using "videos" (not "clips") per project rule. Keep the file under 500 lines (it is ~283; well within budget).
  </action>
  <acceptance_criteria>
    <automated>cd lmc-app && grep -q "get_check_scout_public" "app/(seeker)/delivery.tsx" && grep -q "gps_verified === true" "app/(seeker)/delivery.tsx" && ! grep -q "Jake C" "app/(seeker)/delivery.tsx" && ! grep -q "247 videos" "app/(seeker)/delivery.tsx" && grep -q "scoutProfile" "app/(seeker)/delivery.tsx"</automated>
  </acceptance_criteria>
  <verify>
    <automated>cd lmc-app && npx tsc --noEmit</automated>
  </verify>
  <done>Badge renders only on gps_verified===true; scout card shows RPC-driven name/initial/meta with graceful null handling; no "Jake C." or hardcoded rating remain; supabase imported; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| delivery.tsx -> get_check_scout_public | The client passes its own checkId; the server enforces ownership |
| clip.gps_verified -> badge render | Server-owned flag drives a trust-signal UI element |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-06 | Spoofing (false trust signal) | Verified badge | mitigate | Badge keyed strictly to server `clip.gps_verified === true`; null/false never render it. No client-side override. |
| T-09-07 | Information Disclosure | scout identity fetch | mitigate (server) | The RPC (Plan 01) enforces ownership + delivered-status; client merely renders the returned name/rating/count. Failure falls back to generic "Your Scout". |
| T-09-08 | Misrepresentation | fake AI verdict / crowd tags | mitigate | Removed entirely (D-03); no fabricated feature shown to Apple reviewers or users. |
</threat_model>

<verification>
- grep asserts AI/crowd/TAGS removed and styles.sectionLabel retained.
- grep asserts the RPC call + conditional badge present, and "Jake C."/"247 videos" gone.
- `npx tsc --noEmit` is clean (the RPC cast is untyped, so no missing-type error).
- On-device confirmation (real badge + real scout name) is a manual step listed in 09-VALIDATION.md, after Plan 04's live push.
</verification>

<success_criteria>
- Verified badge conditional on gps_verified===true.
- Scout card driven by get_check_scout_public with graceful null handling and "Your Scout" fallback.
- Fake AI Verdict + Crowd Report fully removed; shared sectionLabel style intact.
- tsc clean.
</success_criteria>

<output>
After completion, create `.planning/phases/09-verified-badge-scout-identity-quick-win-reconnects-surface-r/09-02-SUMMARY.md`
</output>
