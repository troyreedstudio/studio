---
phase: 09-verified-badge-scout-identity-quick-win-reconnects-surface-r
plan: 04
type: execute
wave: 3
depends_on: [01, 02, 03]
files_modified:
  - lmc-app/app/lib/database.types.ts
autonomous: true
requirements: [D-01, D-02, D-03, D-04]
must_haves:
  truths:
    - "Migration 0017 is applied to the live Supabase project"
    - "The 0017 pgTAP suite passes against the live DB (IDOR guard + columns verified)"
    - "database.types.ts includes notification_prefs, preferred_cities, and get_check_scout_public"
    - "The full app type-checks clean after the regen"
  artifacts:
    - path: "lmc-app/app/lib/database.types.ts"
      provides: "Regenerated types reflecting 0017 (columns + RPC)"
      contains: "notification_prefs"
  key_links:
    - from: "live Supabase DB"
      to: "0017 migration"
      via: "supabase db push"
      pattern: "0017"
    - from: "database.types.ts"
      to: "0017 schema"
      via: "supabase gen types typescript"
      pattern: "get_check_scout_public"
---

<objective>
[BLOCKING INTEGRATION GATE — orchestrator runs the live operations.] Apply Phase 9's single migration to the live database, prove it with pgTAP, regenerate the TypeScript types so the new columns + RPC are typed, and confirm the whole app type-checks. This is the only plan that touches live infra.

Purpose: Wave 1 wrote the migration + RED test; Wave 2 wrote client code against an untyped cast. This wave makes it real: push 0017, run the live pgTAP (the IDOR guard MUST pass), regenerate `database.types.ts`, and run a final `tsc` across the app.
Output: 0017 live; pgTAP green; `database.types.ts` regenerated; tsc clean.

NOTE (standing lesson): Phase 9 deploys NO edge functions — it is a migration-only backend change. If any unrelated webhook were redeployed, it must keep `--no-verify-jwt`; but no function deploy is expected here. Do not deploy functions.

The credentialed steps (supabase db push, supabase test db, supabase gen types) are run by the orchestrator/operator with project access (ref `cawqasszfbzvbtunamda`). If any step hits an auth error, surface it as a checkpoint rather than guessing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/09-verified-badge-scout-identity-quick-win-reconnects-surface-r/09-RESEARCH.md

@supabase/migrations/0017_phase9_surface_reconnects.sql
@supabase/tests/0017_phase9_reconnects.test.sql

<interfaces>
<!-- Live ops facts. -->
Supabase project ref: cawqasszfbzvbtunamda (from MEMORY: LMC backend LIVE).
Migrations dir: supabase/migrations/ ; tests dir: supabase/tests/.
Generated types path: lmc-app/app/lib/database.types.ts.
tsc command: `cd lmc-app && npx tsc --noEmit`.
pgTAP run: `supabase test db` (runs the whole supabase/tests suite) OR
  `pg_prove supabase/tests/0017_phase9_reconnects.test.sql` against the DB.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Push migration 0017 to the live DB + run the pgTAP suite</name>
  <files>supabase/migrations/0017_phase9_surface_reconnects.sql</files>
  <read_first>
    - supabase/migrations/0017_phase9_surface_reconnects.sql
    - supabase/tests/0017_phase9_reconnects.test.sql
  </read_first>
  <action>
    From the repo root (where supabase/ lives):
    1. `supabase db push` — applies 0017 to the linked live project (ref cawqasszfbzvbtunamda). If a link prompt or auth error appears, STOP and surface it (checkpoint) — do not force.
    2. `supabase test db` — runs the full pgTAP suite including 0017. The new file should now go GREEN (it was RED-by-design pre-push). The IDOR throws_ok + not-delivered throws_ok + column-existence assertions MUST pass.
    If 0017's tests fail, fix the migration (Plan 01 file) and re-push before proceeding — do not regen types against a broken schema.
  </action>
  <acceptance_criteria>
    <automated>supabase test db 2>&1 | grep -Eiq "0017|ok|pass" || echo "RUN-LIVE: supabase test db must pass 0017 (operator-run)"</automated>
  </acceptance_criteria>
  <verify>
    <automated>grep -q "get_check_scout_public" supabase/migrations/0017_phase9_surface_reconnects.sql</automated>
  </verify>
  <done>0017 applied to the live DB; the full pgTAP suite (incl. 0017 IDOR + column assertions) passes green.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate database.types.ts + final app type-check</name>
  <files>lmc-app/app/lib/database.types.ts</files>
  <read_first>
    - lmc-app/app/lib/database.types.ts (current generated file — confirm header/format)
  </read_first>
  <action>
    1. Regenerate types from the live schema into the existing path, e.g.
       `supabase gen types typescript --project-id cawqasszfbzvbtunamda > lmc-app/app/lib/database.types.ts`
       (use whatever flag form this project already uses — match the existing file's generator; if a linked-project form is configured, prefer `supabase gen types typescript --linked`).
    2. Confirm the regenerated file now includes `notification_prefs`, `preferred_cities` on the profiles Row, and `get_check_scout_public` in the Functions section.
    3. Run `cd lmc-app && npx tsc --noEmit` — must be clean. The Wave-2 `(supabase as any)` casts remain harmless; nothing should newly break. If a real type error surfaces (e.g. RPC arg shape), fix the calling client file minimally.
  </action>
  <acceptance_criteria>
    <automated>grep -q "notification_prefs" lmc-app/app/lib/database.types.ts && grep -q "preferred_cities" lmc-app/app/lib/database.types.ts && grep -q "get_check_scout_public" lmc-app/app/lib/database.types.ts</automated>
  </acceptance_criteria>
  <verify>
    <automated>cd lmc-app && npx tsc --noEmit</automated>
  </verify>
  <done>database.types.ts regenerated with both new columns + the RPC; `npx tsc --noEmit` is clean across the app.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator -> live Supabase project | Schema migration applied to production data store |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-12 | Tampering (bad migration to prod) | supabase db push | mitigate | 0017 is additive + idempotent (add column if not exists; create or replace function); pgTAP run immediately after push validates the IDOR guard before any client relies on it. |
| T-09-13 | Repudiation / drift | regenerated types vs live schema | mitigate | Types regenerated directly from the live project id; tsc confirms the client matches the deployed schema. |
</threat_model>

<verification>
- Live pgTAP suite passes (0017 IDOR + column assertions).
- database.types.ts contains the two new columns + the RPC.
- `npx tsc --noEmit` clean app-wide.
</verification>

<success_criteria>
- 0017 applied to live DB; pgTAP green.
- Types regenerated; the Wave-2 client code now matches the deployed schema.
- App type-checks clean. Phase 9 ready for on-device verification (real badge, real scout, persisted reconnects).
</success_criteria>

<output>
After completion, create `.planning/phases/09-verified-badge-scout-identity-quick-win-reconnects-surface-r/09-04-SUMMARY.md`
</output>
