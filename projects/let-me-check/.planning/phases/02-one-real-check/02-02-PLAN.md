---
phase: 02-one-real-check
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - lmc-app/app/lib/database.types.ts
autonomous: false
requirements: [CHECK-01, CHECK-02, CHECK-03, CHECK-05, CHECK-06, DISP-04]

must_haves:
  truths:
    - "Migrations 0007-0009 are applied to the live linked Supabase project (cawqasszfbzvbtunamda)"
    - "checks is live in the supabase_realtime publication"
    - "database.types.ts regenerated includes clips, accept_check, is_valid_check_transition, no_scout, and the location columns"
    - "The pgTAP suite (Phase-1 + the three Phase-2 tests) runs green against the DB"
  artifacts:
    - path: "lmc-app/app/lib/database.types.ts"
      provides: "Regenerated typed surface for the new SQL objects so Plan 03's lib compiles"
      contains: "accept_check"
  key_links:
    - from: "supabase db push --linked"
      to: "live Postgres (cawqasszfbzvbtunamda)"
      via: "applies 0007-0009"
      pattern: "0009_scout_rls_realtime"
    - from: "supabase gen types typescript --linked"
      to: "lmc-app/app/lib/database.types.ts"
      via: "regenerates the client types"
      pattern: "accept_check"
---

<objective>
[BLOCKING] Push Phase-2 migrations (0007-0009) to the live linked Supabase project, confirm the Realtime publication on `checks`, run the pgTAP suite, and regenerate `database.types.ts` so the Plan-03 client lib type-checks against the new `clips` table + `accept_check`/`is_valid_check_transition` functions + `no_scout` enum + location columns.

Purpose: The migrations are authored offline (Plan 01) but the live run is a human checkpoint — same constraint Phase 1 hit (no Docker / CLI login is environmental). Build/tsc pass WITHOUT the push because types come from the checked-in file, so this gate is what prevents a false-positive "green" before the schema is actually live.

Output: Live schema applied + a regenerated, committed `database.types.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-one-real-check/02-RESEARCH.md
@.planning/phases/01-foundation-auth-persistence-event-log/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-push offline gate — migrations present + ordered, tsc clean on current types</name>
  <files>(read-only verification)</files>
  <read_first>
    - supabase/migrations/0007_check_transitions.sql
    - supabase/migrations/0008_clips_location.sql
    - supabase/migrations/0009_scout_rls_realtime.sql
  </read_first>
  <action>
Confirm the three Plan-01 migrations exist and are correctly ordered after 0006, then confirm the app still type-checks against the CURRENT (pre-regen) types so the only thing the push changes is the live schema + regenerated types. Run: list `supabase/migrations/` (expect 0001-0009), and `cd lmc-app && npx tsc --noEmit` (expect exit 0). Do NOT edit any file in this task — it is the safety check before the live push.
  </action>
  <verify>
    <automated>ls supabase/migrations/0007_check_transitions.sql supabase/migrations/0008_clips_location.sql supabase/migrations/0009_scout_rls_realtime.sql >/dev/null && cd lmc-app && npx tsc --noEmit && echo PREPUSH_OK</automated>
  </verify>
  <acceptance_criteria>
    - All three migration files exist (ls exits 0)
    - `cd lmc-app && npx tsc --noEmit` exits 0 against the current checked-in database.types.ts
  </acceptance_criteria>
  <done>Migrations present and ordered; tsc clean pre-push; ready for the live checkpoint.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [BLOCKING] Push 0007-0009 live + confirm Realtime publication + run pgTAP</name>
  <files>(no files — human-action checkpoint: live push + publication toggle + pgTAP run)</files>
  <action>
This is a human-action checkpoint, not an automated edit. The agent pauses; Troy runs the live push from the repo root with the Supabase CLI logged in and the project linked: (1) `supabase db push --linked` to apply 0007-0009; (2) confirm the Realtime publication includes `checks` (Dashboard Database -> Publications -> supabase_realtime, or `select tablename from pg_publication_tables where pubname = 'supabase_realtime' and tablename = 'checks';` returns one row); (3) run the pgTAP suite via `supabase db reset && supabase test db` if Docker is available, else run the three Phase-2 test files in the SQL editor against staging and confirm no failures. See how-to-verify for the exact steps. Resume with "pushed".
  </action>
  <what-built>Migrations 0007 (transition hardening + accept_check), 0008 (clips + location + no_scout), 0009 (Scout RLS + realtime publication) are authored and offline-verified. The live project `cawqasszfbzvbtunamda` is linked.</what-built>
  <how-to-verify>
From the repo root (`projects/let-me-check`), with the Supabase CLI logged in and the project linked:

1. Apply the new migrations to the live DB:
   `supabase db push --linked`
   (expect it to apply 0007, 0008, 0009; if it reports they are already applied, that is fine.)

2. Confirm the Realtime publication includes `checks`. Either in the Dashboard (Database → Publications → supabase_realtime → ensure `checks` is toggled on), or by SQL:
   `select tablename from pg_publication_tables where pubname = 'supabase_realtime' and tablename = 'checks';`
   (expect one row.)

3. Run the pgTAP suite (Phase-1 + the three Phase-2 tests). If Docker is available locally:
   `supabase db reset && supabase test db`
   (expect all tests, including check_transitions / accept_check_atomic / scout_rls, to pass.)
   If Docker is NOT available, run the three Phase-2 test files manually in the SQL editor against a scratch/staging DB and confirm `finish()` reports no failures.

Reply "pushed" when 0007-0009 are applied and the publication shows `checks`, or paste any error.
  </how-to-verify>
  <verify>Manual: `select tablename from pg_publication_tables where pubname = 'supabase_realtime' and tablename = 'checks';` returns one row; pgTAP suite reports no failures. No CI command — requires the linked live project + credentials.</verify>
  <done>0007-0009 applied to the live project; the supabase_realtime publication includes checks; the pgTAP suite (incl. the three Phase-2 tests) is green or manually confirmed.</done>
  <resume-signal>Type "pushed" once migrations are live and the realtime publication includes checks, or paste any error to resolve.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Regenerate database.types.ts from the live schema</name>
  <files>lmc-app/app/lib/database.types.ts</files>
  <read_first>
    - lmc-app/app/lib/database.types.ts (the current generated file — confirm it lacks clips/accept_check before regen)
  </read_first>
  <action>
After the push (Task 2) is confirmed, regenerate the typed client surface so Plan 03 compiles against the new objects. Run from the repo root:
`supabase gen types typescript --linked > lmc-app/app/lib/database.types.ts`
This overwrites the generated file (it is a generated artifact — full overwrite is correct). The regenerated file MUST now contain the `clips` table type, the `accept_check` and `is_valid_check_transition` functions, the `no_scout` enum value, and `requested_lat`/`requested_lng`/`location_label` on checks. Then run `cd lmc-app && npx tsc --noEmit` to confirm the existing code still compiles against the regenerated types (expect exit 0).
  </action>
  <verify>
    <automated>grep -q "accept_check" lmc-app/app/lib/database.types.ts && grep -q "clips" lmc-app/app/lib/database.types.ts && grep -q "no_scout" lmc-app/app/lib/database.types.ts && grep -q "location_label" lmc-app/app/lib/database.types.ts && cd lmc-app && npx tsc --noEmit && echo TYPES_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "accept_check" lmc-app/app/lib/database.types.ts` (function in the regenerated types)
    - `grep -q "is_valid_check_transition" lmc-app/app/lib/database.types.ts`
    - `grep -q "no_scout" lmc-app/app/lib/database.types.ts` (enum value)
    - `grep -q "location_label" lmc-app/app/lib/database.types.ts` (new checks column)
    - `cd lmc-app && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>database.types.ts regenerated from the live schema, contains all new objects, and the app still type-checks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local CLI (access token) → live Supabase project | The push applies DDL to production; an access token is the credential — never commit it |
| generated types file → client build | A stale types file would let new lib code type-check against objects that aren't live (false green) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08 | Spoofing/Info Disclosure | Supabase access token committed to git | mitigate | Token used only in the CLI env for the push; never written to a tracked file; .env stays gitignored (Phase-1 posture). |
| T-02-09 | Tampering (false green) | Plan 03/04/05 build passes against a schema that isn't live | mitigate | This BLOCKING gate forces the push + regen BEFORE downstream plans run; tsc against regenerated types proves the live objects exist. |
</threat_model>

<verification>
- 0007-0009 applied live; `pg_publication_tables` shows `checks` under `supabase_realtime`.
- pgTAP suite green (or the three Phase-2 files manually confirmed passing).
- database.types.ts regenerated, contains the new objects, tsc exits 0.
</verification>

<success_criteria>
- The live schema matches the authored migrations, Realtime is enabled on checks, and the client types reflect the new SQL objects.
</success_criteria>

<output>
After completion, create `.planning/phases/02-one-real-check/02-02-SUMMARY.md`.
</output>
