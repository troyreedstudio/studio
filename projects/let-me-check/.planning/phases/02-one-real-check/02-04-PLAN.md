---
phase: 02-one-real-check
plan: 04
type: execute
wave: 4
depends_on: ["02-03"]
files_modified:
  - lmc-app/app/(seeker)/payment.tsx
  - lmc-app/app/(seeker)/finding.tsx
  - lmc-app/app/(seeker)/waiting.tsx
  - lmc-app/app/(seeker)/delivery.tsx
autonomous: false
requirements: [CHECK-01, CHECK-05, CHECK-06, DISP-04]

must_haves:
  truths:
    - "Confirming a request creates a real check server-side and carries the real checkId forward"
    - "The Seeker sees live status driven by real transitions (dispatching/assigned/filming/delivered) — the fake setInterval countdown and 'Skip ahead · prototype' link are gone"
    - "Honest terminal states route correctly: cancelled -> cancelled.tsx, no_scout/expired -> error.tsx"
    - "The delivery screen shows the real check + clips metadata (when/where filmed) and a 5-star tap persists the rating"
  artifacts:
    - path: "lmc-app/app/(seeker)/payment.tsx"
      provides: "Confirm calls createCheck() and routes to finding with the real checkId (NO real payment)"
      contains: "createCheck"
    - path: "lmc-app/app/(seeker)/finding.tsx"
      provides: "Subscribes to the real check; routes forward on assigned, to error on no_scout"
      contains: "subscribeToCheck"
    - path: "lmc-app/app/(seeker)/waiting.tsx"
      provides: "Live status off subscribeToCheck; fake countdown + Skip-ahead removed; terminal routing"
      contains: "subscribeToCheck"
    - path: "lmc-app/app/(seeker)/delivery.tsx"
      provides: "Real check + clips metadata + rateCheck() on star tap"
      contains: "rateCheck"
  key_links:
    - from: "payment.tsx confirm"
      to: "createCheck()"
      via: "import from app/lib/checks; route to finding with checkId"
      pattern: "createCheck\\("
    - from: "waiting.tsx"
      to: "subscribeToCheck(checkId)"
      via: "import from app/lib/realtime; drive status off the row"
      pattern: "subscribeToCheck\\("
    - from: "delivery.tsx star tap"
      to: "rateCheck(checkId, stars)"
      via: "import from app/lib/checks"
      pattern: "rateCheck\\("
---

<objective>
Wire the Seeker side of the real check loop: confirming a request creates a real check (CHECK-01, NO money), `finding.tsx` + `waiting.tsx` watch live status via `subscribeToCheck` (DISP-04) — deleting the fake `setInterval` countdown and the "Skip ahead · prototype" link — and `delivery.tsx` shows real clip metadata + persists the rating (CHECK-05/06).

Purpose: This is the Seeker-visible payoff of the phase: a genuine check, watched live, with honest failure paths, ending in a persisted rating. The data/subscription logic comes from the Plan-03 lib so these (already 500+ line) screens don't grow with business logic.

Output: Four wired Seeker screens carrying a real `checkId` end-to-end, no fake countdown, real terminal routing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-one-real-check/02-RESEARCH.md

<interfaces>
<!-- From Plan 03 (read app/lib/checks.ts + app/lib/realtime.ts before editing): -->
- createCheck(input: { tier:'standard'|'priority'; locationLabel:string; lat?:number; lng?:number; venueId?:string; marketId?:string; currency?:string }): Promise<string>
- getCheck(checkId: string): Promise<CheckRow | null>
- rateCheck(checkId: string, stars: number): Promise<void>
- cancelCheck(checkId: string): Promise<void>
- subscribeToCheck(checkId, onStatus: (row: CheckRow) => void, onError?: () => void): () => void
- type CheckRow has: id, status (check_status incl 'no_scout'), tier, location_label, requested_lat/lng, scout_id, created_at, updated_at

Existing screen flow + params (from grep):
- venue.tsx -> router.push('/(seeker)/payment', { venue, city, tier, time, ... })
- payment.tsx confirm currently: setTimeout -> router.replace('/(seeker)/finding', { venue, city, tier, time, total })
- finding.tsx: useLocalSearchParams; setInterval status cycling; setTimeout -> router.replace('/(seeker)/confirmed', {...}); a no-scouts button -> error.tsx { type:'no-scouts' }
- waiting.tsx: useLocalSearchParams { venue, city, tier, time }; setInterval countdown (line 179); setTimeout -> delivery (line 185); "Skip ahead · prototype" link (line ~435)
- delivery.tsx: useLocalSearchParams { venue, city }; const [rating,setRating]; stars map setRating(star) (line ~74)
- terminal screens that exist: cancelled.tsx, error.tsx, confirmed.tsx
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: payment.tsx confirm -> createCheck() (CHECK-01, no money) + carry real checkId</name>
  <files>lmc-app/app/(seeker)/payment.tsx</files>
  <read_first>
    - lmc-app/app/(seeker)/payment.tsx (the confirm handler at ~line 227-233 and the params it reads)
    - lmc-app/app/lib/checks.ts (createCheck signature + CheckRow)
    - lmc-app/app/(seeker)/venue.tsx (what params it forwards to payment — venue/city/tier/time)
  </read_first>
  <action>
In payment.tsx, replace the fake confirm `setTimeout(... router.replace('/(seeker)/finding', { venue, city, tier, time, total }))` with a real create. On confirm press (when a card exists — keep the existing card-gating UX, but do NOT call any Stripe/PaymentIntent; money is Phase 4): set processing true, then `const checkId = await createCheck({ tier: (tier === 'priority' ? 'priority' : 'standard'), locationLabel: String(venue), marketId: <existing market param if present, else undefined>, venueId: <existing venueId param if present, else undefined> })`. On success, `router.replace({ pathname: '/(seeker)/finding', params: { checkId, venue: String(venue), city: String(city), tier: String(tier), time: String(time) } })`. Wrap in try/catch; on error keep the user on payment and surface a simple inline error/toast (reuse any existing error affordance, else a minimal Alert). Leave a `// TODO(phase-4): authorize a Stripe hold here` at the confirm site so the money seam is documented. Keep the recurring-setup branch untouched. Import createCheck from '../lib/checks'. Do NOT add bulk to the file — the handler change is small.
  </action>
  <verify>
    <automated>cd lmc-app && npx tsc --noEmit && grep -q "createCheck(" app/'(seeker)'/payment.tsx && grep -q "checkId" app/'(seeker)'/payment.tsx && grep -q "TODO(phase-4)" app/'(seeker)'/payment.tsx && echo PAYMENT_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "createCheck(" app/(seeker)/payment.tsx` and it passes a `tier` of 'standard'|'priority'
    - confirm routes to finding with a `checkId` param: `grep -q "checkId" app/(seeker)/payment.tsx`
    - NO Stripe/PaymentIntent call added; a `TODO(phase-4)` money seam comment is present
    - `cd lmc-app && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Confirming a request creates a real check and forwards the real checkId to finding; no real payment; money seam documented.</done>
</task>

<task type="auto">
  <name>Task 2: finding.tsx + waiting.tsx -> live status via subscribeToCheck (DISP-04); delete the fake countdown</name>
  <files>lmc-app/app/(seeker)/finding.tsx, lmc-app/app/(seeker)/waiting.tsx</files>
  <read_first>
    - lmc-app/app/(seeker)/finding.tsx (the setInterval status cycling + setTimeout->confirmed + no-scouts button)
    - lmc-app/app/(seeker)/waiting.tsx (the setInterval countdown line 179, the setTimeout->delivery line 185, the "Skip ahead · prototype" link ~line 435)
    - lmc-app/app/lib/realtime.ts (subscribeToCheck) + lmc-app/app/lib/checks.ts (getCheck, cancelCheck, CheckRow)
    - .planning/phases/02-one-real-check/02-RESEARCH.md (Code Examples — "Driving waiting.tsx off real status")
  </read_first>
  <action>
Both screens read `checkId` from useLocalSearchParams and drive their UI off the real row.
finding.tsx (the "searching for a Scout" phase = status 'dispatching'): on mount `getCheck(checkId).then(setCheck)` then `const unsub = subscribeToCheck(checkId, setCheck, () => getCheck(checkId).then(setCheck)); return unsub;`. REMOVE the `setTimeout(...->confirmed)` fake-match and the status-cycling that fakes progress (you may keep the animated status COPY but it must not drive navigation). On `check.status === 'assigned'` route forward to waiting (`router.replace({ pathname:'/(seeker)/waiting', params:{ checkId, venue, city, tier, time } })`); on `'no_scout' | 'expired'` route to error.tsx with `{ type:'no-scouts', reason: check.status }`; on `'cancelled'` route to cancelled.tsx. Keep the manual "cancel" affordance wired to `cancelCheck(checkId)`.
waiting.tsx (status 'assigned' -> 'filming' -> 'delivered'): replace the countdown driver — DELETE the `setInterval` that decrements secondsLeft (line ~179) and the `setTimeout(...->delivery)` (line ~185) and the "Skip ahead · prototype" link (~line 435). Add `getCheck(checkId).then(setCheck)` + `subscribeToCheck(checkId, setCheck, refetch)`. Map `check.status` to the existing step UI (assigned=accepted, filming=filming). On `'delivered' | 'rated'` `router.replace({ pathname:'/(seeker)/delivery', params:{ checkId } })`; on `'cancelled'` -> cancelled.tsx; on `'no_scout' | 'expired'` -> error.tsx. Add an AppState 'active' listener that re-fetches via getCheck on foreground (Pitfall 4 reconciliation). The Mapbox scout-jitter cosmetic can stay; it must not drive navigation. Keep both files lean — put no business logic inline beyond the status->route mapping (the lib does the data work).
  </action>
  <verify>
    <automated>cd lmc-app && npx tsc --noEmit && grep -q "subscribeToCheck(" app/'(seeker)'/finding.tsx && grep -q "subscribeToCheck(" app/'(seeker)'/waiting.tsx && ! grep -q "Skip ahead" app/'(seeker)'/waiting.tsx && grep -q "no_scout\|no-scouts" app/'(seeker)'/finding.tsx && echo LIVE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "subscribeToCheck(" app/(seeker)/finding.tsx` and `grep -q "subscribeToCheck(" app/(seeker)/waiting.tsx`
    - `! grep -q "Skip ahead" app/(seeker)/waiting.tsx` (prototype skip link removed)
    - waiting.tsx no longer has a countdown `setInterval` that decrements `secondsLeft` (the timer is gone) — confirm by reading; only the cosmetic scout-jitter interval may remain
    - finding/waiting route to error.tsx on `no_scout`/`expired` and cancelled.tsx on `cancelled`
    - cancel affordance calls `cancelCheck(`
    - `cd lmc-app && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>finding + waiting are driven by real status via subscribeToCheck; the fake countdown and Skip-ahead are gone; terminal states route honestly.</done>
</task>

<task type="auto">
  <name>Task 3: delivery.tsx -> real check + clips metadata + rateCheck() (CHECK-05/06)</name>
  <files>lmc-app/app/(seeker)/delivery.tsx</files>
  <read_first>
    - lmc-app/app/(seeker)/delivery.tsx (the stars map at ~line 74 + the hard-coded "Filmed 2 min ago" / scout placeholder copy)
    - lmc-app/app/lib/checks.ts (getCheck, rateCheck, CheckRow)
  </read_first>
  <action>
In delivery.tsx, read `checkId` from useLocalSearchParams. On mount, `getCheck(checkId)` to load the real row; also fetch the check's clip metadata for the "when/where filmed" line — prefer adding a `getCheckClip(checkId)` helper to lib/checks.ts (`supabase.from('clips').select('filmed_at, filmed_lat, filmed_lng').eq('check_id', checkId).maybeSingle()`) and calling it, to keep the screen thin. Replace the hard-coded "Filmed 2 min ago" with the real `filmed_at` (formatted) and the location with `check.location_label`; if clip metadata is null, show a graceful fallback. Wire the 5-star tap: keep `setRating(star)` for the visual, and on tap (or a confirm button if one exists) call `await rateCheck(checkId, star)`; guard against double-submit. The real video source stays the existing placeholder/sample (real Mux playback is Phase 3) — do not add a real video pipeline. The "AI VERDICT" line + crowd tags stay as static placeholder UI (Phase 6). Keep the file lean.
  </action>
  <verify>
    <automated>cd lmc-app && npx tsc --noEmit && grep -q "rateCheck(" app/'(seeker)'/delivery.tsx && grep -q "getCheck(\|getCheckClip(" app/'(seeker)'/delivery.tsx && grep -q "checkId" app/'(seeker)'/delivery.tsx && echo DELIVERY_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "rateCheck(" app/(seeker)/delivery.tsx` (rating persists)
    - delivery loads the real row: `grep -q "getCheck(" app/(seeker)/delivery.tsx`
    - clip metadata read present (getCheckClip helper or a clips select) and used for "when/where filmed"
    - the hard-coded "Filmed 2 min ago" string is replaced/removed: `! grep -q "Filmed 2 min ago" app/(seeker)/delivery.tsx`
    - `cd lmc-app && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>delivery shows the real check + clip metadata and a star tap persists the rating to the ratings table via rateCheck.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: On-device live-status walk-through (DISP-04 + CHECK-01/05/06)</name>
  <files>(no files — human verification)</files>
  <action>
This is a human-verification checkpoint, not an automated edit. The agent pauses here; Troy runs the on-device walk-through described below and replies "verified" or reports what stuck. Run this after BOTH Plan 04 and Plan 05 land (the Scout side drives the transitions the Seeker watches), on a dev build with two sessions against the live linked Supabase.
  </action>
  <what-built>The Seeker flow is wired to real checks: confirm creates a check, finding/waiting watch live status via Realtime, delivery shows real clip metadata + persists a rating. (Requires Plan 05's Scout side to drive the transitions; run this after both 04 and 05 land, on a dev build with two sessions.)</what-built>
  <how-to-verify>
On a dev build (Realtime + Mapbox need a dev build, not Expo Go), with a live linked Supabase:
1. As a Seeker (session A): browse a venue, pick a tier, confirm. Confirm a real `checks` row appears in Supabase with status 'dispatching' and your seeker_id.
2. Stay on the finding/waiting screen. From a Scout session (B) on another device/simulator, accept the check (Plan 05). Confirm the Seeker's screen moves finding -> waiting (accepted) WITHOUT a manual refresh — that is live status (DISP-04).
3. Let the Scout proceed to filming -> submit (Plan 05). Confirm the Seeker's screen advances to delivery automatically.
4. On delivery: confirm "when/where filmed" reflects the real clip's filmed_at + your location_label (not "Filmed 2 min ago"). Tap a star rating; confirm a row appears in the `ratings` table and the check is now 'rated' in Supabase.
5. Failure path: create a second check, and from the Scout side (or via a test transition_check to 'no_scout') confirm the Seeker lands on error.tsx, and that a cancel from the Seeker lands on cancelled.tsx.
Reply "verified" if the status moves live and the rating persists, or describe what stuck.
  </how-to-verify>
  <verify>Manual on-device verification — see how-to-verify steps. No automated command (true live Realtime across two sessions cannot be asserted in CI).</verify>
  <done>Troy confirms the Seeker watches status move live across the loop and the rating persists, or reports the issue to resolve.</done>
  <resume-signal>Type "verified" once the Seeker watches status live and the rating persists, or describe the issue.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Seeker screen → server (createCheck/rateCheck) | The client requests state changes; the server RPCs authorize (seeker owns the check) |
| Realtime channel → this Seeker | Postgres Changes RLS-checks each event; only the owning Seeker receives their check's updates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-13 | Elevation | A Seeker rates a check they do not own | mitigate | rateCheck inserts ratings WITH CHECK auth.uid()=seeker_id (Phase-1 RLS) and transition_check 'rated' requires v_uid=v_seeker (0007). Verified server-side + the on-device walk-through. |
| T-02-06 | Information Disclosure | Seeker sees another Seeker's check via the live channel | mitigate | subscribeToCheck filters id=eq.<own checkId>; checks_select_own RLS confines Postgres Changes delivery; realtime token = session (on-device verify). |
| T-02-14 | Tampering | Client drives status by faking a transition (carrying forward the prototype skip) | mitigate | The fake setInterval/Skip-ahead are deleted; navigation is driven by the real row only; the client has no UPDATE path on checks. |
</threat_model>

<verification>
- `cd lmc-app && npx tsc --noEmit` exits 0 after all three screen edits.
- Greps: createCheck in payment, subscribeToCheck in finding + waiting, rateCheck in delivery; "Skip ahead" and "Filmed 2 min ago" removed.
- On-device checkpoint (Task 4) confirms live status + persisted rating (run with Plan 05).
</verification>

<success_criteria>
- A Seeker can request a real check, watch its status move live through real transitions (incl. honest no-Scout/cancelled paths), watch the delivered result with true clip metadata, and rate it — the rating persisting to the ratings table.
</success_criteria>

<output>
After completion, create `.planning/phases/02-one-real-check/02-04-SUMMARY.md`.
</output>
