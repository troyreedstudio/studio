---
phase: 05-verification-moat-dispatch-geofenced-dispatch-only-scouts-in
plan: 06
type: execute
wave: 4
depends_on: ["05-01", "05-02", "05-03", "05-04", "05-05"]
files_modified:
  - lmc-app/app/lib/database.types.ts
autonomous: false
requirements: [DISP-01, DISP-02, DISP-03, SCOUT-03, VER-01, SAFE-01]
user_setup:
  - service: google-cloud-vision
    why: "Advisory signage detection (D-06) — reads the venue sign on a clip; never gates delivery"
    env_vars:
      - name: GOOGLE_VISION_API_KEY
        source: "Google Cloud Console -> APIs & Services -> Credentials -> Create API key (enable Cloud Vision API). Set via: supabase secrets set GOOGLE_VISION_API_KEY=..."
must_haves:
  truths:
    - "0012 + 0012b migrations are live; all Phase-5 pgTAP suites pass against the live DB"
    - "verify-clip + signage-check Edge Functions are deployed; GOOGLE_VISION_API_KEY secret is set"
    - "database.types.ts is regenerated so the client sees scout_locations/market_config/new columns"
    - "On device: a Scout in radius gets the job; an off-fence clip is auto-rejected + re-dispatched (Seeker not charged); signage flag is recorded but never blocks"
  artifacts:
    - path: "lmc-app/app/lib/database.types.ts"
      provides: "regenerated Supabase types incl. scout_locations, market_config, no_film_zones, new clips/checks columns"
      contains: "scout_locations"
  key_links:
    - from: "live Supabase"
      to: "pgTAP + deployed Edge Functions + Vision secret"
      via: "db push + functions deploy + secrets set"
      pattern: "supabase"
---

<objective>
Take Phase 5 live: push migrations 0012 + 0012b, run all Phase-5 pgTAP suites against the live DB, deploy the verify-clip + signage-check Edge Functions (and redeploy the modified mux-webhook + mux-upload-url), set the GOOGLE_VISION_API_KEY secret (human checkpoint, like the Stripe/Mux keys), regenerate database.types.ts, then run the on-device geo walk-through (two sessions, real GPS) confirming dispatch + GPS auto-reject + advisory signage.

Purpose: every prior Phase-5 plan was offline-verified; nothing is actually true until the schema and functions are live and the end-to-end geo loop is walked on a real device. This is the [BLOCKING] gate.
Output: live migrations + functions + Vision secret + regenerated types + a signed-off on-device walk-through.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-verification-moat-dispatch-geofenced-dispatch-only-scouts-in/05-CONTEXT.md
@.planning/phases/05-verification-moat-dispatch-geofenced-dispatch-only-scouts-in/05-RESEARCH.md

<interfaces>
<!-- Live-deploy patterns from prior phases (mirror exactly): -->
  Phase 3 (03-04) + Phase 4 (04-05) both: SUPABASE_ACCESS_TOKEN set for non-TTY -> supabase db push -> supabase functions deploy <name> -> supabase secrets set -> supabase gen types typescript --linked > lmc-app/app/lib/database.types.ts
  Webhooks deployed with --no-verify-jwt; user/service functions keep Supabase JWT (STATE.md Phase-4 note).
  verify-clip + signage-check are service-role-invoked from mux-webhook (server-to-server) -> deploy with --no-verify-jwt is acceptable (they are not called with a user JWT); confirm against the mux-webhook deploy flag used in 03-04.
  Project ref: cawqasszfbzvbtunamda (from MEMORY: LMC Supabase backend live).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: [BLOCKING] Provide Google Vision API key</name>
  <action>PAUSE for the human to supply a Google Cloud Vision API key (or reply "skip signage"). Claude cannot create a billing-enabled GCP API key on the user's behalf. The key is consumed by Task 2 via `supabase secrets set GOOGLE_VISION_API_KEY=...`. See what-built / how-to-verify / resume-signal below.</action>
  <what-built>signage-check Edge Function (Plan 04) is ready; it needs a Google Cloud Vision API key to run the advisory signage read. Without it, signage-check degrades to signage_confirmed=null (no error) — but the advisory signal stays dark until the key is set.</what-built>
  <how-to-verify>
    1. In Google Cloud Console, enable the Cloud Vision API and create an API key (APIs & Services -> Credentials -> Create credentials -> API key).
    2. Restrict the key to the Cloud Vision API.
    3. Provide the key so it can be set as a Supabase secret: `supabase secrets set GOOGLE_VISION_API_KEY=<key>` (this is run in Task 2).
    Note: signage is ADVISORY only (D-06) — if you prefer to defer signage entirely, reply "skip signage" and Phase 5 ships with GPS dispatch + auto-reject live and signage dark (signage_confirmed=null); the key can be added later with zero code change.
  </how-to-verify>
  <resume-signal>Paste the Vision API key (it will be stored only as a Supabase secret, never committed), or reply "skip signage" to ship without it.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] db push 0012 + 0012b, run live pgTAP, deploy functions, set Vision secret, regen types</name>
  <read_first>
    - .planning/phases/03-video-pipeline/03-04-* (the Phase-3 live-deploy SUMMARY/PLAN — exact db push + functions deploy + gen types command sequence + non-TTY token handling)
    - .planning/phases/04-payments-stripe-connect-express-card-hold-at-request-capture/04-05-* (Phase-4 live-deploy flow incl. --no-verify-jwt webhook flag)
    - supabase/migrations/0012_dispatch_verification_spine.sql, supabase/migrations/0012b_dispatch_rpc_accept.sql
    - supabase/tests/0012_geo_spatial.test.sql, supabase/tests/0012b_dispatch_accept.test.sql
  </read_first>
  <files>lmc-app/app/lib/database.types.ts</files>
  <action>
Run the live deploy mirroring the 03-04 / 04-05 sequence (non-TTY: export SUPABASE_ACCESS_TOKEN first; project already linked to ref cawqasszfbzvbtunamda — re-link if needed):

1. `supabase db push` — applies 0012 then 0012b. If the push errors on the WKT-on-geography insert/upsert assumption (RESEARCH A1) or any forward-reference, fix in the migration and re-push (do NOT edit live SQL by hand).
2. `supabase test db` — run ALL pgTAP incl. the two new Phase-5 suites (0012_geo_spatial, 0012b_dispatch_accept). They MUST pass GREEN. The lng/lat-order + 30 m film-fence boundary assertions are the critical ones. Fix any RED before proceeding.
3. Deploy Edge Functions: `supabase functions deploy verify-clip`, `supabase functions deploy signage-check`, and REDEPLOY the modified `supabase functions deploy mux-webhook` and `supabase functions deploy mux-upload-url`. Use the SAME verify-jwt posture as 03-04 for each (mux-webhook stays --no-verify-jwt; verify-clip + signage-check are service-role-invoked server-to-server — deploy --no-verify-jwt unless 03-04 shows otherwise).
4. If a Vision key was provided (Task 1): `supabase secrets set GOOGLE_VISION_API_KEY=<key>`. If "skip signage": do not set it (signage-check returns null gracefully).
5. Regenerate types: `supabase gen types typescript --linked > lmc-app/app/lib/database.types.ts`. Confirm it now contains scout_locations, market_config, no_film_zones, and the new clips/checks columns (coord, gps_verified, filmed_accuracy_m, signage_confirmed).
6. `cd lmc-app && npx tsc --noEmit` — the regenerated types must not break the Plan-05 client code (createCheck coord, dispatch/scout-location helpers).

Capture, in the SUMMARY: the migration push result, the pgTAP pass count, the deployed function list + their verify-jwt flags, whether the Vision secret was set, and the types-regen confirmation.
  </action>
  <verify>
    <automated>grep -q "scout_locations" lmc-app/app/lib/database.types.ts && grep -q "market_config" lmc-app/app/lib/database.types.ts && grep -q "signage_confirmed" lmc-app/app/lib/database.types.ts && cd lmc-app && npx tsc --noEmit 2>&1 | tail -3</automated>
  </verify>
  <done>0012 + 0012b live; live `supabase test db` GREEN (incl. lng/lat-order + 30 m boundary); verify-clip + signage-check deployed; mux-webhook + mux-upload-url redeployed; Vision secret set (or skipped); database.types.ts regenerated with the new tables/columns; tsc passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [BLOCKING] On-device geo walk-through (dispatch + GPS auto-reject + advisory signage)</name>
  <action>PAUSE for the human to run the on-device two-session geo walk-through on a fresh EAS dev build. Claude cannot drive real GPS on two physical devices. Steps + pass criteria are in how-to-verify; resume on "approved".</action>
  <what-built>Full Phase-5 loop is live: geo-filtered dispatch, the GPS film-fence auto-reject (re-dispatch, Seeker not charged), and advisory signage. This needs real devices with real GPS to verify (two sessions: a Seeker and a Scout).</what-built>
  <how-to-verify>
    Use a fresh EAS dev build (rebuild if native expo-location config changed). Two sessions per the STATE.md 02-04/04 cross-session pattern.
    1. DISPATCH (DISP-01): As a Scout, go online near a venue (within ~1.5 km). As the Seeker, request a check at that venue. Confirm the Scout sees it in the dashboard. Then move/simulate the Scout > 2 km away (or use a second Scout far away) and confirm they do NOT see it.
    2. ONE ACTIVE JOB (D-03): With the Scout already on an accepted job, confirm a second open check cannot be accepted (error surfaced).
    3. GPS AUTO-REJECT (D-04/D-05): Film and submit a clip from MORE than 30 m from the venue. Confirm: the clip is auto-rejected, the check returns to dispatching (re-dispatch), the Seeker is NOT charged (no Stripe capture), and the Scout is not paid. Then film from on-site (within ~25 m) and confirm it delivers + captures normally.
    4. ADVISORY SIGNAGE (D-06): With a clip where the sign is readable, confirm delivery still happens (signage never blocks) and signage_confirmed is recorded; with an unreadable sign, confirm it still delivers and records "couldn't confirm" (null/false) — NEVER a rejection.
    5. Confirm event_log rows exist for check.accepted, check.gps_verified / check.gps_rejected, check.redispatched, check.signage_checked.
  </how-to-verify>
  <resume-signal>Reply "approved" when all five pass, or describe what failed (e.g. an off-fence clip that still delivered, a far Scout that saw the job, or a signage misread that rejected a clip).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → live Supabase | db push / functions deploy / secrets set with privileged token |
| GOOGLE_VISION_API_KEY → Supabase secret | Secret crosses into Deno.env only; never committed or returned to client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-26 | Information Disclosure | Vision API key in repo | mitigate | Key provided via checkpoint, set as Supabase secret (supabase secrets set), never written to a committed file; CLAUDE.md forbids committing secrets |
| T-05-27 | Tampering | verify-clip callable with user JWT to skip gate | mitigate | verify-clip is service-role-invoked from mux-webhook only; even if reachable, it only reports distance + sets gps_verified — it cannot deliver (mux-webhook owns that) |
| T-05-28 | Spoofing | simulated GPS in dev passes the on-device test falsely | accept | Dev-build simulated GPS is a TEST aid; production mock-GPS detection is Phase 6 (VER-05), explicitly deferred |
| T-05-29 | Repudiation | no audit of the live verification events | mitigate | Walk-through Step 5 confirms event_log rows for accept/gps_verified/gps_rejected/redispatch/signage (DATA-04) |
</threat_model>

<verification>
- `supabase test db` GREEN against live (incl. 0012_geo_spatial + 0012b_dispatch_accept)
- `grep -q "scout_locations" lmc-app/app/lib/database.types.ts` (Task 2)
- On-device walk-through Steps 1-5 approved (Task 3)
</verification>

<success_criteria>
- Migrations + Edge Functions live; live pgTAP green; types regenerated; tsc passes.
- On device: nearby Scout gets the job, far Scout does not; off-fence clip auto-rejected + re-dispatched with no charge; on-site clip delivers; signage advisory recorded but never blocks.
- event_log captures every dispatch + verification event.
</success_criteria>

<output>
After completion, create `.planning/phases/05-verification-moat-dispatch-geofenced-dispatch-only-scouts-in/05-06-SUMMARY.md`
</output>
