node_modules/
.expo/
.next/
dist/
.env
.env.*
!.env.example
*.log
.DS_Store

# Flutter
.dart_tool/
build/
.gradle/
.idea/
*.iml
.flutter-plugins
.flutter-plugins-dependencies
.packages

# iOS/Android build artifacts
ios/Pods/
ios/.symlinks/
android/.gradle/
android/app/build/

# Claude Code / RuFlo / agent tooling (machine-generated, not source)
.claude-flow/
.claude/
.swarm/
.mcp.json
*.db

# Generated security-audit reports (noisy, regenerated on demand)
SECURITY_*.json
SECURITY_*.txt
security-audit.json

# Local backups (large, not source)
backups/
project backups*/
*.WRONG-BACKUP
*.OLD

# Android signing material — must NEVER be committed
*.jks
*.keystore
key.properties
